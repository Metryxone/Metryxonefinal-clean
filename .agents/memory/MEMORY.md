- [Role title → curated Role-DNA crosswalk](role-title-crosswalk.md) — free-text job titles → onto_role_competency_profiles (NOT ont_roles) for matching; abstain-never-fabricate, Coverage⟂Confidence, distinctive-token guard; canonical job substrate is job_postings (employer_jobs fallback); flag talentMatching, read-only at match time.
- [Ecosystem-activation re-certification](ecosystem-activation-recertification.md) — flag-gated read-only journey validator; question set MUST live in the service (single source for report/API/panel), tag construction-verified questions activation_na (exclude from activation count, else "no data yet" lies); verdict STRUCTURAL-only, activation reported alongside never composited, null≠0.
- [Live Employer Ecosystem Activation (MX-103X)](employer-ecosystem-activation.md) · [job-store projection (MX-103W)](employer-job-store-projection.md) — flag-gated read-only audit/cert of the 9-stage hiring funnel; job-store is SPLIT (posting→job_postings, assessment/interview→employer_jobs) so a true E2E must bridge it + match needs a real onto_role_competency_profiles role; exercisable≠real-adoption (smoke vs audit, never composited); substrate AND required-tables; outcome abstains <k_min=30; MX-103W: projecting job_postings→employer_jobs needs INSERT mapped to REAL divergent cols (CREATE IF NOT EXISTS is a no-op; query information_schema first), reversible=inactive never DELETE, Readiness(structural)≠Adoption(0 honest).
- [Outcome Intelligence Activation (MX-102X)](outcome-intelligence-activation.md) — flag-gated read-only composer unifying SIX realized-outcome types over existing engines (own 6-type taxonomy, don't mutate live validation-loop OUTCOME_TYPES); Coverage⟂Confidence never composited; demo MUST be excluded from coverage AND ledger in lockstep or coverage self-inflates.
- [Cert flag-set must match live workflow](cert-flagset-must-match-live-workflow.md) — a flag-gated cert composer reads FF_* at runtime; truncated flags depress Activation/journey/Adoption artificially. Honest lever = turn built-but-dormant flags ON in the REAL Backend API workflow then run composer with the SAME set; flags don't seed data (dormant pipes stay 0); 4 dims (assessment-approval/employer-adoption/knowledge-depth/outcome-k_min) can't hit 100% without fabrication.
- [Enterprise Certification (MX-105X)](enterprise-certification-mx105x.md) — top-level read-only flag-gated composer; FOUR axes Structural⟂Activation⟂Adoption⟂Outcome-Confidence NEVER composited (verdict STRUCTURAL-only), null≠0; aggregate views must COMPOSE the sub-views not re-issue ad-hoc SQL (recompute drift → review reject).
- [Global Intelligence composer (MX-76X)](global-intelligence-compose-never-throws.md) — read-only flag-gated (globalIntelligence) composer over existing global assets (zero DDL); a never-throws GET does NOT inherit probing from REUSED readers (wrap createLocalization.profile or it 500s); 7-region crosswalk reconciles 3 live taxonomies, AFRICA/LATAM empty-honest, role-DNA geo variant null by design, DEFAULT_CURRENCY is an object.
- [Competency→EI dimension mapping (3.2)](competency-ei-dimension-mapping.md) · [EI History & Progression (3.11)](ei-history-progression.md) — pg "inconsistent types deduced for $1" when one bind feeds INSERT...SELECT varchar AND EXISTS text id (cast each $N); never-throws wraps EVERY db read; lazy ensure-schema mirrors migration CHECK; 3.11: ONE trend impl in trend-engine.ts (dashboard re-imports), two history sources (scoring_runs/ei_profile_snapshots), ≥2 MEASURED gate, NULL never 0, risk dims invert direction; compose-never-recompute.
- [Readiness engine axis honesty](readiness-engine-axis-honesty.md) — Structural counts ONLY config/reference tables; seeding demo rows into a runtime table mis-mapped to Structural is metric gaming — move it to Data/Intelligence; seed must fail-fast (exit 1) or a partial seed poisons the audit.
- [Adaptive Assessment Activation (MX-100X P4)](adaptive-difficulty-activation.md) — flag adaptiveDifficultyActivation; live served bank is 100% medium → SERVED difficulty can't shift (target/readiness thresholds do; honest ceiling); senior bands==legacy ladder (flag-ON senior byte-identical); Role-DNA expected_level read path (role→onto_roles→onto_dna_profiles→competency_runtime_weights, EMPTY→null→stage anchor, scale 0–100 guarded not coerced); smoke keeps flag OFF (503-before-auth), no-op test must prove ordering not a tautology.
- [onto_competency_type_map NOT NULL cols](onto-competency-type-map-not-null.md) — confidence (default 'high') AND evidence are both NOT NULL; an upsert passing confidence=NULL 500s (default only applies when column OMITTED); manual/bulk = confidence 'high' + real evidence + needs_review=true.
- [Runtime activation traps (HTTP-driven)](runtime-activation-traps.md) — activate via real endpoints not SQL; employer register ALTERs employer_jobs created lazily by recruiter-postings (bootstrap GET /api/career/recruiter-postings first); CAPADEX needs sdi_items migration+restart; shared dev/prod DB so demo data must be @example.com-purgeable; candidate_master keys on email, users.id is varchar.
- [Usage metering dimensions (Phase 6.5)](usage-metering-dimensions.md) — 3 counting kinds (period_count/level=latest/credit_balance); fail-closed quota needs pg_advisory_xact_lock in a txn or writers overrun; reads to_regclass-probe (no ensure-schema).
- [Invoice & GST Engine](invoice-gst-engine.md) — flag invoiceGstEngine; refund/credit docs require a REFUNDED capadex_payment (subscriptions have no refund ledger → abstain, never fabricate); getSellerConfig lazily inserts seller row state_code=NULL so seed/UPDATE must call it FIRST; txn-safe ON CONFLICT numbering.
- [Merged task backfills don't reach the live DB](merged-task-data-not-in-live-db.md) — task-agent data backfills write to the ISOLATED env; merge carries CODE + migration DDL only, NOT rows; verify live activation by querying the shared DB, re-run seeds in prod.
- [Mission Control aggregator honesty math](mission-control-aggregator.md) — read-only /api/admin/mission-control folds ~45 nullable counts into 8 widgets; keep null(absent)≠0(empty), sumN preserves null, coverage denom = ALL declared sources not just present ones.
- [Enterprise Governance console](enterprise-governance-console.md) — read-only flag-gated console COMPOSING the governanceRbacV2 write subsystem (RBAC/approvals/audit/security) + Data Gov + renormalised Compliance index; don't call existing governance engines on GETs (they run DDL — reuse only the PURE deriveCategory); a silent-zero count() helper makes unreadable indistinguishable from empty → onError must flip degraded.
- [Enterprise Command Center (IA + Product Command Centers)](enterprise-command-center.md) — concernsPool IS the main DATABASE_URL pool; runtime=capadex_session_telemetry; read-only never-throws aggregator family (status≠score, snapshot=only write path); additive UX shell traps (collide-sticky, opt-in max-width, no deep-link w/o routing).
- [Platform Intelligence Console (6.10)](platform-intelligence-console.md) — read-only console composing commercial engines into 7 metric groups; global app.use('/api/admin') auth gate makes route-level flag-first ordering moot (unauth always 401, never strict-503) so smoke asserts {401,403,503}; every unmeasurable rate = null + explicit note.
- [EIOS 28-pillar architecture (EP-EIOS-98X)](eios-architecture.md) — eios-core.ts (P3,6–17) + eios-intelligence.ts (P18–28+cert); 31 routes all requireAuth; 71 cert checks target ≥98%; lazy schema eios_campaigns/scenarios/workforce_plans/outcome_tracking; k_min=30 in P18.
- [Flag-gated additive discipline](eios-worldclass-flag-discipline.md) — byte-identical-OFF includes SCHEMA (gate the DDL); a GET must never write (capture=explicit POST); honest counts from rowCount not input cardinality; file-registry flags absent from /api/admin/feature-flags so probe the gated endpoint (res.ok) + conditional-spread the nav tab to hide it OFF; see also [admin-tab](flag-gated-admin-tab-byte-identical.md).
- [Talent Intelligence Graph + employer demo-seed/calibration](employer-tig-architecture.md) — tig_* 9 entity types, manages/belongs_to/exhibits edges, LBI keyed by user_email; calibration write-once snapshot, Brier/ECE RAW, borrowed prior never upgrades TRUST, LEARNED from Hired/Rejected + predicted_prob_at_decision (≥30→calibrated); employer requireAuth SESSION-only → seed server-side, mark demo @example.com; see also [demo-seed](employer-demo-seed-calibration.md).
- [Employer Portal (EP-98)](employer-portal.md) — 7 employer_* tables; `values`→`values_list` reserved-keyword trap; M5 auth gap fixed via app.use('/api/m5',requireAuth); intel tabs frontend-computed; candidate drawer surfaces Coverage(domains) vs Confidence(calibration) SEPARATELY + monotonic-token guard so async hiring data never misattributes to wrong candidate.
- [Entitlement gate scope isolation](entitlement-gate-scope.md) — gateSessionEntitlement defined inside registerCapadexRoutes is NOT visible to sibling exported fns in the same file; each exported function needs its own local `requireEntitlement(pool,{sessionParam:'id'})` instance.
- [WCL pattern-trend chain](wcl-pattern-trend-chain.md) — WCL1/WCL2/WCL3 are compute-on-demand (no persist table); keyed by guest_email resolved from sessionId; IntelligenceLayers fetches BOTH /api/competency/intelligence/summary (CIE) and /api/intelligence/wcl (WCL) independently; FF_FORECAST_INTELLIGENCE=1 required in workflow command.
- [CAPADEX composite activation](capadex-composite-activation.md) · [4-tier ontology self-bootstrap](capadex-ontology-self-bootstrap.md) — composite: session_signals.session_id now uuid, /patterns returns patterns[]+composites[] , postCompletionHooks fires pipeline fire-and-forget; ontology auto-creates ONLY via shared ensureSignalOntologySchema (seeder+mapping-engine+hub import it), existence ≠ population (still needs manual seed).
- [Report Factory execution engines](report-factory-engines.md) — pdf-renderer (pdfkit, /tmp/rf_exports), benchmark-engine (13 metric resolvers, k=30 suppression), viz-data-resolver (6 data_source dispatchers); zero rows in dev = honest (no completed sessions); fire-and-forget export via setImmediate.
- [Future Readiness Platform (FRP)](frp-platform.md) — FF_FUTURE_READINESS flag; 10 frp_* tables; 5-signal FRI (skill_durability 30%/market_alignment 25%/adaptability 20%/learning_velocity 15%/role_resilience 10%); routes /api/frp/* + /api/admin/frp/*; tab id=future-readiness zone=intelligence; nav id=frp-admin.
- [Competency Ontology architecture](competency-ontology-architecture.md) — 12-layer hierarchy; table prefixes ont_/map_/ref_/ver_/lfc_/gov_; CAPADEX bridge via concern_bridge_tag (NOT concern_id — disjoint spaces); schema bug: pool.query().catch(()=>null) swallows ALL DDL in the batch.
- [Competency Framework Intelligence — Phase 1 index](competency-framework-intelligence-phase1-index.md) — "competency assessment Phase 1" canonical index is IN CODE (competency-intelligence.ts markers): foundation+1.1/1.2/1.4/1.5/1.6/1.7, NO 1.3 or 1.8-1.15; live shared DB drifts from prior audit (master_ext/hierarchy empty); count DISTINCT competency_id not rows; routes 401-not-503 = flag ON.
- [Career Graph Intelligence (CGI) architecture](cgi-architecture.md) — 16 cg_* tables; 5 pure engines; flag FF_CAREER_GRAPH; k=10 readiness cohort; ensureSchema via app.use middleware (not requireAuth); literal-before-param route order enforced.
- [P-R2 Occupation Graph & Snapshot](p-r2-occupation-snapshot-lessons.md) · [Occupation seed self-contained](occupation-seed-self-contained.md) — ensureSchema lazy-init blocked by requireAuth; CREATE TABLE+index on a missing column silently breaks schemaPromise; seed = idempotent service file called at route-registration that inserts its OWN base occupation+skill rows (ON CONFLICT DO NOTHING) or new domains skip at link-time; W9 needs O*NET/ESCO bulk import not manual seed.
- [EI Admin Health + enhancements](ei-admin-health-panel.md) · [EI Formula Authority (8-dim)](ei-formula-authority.md) — EIHealthPanel + /api/admin/ei/* (ei_events lazy-schema, nav ei-health), rec confidence deterministic (gap/transferability/mobility), auto-snapshot Hook 23, drop alert ≥5pts ([enh](ei-intelligence-enhancements.md)); Formula single source employabilityEngine.ts (backend 6-dim is a separate entity-resolved score, not the gauge driver).
- [PR0 Shared Foundation Activation](pr0-shared-foundation-activation.md) — workflow flags via configureWorkflow only; wcl0 is email-keyed (join users first); career-memory dual-store pattern; resolveEffectiveUserId IDOR guard; career_recs backfill join.
- [pg COUNT string-coercion](pg-count-string-coercion.md) · [longitudinal null→0](longitudinal-consumption-null-coercion.md) — pg COUNT() returns STRINGS (`=== 0` silently upgrades stubs to PARTIAL → Number() before compare); Number() over NULLABLE score cols turns null→0 (fake datapoint flips consumed false→true) → map null/''→NaN BEFORE isFinite, 0 snapshots→0% honest.
- [LBI architecture & ⟂ Competency separation](lbi-architecture-state.md) · [separation](competency-vs-lbi-separation.md) — LBI (student lbi_*) and Competency Assessment (onto_*/competency_*) are TWO independent products BY DESIGN (never bridge lbi_→onto_competencies, audit each on its own side); LBI: P-R7 consolidated W1–W10, System C retired, lbi-intelligence.ts 14 routes, chain trigger every session, ensureSchema lazy (hit /api/lbi/interventions to bootstrap).
- [WC-C8A security remediation patterns](wc-c8a-security-patterns.md) — requireSuperAdmin defined late in routes.ts, use inline guard before its definition; MFA verify must sanitize password hash; `crypto.hash()` doesn't exist — use scrypt; CONDITIONAL GO ≠ GO when e2e paths untested; G15 Razorpay smoke-test + MFA e2e still outstanding before launch.
- [WC-L5 Memory Intelligence](wcl5-memory-intelligence.md) — compose-only snapshot of WC-L0→L4 into wcl5_memory; inherit confidence, fail-closed, semantic memory_key (UPSERT idempotent); trend folds into behaviour_memory NOT forecast (9 vs 4); recall/trend/forecast denom = longitudinal users not all sessions; backfill-time user-state is duplicated per session (disclose).
- [Replit deployment-pane secrets](replit-deployment-pane-secrets.md) — deployment-pane secrets are invisible to repo grep; never "confirm absent" from .replit alone; use DECISION_REQUIRED (not FAIL) for owner-decided config gates.
- [WC-3 L5A/L5B Question Stage & Context Intelligence](wc3-question-stage-intelligence.md) — derive per-q canonical stage from type/response/polarity/narrative (dead `stage` col), key by clarity SERIAL id; context is an ABSENT orthogonal axis (~80% Qs legit context-free → coverage ceiling ~half); lexicons over-count → probe tight-vs-loose; see also [L5B](wc3-context-intelligence-audit.md).
- [CAPADEX decision-chain gaps & taxonomy traps](capadex-decision-chain-gaps.md) · [Decision Orchestration (WC-6)](capadex-decision-orchestration.md) — stage taxonomy SPLIT (BE 5-stage vs FE CAP_* 4-code, reconcile before stage-keyed UX); growth-plan EXISTS in M5 (wire don't rebuild); destinations mostly REAL (mentor+M5), gap=conductor + journey→M5 bridge + decision→subscription mapping + entitlement; rich sub schema NOT tier/features/modules; only LBI a real product, mentoring=fallback+stub.
- [Derived scoring/prioritization audit honesty](derived-scoring-audit-honesty.md) — a scoring audit over a grounding audit must map readiness deterministically, never silent-upgrade a stub, flag safety force-elevation as an explicit tier exception, add a traceability appendix.
- [Commercial readiness, entitlement & spine (dual-axis)](wc-c1-commercial-readiness.md) — Structural(code) ⟂ Activation(live+sold) NEVER composited; commerce reads fail CLOSED (ledger=capadex_payments paid only), cert excludes demo on EVERY metric, NAMED metric keeps its denominator, flag-gate before ensure-schema, metered identity=server principal (IDOR); package→entitlement permanently absent (users has no email col). See [wc7c](wc7c-commercial-activation.md) · [spine](commercial-task7-spine.md) · [wc-c6a](wc-c6a-productization-audit.md) · [pkg-gap](subscription-package-entitlement-gap.md) · [wc-c5](wc-c5-renewal-readiness.md) · [wc-c4](wc-c4-entitlement-enforcement.md) · [wc-c3](wc-c3-entitlement-enforcement.md) · [wc-c2](wc-c2-entitlement-readiness.md) · [wave-2](commercial-wave-2.md) · [razorpay](commercial-spine-razorpay-security.md) (payment verify requires local↔gateway linkage/IDOR, webhook fails CLOSED, idempotency null-replay→409 not 500).
- [WC-8/WC-9 Future-Readiness audit & activation](wc8-future-readiness-audit.md) — finding = front-half (ask/classify) present, back-half (outcome models+products) missing; back-half pipeline EXISTS (outcomes/journeys=seed ROWS by model_key, no DDL, reuse construct vocab → day-1); keystone = ONE AI-skill taxonomy + occupation exposure; boundary short tokens (naive 'ai' 50×); never conflate "structural path 8/9" w/ content-ready; see also [WC-9](wc9-future-readiness-activation.md).
- [WC-3 L5C/L5D Runtime Outcome & Journey Projection](l5c-runtime-outcome-projection.md) — Question→BridgeTag→Construct→OutcomeModel deterministic, construct-reachable ≠ outcome-reachable; residual DERIVED from projection output never a broader ontology (over-claim=fabrication); journey downstream of outcome (coverage ceiling), mentoring catch-all dilutes, mirror the live exam guard; see also [L5D](l5d-runtime-journey-projection.md).
- [CAPADEX → Career Builder behaviour bridge](career-behavior-bridge.md) — per-job ranking modifiers need a per-row feature (not a user scalar); gate "absent" on real-data marker, not a neutral fallback object.
- [WC-L0 User Intelligence Foundation](wcl0-user-intelligence-foundation.md) — persistence-only (no new engine); Coverage≠Accuracy (report separately), runtime persona ≠ user-selected, behaviour NULL when no graph (never from score), snapshot lever = ensure-at-least-one (append-only, no unique constraint).
- [Assessment preview per-instance variation](assessment-preview-personalization.md) — "looks the same for every X" → copy keyed on a coarse bucket; surface the specific entity, and never length-gate personalization (truncate for display only).
- [WC-7B Activation Intelligence (Tier A)](wc7b-activation-intelligence.md) · [Career OS orchestration engines](career-os-orchestration-engines.md) — orchestrators COMPOSE never recompute; two fire-and-forget builders of one idempotent snapshot RACE → one sole builder; L5B relevance_risk is TEXT (map to bounded penalty or NaN kills tiebreak); when surfacing in a tab thread the FULL ctx (profile/openJobs/eiScore) or whole categories silently never fire.
- [WC-11 Decision Intelligence measurement](wc11-decision-intelligence-measurement.md) — a script calling the flag-gated composer measures ITS OWN process flag state (run with FF_*=1 + print them); "decision-driven" must be a provenance check (route_key identity / source), not a tautology; "SELECT-only" is a lie if shared getters run ensure-schema DDL.
- [Behaviour namespace alignment (WC-L0)](behaviour-namespace-alignment.md) — projection regex matched POSITIVE keys the runtime never emits → construct dims empty; fix routes concern keys to dims as deficit with NEUTRAL CAP (≤50, never a strength); positive wins; reach still gated by graph-capture gap.
- [Build & deploy tooling reality](build-and-deploy-tooling.md) — prod runs backend on tsx (never compiled/typechecked); only real launch gate is frontend vite build; don't try to add a backend tsc gate.
- [Audit-artifact PII masking](audit-artifact-pii-masking.md) — audit/measure scripts write committed .md files; mask user emails to irreversible user_<sha256> pseudonyms before writeFileSync, or the honesty pass FAILs on PII even when the math is right.
- [Launch-validation evidence integrity](launch-validation-evidence-integrity.md) — an "evidence-derived" GO/NO-GO script still self-inflates: caught query errors masquerade as findings; a blocker clears on owner ATTESTATION not a SEND; annotate hardcoded PASS rows as static; secret-presence = "mechanism armed" not "rotation occurred"; mask PII in prose.
- [Cross-module event-driven sync](cross-module-event-sync.md) — adaptive bus already exists (connect, don't rebuild); identity-space trap: event-log user_id is BIGINT but refresh-state scope_id is TEXT, career-seeker ids are UUID — never Number()-coerce.
- [Outcome attribution baseline drift](outcome-attribution-drift.md) — net drift PER METRIC (not per axis), don't force an axis hint, realize a stored-outcome preference end-to-end, give each axis a real series or let it degrade — else it reads as fabricated causation.
- [AQ-2R runtime metadata activation](aq2r-runtime-metadata-activation.md) — a within-pool re-rank only moves dims that VARY within the pool; AQ-2 fixed signal/age/behavior/capability at tag granularity → signal/construct deltas are 0 by construction (measure differentiability, don't tune weights).
- [Concern → Signal mapping](concern-signal-mapping.md) · [Bridge-tag coverage & resolver](bridge-tag-coverage.md) — exact bridge tag weak → token-semantic PRIMARY, band stats over Tier-3 only, orphans flagged never fabricated; keep ONE shared resolver (runtime+tooling import, never copy), "covered"=has clarity rows ≠ canonical membership, retire GENERAL_CONCERN by sibling remap not bulk-gen.
- [Audit runtime fidelity](audit-runtime-fidelity.md) — an offline audit reporting a runtime behaviour must mirror the prod resolver+order (orphan tags REMAP, not literal static fallback); emit per-row resolves_to/route; regenerate artifacts, never hand-edit.
- [AQ-1 audit-engine honesty](aq1-audit-engine-honesty.md) — when the audit IS the deliverable: measure headline claims at runtime (never hardcode), code thresholds must match the prose definition, cite engine JSON not discovery notes, half-open band ranges in code, never tune the verdict.
- [Simulation harness calibration](simulation-harness-calibration.md) — black-box validator: simulant emits LIVED answers (engine reverse-scores `(-)`), concernMatch is SEMANTIC (engine remaps to master buckets), seed-404 is its own dimension, and the harness is ALLOWED to fail — never tune metrics to force a pass.
- [CAPADEX hypothesis investigation](capadex-hypothesis-investigation.md) — additive bands/governance over existing engines; DB engine flags (snake) vs static config gate (camel, default-off) — never mix; ranking surface is read-only, selection stays byte-identical.
- [IntroPhase progressive-reveal form](introphase-progressive-form.md) — formInvalid is unordered OR; CTA label is an ORDERED cascade that must mirror field reveal order; clear any name sentinel (e.g. 'Anonymous') on persona reset.
- [Concern resolver repair (RRP-1)](concern-resolver-repair.md) — IDF-weighted match is PRIMARY rank, cascade only breaks ties (concern_id LAST); honest tie metric = concern_id_fallback not weighted ties; short-intent drops the 60% gate; clamp maxIdf>0 or specificity is NaN; synonym/missing-construct are audit-only, never fabricate.
- [CAPADEX concern routing & clarity-picker filters](capadex-concern-routing-fallback.md) · [picker](capadex-clarity-picker-filters.md) — tier-4 resolveCapadexConcern fires only when exact bank absent; derive adultness from age (>=24) not persona key alone (empty-persona adults else mis-route to student banks); recover clarity-row age/persona via the BRIDGE TAG not concern_cluster (cross-tag twin); persona SOFT (63% provider-only families) + age HARD else students dead-end to static fallback.
- [Proxy-language reframe engine](proxy-language-engine.md) — name anchors only on a SUBJECT-position `you` (prep-objects → them, even if first); pure module + parity-detector cleanup; never auto-author abstract stems; options stay first-person by contract.
- [Confidence clarity option generation](confidence-option-generation.md) — confidence-type clarity Qs all shared one bland Very Low→Very High scale; frontend renders DB options verbatim; regenerate context-aware text by feeling-word, preserve *_score, dry-run CSV then --apply.
- [Orphan concern fallback & remap leakage](orphan-concern-fallback.md) · [remap](bridge-tag-orphan-remap-leakage.md) — empty-spine concerns get ONE low-confidence general-support insight built read-only at /explain (never seeded → can't inflate composites/patterns), emit only when spine fully empty; off-topic/wrong-age clarity Qs are usually an orphan bridge tag mis-remapped by a GREEDY keyword rule (family age gate can't catch) → fix via a hand-verified ORPHAN_BRIDGE_TAG_FALLBACK override.
- [Static serving & served-doc single-sourcing](static-serving-and-docs.md) — backend/public is build output (deploy copies frontend dist into it); serve single-source docs via a symlink in frontend/public, never parallel copies.
- [CAPADEX investigation surfaces](capadex-investigation-surfaces.md) — coverage classify→NONE not default; utility self-disables when map empty + dead_end is human-review/can-be-0 (strengths-canon: see replit.md + behavior-graph-consumer.md).
- [Question registry governance triage](question-registry-governance.md) — "low-signal/unused" buckets must gate on SYSTEM-WIDE usage existing (cold start = whole bank floods); status transitions human-only; bucket dedup by group key (Σ small n²) + snapshot metrics, read paginated.
- [Adaptive Questioning (Phase B)](adaptive-questioning.md) — /adaptive-next MUST rebuild the pool via the SAME analyze envelope+analyzeConcern; client response_value is a naive index distress proxy by design; arm only when no prefill; every failure falls back to the batch (200, never 500).
- [Vite HMR vs mockup-sandbox port hijack](vite-hmr-mockup-port-hijack.md) — "preview/HMR flapping (server connection lost / WebSocket closed)" = canvas mockup Vite squats externalPort 80; .replit ports can't be hand-edited & mockup workflow can't be deleted; cure is HMR clientPort = app port (5000), not 443.
- [Atomic GENERAL_CONCERN catch-all](atomic-bridge-general-concern.md) — the big "unmapped" atomic bucket is mostly POSITIVE strengths (correct, not a bug); remap ONLY negative catch-all via hand-verified family map, flag the rest, never fabricate; runtime keys off atomic_signal_id not bridge tag.
- [CAPADEX report tone & palette](capadex-report-tone-palette.md) — help-seeking report must read hopeful/light, not near-black; preview lightened to navy→teal; keep header/CTA deep enough for white text (~4.5:1), don't revert to the old dark canon.
- [L5C bridge-tag → construct crosswalk](l5c-bridge-construct-crosswalk.md) — INERT clarity bridge_tag→construct map for Outcome Projection; honest reachability ceiling ~85.6% (UNMAPPED residual is institutional/holistic, never force); construct-reachable ≠ outcome-reachable; umbrella-token HIGH = overreach vector, re-adjudicate before runtime.
- [Clarity master_bridge_tag derivation](clarity-bridge-tag-classifier.md) — join is bucket-level not concern_id; tag is DERIVED (curated prefix→token-heuristic→UNMAPPED), not authored; a clarity CSV without the column silently defaults all rows to UNMAPPED & tanks joinability — shared classifier on import + backfill, residual UNMAPPED never fabricated.
- [CAPADEX Problem Intelligence Layer (PIL) — all phases](pil-problem-intelligence-layer.md) — strictly additive, stop-for-approval per phase; master row = 3 framings of ONE construct (cap↔problem WITHIN-row); curated frames w/ quality gate that REJECTS generic fallbacks (never fabricate); see topic file for per-phase traps (1.5/1.6/2/3/5).
- [CRA Precision Bridge (20/20)](candidate-cra-precise-bridge.md) — all 20 CRA codes map to DISTINCT genome rows (exact + curated-synonym + SME-authored distinct comps, NEVER fabricated synonyms); NOT_ON_PRECISE_SCALE derived-empty; write-path existence-guarded.
- [MX-301B engine assessment-receipt validation](mx301b-engine-activation-validation.md) — `received` needs engine's own `measurable` AND measured values flowing (talent-match measurable:true but evidence_mix.measured=0 = precise⟂domain-proxy split → wired_no_data); mutation routes probed UNAUTH-ONLY (read-only); self-auth bound to demo subject; flag-OFF=honest not-activated.
- [MX-301D persona experience validation](mx301d-persona-experience-validation.md) — "one assessment everywhere": compute-score reads cra_scores (NOT onto ledger→candidate self=empty wired_no_data, substrate split); career-intelligence/-readiness :subject are super-admin-ONLY (admin drill-down), candidate self surfaces are /api/career/hub/* (no :id); "same assessment" proof = ONE onto substrate + ≥2 independent persona lenses, numeric fingerprint optional-strengthener never the gate; flags-unenable'able→founder 503 honest flag_gated.
- [Flag enable when configureWorkflow limit-blocked](workflow-limit-flag-via-env-var.md) — configureWorkflow counts .replit-DEFINED workflows (12/10, uneditable; removeWorkflow only drops runtime) → cant append FF_*; instead setEnvVars dev-only (feature-flags reads process.env via envOverride), restart, reversible, prod stays OFF.
- [Audit-deliverable evidence regeneration](audit-evidence-regeneration.md) — when a task BOTH fixes defects and emits a committed scan artifact, re-run the scanner AFTER the fixes or the evidence contradicts the "fixed" claims (review FAILs on honesty).
- [MX-301E UI certification honesty](mx301e-ui-certification-honesty.md) — an honesty "100%" means tighten the detector FIRST (catch more real gaps) then close them; report mechanically-scannable defect-class closure (Gaps 1–4) SEPARATELY from the inherent visual-review ceiling, never composite; readsData=actual call not import, real state gap=no-loading-AND-no-error, promote deliberate secondary palette to a named token.
- [MX-301 demo report-pack measurability + purge](mx301-demo-report-pack.md) — radar/heatmap read computeTypeProfile REAL fields (domain_proxy only, exclude no-bank-code dom_strategic); multi-axis needs a multi-TYPE blueprint; readiness needs role_id w/ real onto_role_competency_profiles; demo @example.com purge FK-blocked by admin_audit_logs.admin_user_id (NO ACTION) → purge audit before users + assertPurged throws.
