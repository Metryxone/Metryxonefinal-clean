- [Readiness engine axis honesty](readiness-engine-axis-honesty.md) — Structural counts ONLY config/reference tables; seeding demo rows into a runtime table mis-mapped to Structural is metric gaming — move it to Data/Intelligence; seed must fail-fast (exit 1) or a partial seed poisons the audit.
- [onto_competency_type_map NOT NULL cols](onto-competency-type-map-not-null.md) — confidence (default 'high') AND evidence are both NOT NULL; an upsert passing confidence=NULL 500s (default only applies when column OMITTED); manual/bulk = confidence 'high' + real evidence + needs_review=true.
- [Runtime activation traps (HTTP-driven)](runtime-activation-traps.md) — activate via real endpoints not SQL; employer register ALTERs employer_jobs created lazily by recruiter-postings (bootstrap GET /api/career/recruiter-postings first); CAPADEX needs sdi_items migration+restart; shared dev/prod DB so demo data must be @example.com-purgeable; candidate_master keys on email, users.id is varchar.
- [Commercial spine Razorpay security](commercial-spine-razorpay-security.md) — payment verify must require local↔gateway linkage (IDOR), webhook fails CLOSED when configured, idempotency null-replay → 409 not 500.
- [Invoice & GST Engine](invoice-gst-engine.md) — flag invoiceGstEngine; refund/credit docs require a REFUNDED capadex_payment (subscriptions have no refund ledger → abstain, never fabricate); getSellerConfig lazily inserts seller row state_code=NULL so seed/UPDATE must call it FIRST; txn-safe ON CONFLICT numbering.
- [Merged task backfills don't reach the live DB](merged-task-data-not-in-live-db.md) — task-agent data backfills write to the ISOLATED env; merge carries CODE + migration DDL only, NOT rows; verify live activation by querying the shared DB, re-run seeds in prod.
- [Mission Control aggregator honesty math](mission-control-aggregator.md) — read-only /api/admin/mission-control folds ~45 nullable counts into 8 widgets; keep null(absent)≠0(empty), sumN preserves null, coverage denom = ALL declared sources not just present ones.
- [Enterprise Command Center (IA + Product Command Centers)](enterprise-command-center.md) — concernsPool IS the main DATABASE_URL pool; capadex_sessions/concerns_master DON'T exist here (runtime=capadex_session_telemetry); LBI has no ts col → trend honestly unavailable; sidebar multi-tier keys on openGroups not labsOpen; read-only never-throws aggregator family (search/action/notification/readiness/health) — status≠score (health status=worst, score=mean), info/unknown excluded from score, snapshot=only write path, icon-import trap; additive UX shell (AdminShellBar breadcrumbs+context bar) — two sticky top-0 collide so wrap header+bar in ONE sticky; global max-width content change is NOT byte-identical (keep opt-in); no deep-link action w/o URL routing.
- [EIOS 28-pillar architecture (EP-EIOS-98X)](eios-architecture.md) — eios-core.ts (P3,6–17) + eios-intelligence.ts (P18–28+cert); 31 routes all requireAuth; 71 cert checks target ≥98%; lazy schema eios_campaigns/scenarios/workforce_plans/outcome_tracking; k_min=30 in P18.
- [Flag-gated additive: 3 review traps](eios-worldclass-flag-discipline.md) — byte-identical-OFF includes SCHEMA (gate the DDL); a GET must never write (capture=explicit POST); honest counts from rowCount not input cardinality.
- [Flag-gated admin tab byte-identical-OFF](flag-gated-admin-tab-byte-identical.md) — file-registry flags absent from /api/admin/feature-flags; probe the gated endpoint (res.ok) + conditional-spread the nav tab so it hides when OFF.
- [Talent Intelligence Graph (EP-98-W2)](employer-tig-architecture.md) — tig_* namespace; 9 entity types (incl. organization); manages/belongs_to/exhibits edges; LBI keyed by user_email (NOT user_id); 25 STRUCTURAL_CHECKS; calibration: write-once snapshot, Brier/ECE RAW, borrowed prior never upgrades TRUST.
- [Employer demo-seed & calibration](employer-demo-seed-calibration.md) — employer requireAuth is SESSION-only (no token) so seed via exported runHiringAnalysis+buildTIGForOrg server-side; calibration LEARNED from Hired/Rejected stages + predicted_prob_at_decision, ≥30→calibrated; mark demo (source='Demo Seed'/@example.com).
- [Employer Portal (EP-98)](employer-portal.md) — 7 employer_* tables; `values`→`values_list` reserved-keyword trap; M5 auth gap fixed via app.use('/api/m5',requireAuth); intel tabs frontend-computed; candidate drawer surfaces Coverage(domains) vs Confidence(calibration) SEPARATELY + monotonic-token guard so async hiring data never misattributes to wrong candidate.
- [Entitlement gate scope isolation](entitlement-gate-scope.md) — gateSessionEntitlement defined inside registerCapadexRoutes is NOT visible to sibling exported fns in the same file; each exported function needs its own local `requireEntitlement(pool,{sessionParam:'id'})` instance.
- [WCL pattern-trend chain](wcl-pattern-trend-chain.md) — WCL1/WCL2/WCL3 are compute-on-demand (no persist table); keyed by guest_email resolved from sessionId; IntelligenceLayers fetches BOTH /api/competency/intelligence/summary (CIE) and /api/intelligence/wcl (WCL) independently; FF_FORECAST_INTELLIGENCE=1 required in workflow command.
- [CAPADEX composite intelligence activation](capadex-composite-activation.md) — capadex_session_signals.session_id was varchar (now uuid); /patterns endpoint returns both patterns[] + composites[]; postCompletionHooks already fires pipeline fire-and-forget.
- [CAPADEX 4-tier ontology self-bootstrap](capadex-ontology-self-bootstrap.md) — domains/families/signals/atomic auto-create ONLY via shared services/signal-ontology-schema.ts ensureSignalOntologySchema (seeder+mapping-engine+hub import it); existence ≠ population (rows still need manual seed).
- [Report Factory execution engines](report-factory-engines.md) — pdf-renderer (pdfkit, /tmp/rf_exports), benchmark-engine (13 metric resolvers, k=30 suppression), viz-data-resolver (6 data_source dispatchers); zero rows in dev = honest (no completed sessions); fire-and-forget export via setImmediate.
- [Future Readiness Platform (FRP)](frp-platform.md) — FF_FUTURE_READINESS flag; 10 frp_* tables; 5-signal FRI (skill_durability 30%/market_alignment 25%/adaptability 20%/learning_velocity 15%/role_resilience 10%); routes /api/frp/* + /api/admin/frp/*; tab id=future-readiness zone=intelligence; nav id=frp-admin.
- [Competency Ontology architecture](competency-ontology-architecture.md) — 12-layer hierarchy; table prefixes ont_/map_/ref_/ver_/lfc_/gov_; CAPADEX bridge via concern_bridge_tag (NOT concern_id — disjoint spaces); schema bug: pool.query().catch(()=>null) swallows ALL DDL in the batch.
- [Career Graph Intelligence (CGI) architecture](cgi-architecture.md) — 16 cg_* tables; 5 pure engines; flag FF_CAREER_GRAPH; k=10 readiness cohort; ensureSchema via app.use middleware (not requireAuth); literal-before-param route order enforced.
- [P-R2 Occupation Graph & Snapshot Schema](p-r2-occupation-snapshot-lessons.md) — ensureSchema lazy-init is blocked by requireAuth; CREATE TABLE + index on missing column silently breaks schemaPromise; seed pattern = idempotent service file called at route-registration time.
- [Occupation seed self-contained pattern](occupation-seed-self-contained.md) — seed must insert base occupation+skill rows itself (ON CONFLICT DO NOTHING) or new domains silently skip at link-time; W9 targets need O*NET/ESCO bulk import, not manual seed.
- [EI Admin Health Panel (P-R3A)](ei-admin-health-panel.md) — EIHealthPanel + /api/admin/ei/* routes; ei_events lazy-schema; confidence on Recommendation is deterministic (gap/transferability/mobility driven, never tuned); nav id=ei-health.
- [EI intelligence enhancements (5 features)](ei-intelligence-enhancements.md) — breakdown in trajectory, auto-snapshot Hook 23, report archiving to rf_generated_reports (report_type='ei_intelligence'), adminUserId param, drop alert ≥5pts via sendEIDropAlert.
- [EI Formula Authority (8-dim)](ei-formula-authority.md) — single source in employabilityEngine.ts; classifiers must NOT be duplicated inline; backend 6-dim is a separate entity-resolved score, not the gauge driver.
- [PR0 Shared Foundation Activation](pr0-shared-foundation-activation.md) — workflow flags via configureWorkflow only; wcl0 is email-keyed (join users first); career-memory dual-store pattern; resolveEffectiveUserId IDOR guard; career_recs backfill join.
- [P-X1 Shared Product Foundation](px1-shared-foundation.md) — CAPADEX built the shared infra (WC-L5/L0/L1, career-behavior-adapter); EI+LBI don't consume it; CB partial; engineering ceiling post-F4 ~50%, not 70%.
- [pg COUNT string-coercion trap](pg-count-string-coercion.md) — pg COUNT() returns strings not numbers; `=== 0` strict-eq silently upgrades stubs to PARTIAL; always Number() wrap before comparisons in audit scripts.
- [LBI architecture state](lbi-architecture-state.md) — P-R7 consolidated (W1–W10); System C retired; 14 routes in lbi-intelligence.ts; chain trigger fires after every session; ensureSchema lazy (hit /api/lbi/interventions to bootstrap); structural readiness 97%.
- [WC-C8A security remediation patterns](wc-c8a-security-patterns.md) — requireSuperAdmin defined late in routes.ts, use inline guard before its definition; MFA verify must sanitize password hash; `crypto.hash()` doesn't exist — use scrypt; CONDITIONAL GO ≠ GO when e2e paths untested; G15 Razorpay smoke-test + MFA e2e still outstanding before launch.
- [WC-L5 Memory Intelligence](wcl5-memory-intelligence.md) — compose-only snapshot of WC-L0→L4 into wcl5_memory; inherit confidence, fail-closed, semantic memory_key (UPSERT idempotent); trend folds into behaviour_memory NOT forecast (9 vs 4); recall/trend/forecast denom = longitudinal users not all sessions; backfill-time user-state is duplicated per session (disclose).
- [Replit deployment-pane secrets](replit-deployment-pane-secrets.md) — deployment-pane secrets are invisible to repo grep; never "confirm absent" from .replit alone; use DECISION_REQUIRED (not FAIL) for owner-decided config gates.
- [WC-3 L5A Question Stage Intelligence](wc3-question-stage-intelligence.md) — derive per-question canonical stage from type/response/polarity/narrative (NOT the dead `stage` col); key by clarity SERIAL id; single_select votes nothing; Clarity/Growth skew + rare Mastery is an honest catalogue gap.
- [WC-3 L5B Context Intelligence audit](wc3-context-intelligence-audit.md) — context is an absent orthogonal axis (no field); ~80% of questions legitimately context-free so coverage ceiling is ~half; lexicons over-count (Leadership/Digital ~½ noise) → always probe tight-vs-loose; ambient-smear vs shallow-gap drift modes.
- [CAPADEX decision-chain gaps & taxonomy traps](capadex-decision-chain-gaps.md) — stage taxonomy SPLIT (BE 5-stage vs FE CAP_* 4-code, reconcile before stage-keyed UX); a growth-plan DOES exist in M5 (wire it, don't rebuild) but is decoupled from the CAPADEX chain; only LBI is a real product, mentoring=universal-fallback-AND-stub.
- [CAPADEX Decision Orchestration (WC-6)](capadex-decision-orchestration.md) — destinations mostly REAL (mentor+M5 real, correcting WC-5); gap is the conductor + journey→M5 bridge + decision→subscription mapping + entitlement; rich sub schema is NOT tier/features/modules.
- [Derived scoring/prioritization audit honesty](derived-scoring-audit-honesty.md) — a scoring audit over a grounding audit must map readiness deterministically, never silent-upgrade a stub, flag safety force-elevation as an explicit tier exception, add a traceability appendix.
- [WC-7C Commercial Activation](wc7c-commercial-activation.md) — commerce reads (safety + ledger ownership) must fail CLOSED not open; never sell into a stub; D6 low-conf→show_options; offer_fit is directional; STAGE_PRICES duplicated → keep lockstep w/ capadex-payments.
- [Commercial Spine — entitlement classes/metering/recurring revenue](commercial-task7-spine.md) — revenue forecast reuses last+slope NOT projectForecast (clamps 0..100); cert excludes demo on EVERY metric incl. JOIN-based collections; flag-gate before ensure-schema = zero DDL OFF; metered identity is server principal not client email (IDOR); feature-class enforcement primitive separate from stage gate.
- [WC-C6A Subscription Productization audit](wc-c6a-productization-audit.md) — name-collision guard (recompute prior metric on ITS denom + name yours a distinct SUBSET); seed-as-stub (unpriced/null-validity rows aren't sellable — "run the seed" never a fix); cross-audit tier consistency (unexercised real-keys path = gated-real not real); empty catalog = COUNT not 0%; 90% activation EARNED not engineered.
- [Subscription package entitlement gap](subscription-package-entitlement-gap.md) — package→entitlement permanently absent: users table has no email col; identity bridge email→children→student_subscriptions requires migration; ₹299–₹1499 prices CONFIRMED 2026-06-10.
- [WC-C5 renewal-readiness audit](wc-c5-renewal-readiness.md) — earning model (B2C ladder) can't renew by design, renewable model (packages) has 0 sales → renewal coverage is not_measurable (0/0), keystone = establish renewable population FIRST; Activation = per-capability BINARY "can fire NOW" (data-driven, not hardcoded); reconcile WC-C1's "structurally complete" overclaim → PARTIAL.
- [WC-C4 entitlement enforcement gate](wc-c4-entitlement-enforcement.md) — flag check must be FIRST sync stmt (query-spy proves 0 DB touch = the ONLY byte-identical state); "0 paid rows ≠ 0 blocked" (paid stage_code w/o payment 402s, project over ALL sessions via the REAL handler); gate reads session.guest_email server-side but the email is client-asserted upstream (don't say "never trusted"); 402 vs 503 fail-CLOSED; ledger scope = capadex_payments paid only.
- [WC-C3 Entitlement Enforcement audit](wc-c3-entitlement-enforcement.md) — premise "enforcement→>90%" is FALSE on the 6×5 metric (that's productization; enforcement only→20%); ≥90% needs Live-SKU re-baseline (user DECISION); guard enumeration must credit inline+spread+global app.use (naive grep over-reports the admin gap, 340→212/623); RBAC is a SEPARATE axis quarantined from entitlement %; "no route-level guard DETECTED (static; in-handler uninspected)" not "unprotected"; paid endpoints = session-UUID bearer tokens, zero requireEntitlement; effort day-band ONLY in roadmap prose never snapshot.
- [WC-C2 Entitlement Readiness audit](wc-c2-entitlement-readiness.md) — objective's NAMED metric keeps its original denominator (answer it first); offer a tighter re-baseline as a user DECISION, never silent-swap; one requireEntitlement guard = access_enforcement + provisioning (2 cells, not double-count); package SKU entitlement-disjoint (resolver reads ledger only, no feature col, child-keyed grants) so keystone can't lift it; UUIDs = bearer tokens (no backend paywall); activation 0 until real keys+volume.
- [WC-C1 commercial readiness audit](wc-c1-commercial-readiness.md) — dual-axis Structural(code exists) vs Activation(live+sold) NEVER composited; Activation 95% = real revenue not engineering; cap unexercised integrations at gated-real(4); a binary monetization cell needs a LITERAL entitlement feature string (mentor_access yes, career_builder no — its link is in the journey route); read FEATURE_FLAGS object = deploy-default posture; recompute (don't copy) same-day reconciliation.
- [Commercial Wave 2 — commercial lifecycle readiness](commercial-wave-2.md) — compose-only layer over an EMPTY revenue substrate: report Structural vs Data/Activation as TWO axes never composited; cap "after" at gated-real (flags default-OFF + no consumer + no data) so ~80% not 95%; flag aspirational headlines not data-supported; renewal in_grace excludes cancelled; upsell needs prior paid.
- [WC-8 Future-Readiness audit method](wc8-future-readiness-audit.md) — boundary short tokens (naive 'ai' inflated 50×); report L5B context coverage AND literal; finding = front-half (ask/classify) present, back-half (outcome models + products) missing; keystone = one shared AI-skill taxonomy.
- [WC-9 Future-Readiness activation design](wc9-future-readiness-activation.md) — back-half pipeline already EXISTS (outcomes/journeys = seed ROWS keyed by model_key, no DDL); reuse construct vocab so outcomes activate day-1; reskill/upskill are growth-plan mechanisms not outcomes; keystone = ONE asset pair (AI skill taxonomy + occupation exposure); never conflate "structural path 8/9" with "content-ready".
- [L5C Runtime Outcome Projection](l5c-runtime-outcome-projection.md) — Question→BridgeTag→Construct→OutcomeModel is deterministic; construct-reachable ≠ outcome-reachable; report residual set must be DERIVED from the projection's own output, never a broader ontology list (over-claim = fabrication).
- [WC-3 L5D Runtime Journey Projection](l5d-runtime-journey-projection.md) — journey is downstream of outcome (coverage ceiling); a 4th partial layer LOWERS the per-q mean (don't force 95%); mentoring catch-all dilutes confidence; mirror the live exam guard or you over-claim Competitive Exam; family_support dormant is honest.
- [CAPADEX → Career Builder behaviour bridge](career-behavior-bridge.md) — per-job ranking modifiers need a per-row feature (not a user scalar); gate "absent" on real-data marker, not a neutral fallback object.
- [WC-L0 User Intelligence Foundation](wcl0-user-intelligence-foundation.md) — persistence-only (no new engine); Coverage≠Accuracy (report separately), runtime persona ≠ user-selected, behaviour NULL when no graph (never from score), snapshot lever = ensure-at-least-one (append-only, no unique constraint).
- [Assessment preview per-instance variation](assessment-preview-personalization.md) — "looks the same for every X" → copy keyed on a coarse bucket; surface the specific entity, and never length-gate personalization (truncate for display only).
- [WC-7B Activation Intelligence (Tier A)](wc7b-activation-intelligence.md) — orchestrator COMPOSES WC-3 L1/L2/L3 (never recomputes); two fire-and-forget builders of one idempotent snapshot RACE → make one the sole builder; L5B relevance_risk is TEXT not numeric (map to bounded penalty or NaN kills the tiebreak).
- [Career OS orchestration engines](career-os-orchestration-engines.md) — additive intelligence engines re-shape (never recompute) data; when surfacing in a tab, thread the FULL ctx (profile/openJobs/eiScore) or whole categories silently never fire.
- [WC-11 Decision Intelligence measurement](wc11-decision-intelligence-measurement.md) — a script calling the flag-gated composer measures ITS OWN process flag state (run with FF_*=1 + print them); "decision-driven" must be a provenance check (route_key identity / source), not a tautology; "SELECT-only" is a lie if shared getters run ensure-schema DDL.
- [Behavior Graph consumer surface](behavior-graph-consumer.md) — CAPADEX signals are concern-DIAGNOSTIC, not strengths; "strengths" come ONLY from CSI positive_factors / positive growth, never from raw signal magnitude.
- [Behaviour namespace alignment (WC-L0)](behaviour-namespace-alignment.md) — projection regex matched POSITIVE keys the runtime never emits → construct dims empty; fix routes concern keys to dims as deficit with NEUTRAL CAP (≤50, never a strength); positive wins; reach still gated by graph-capture gap.
- [Backend route hot-reload](backend-route-hot-reload.md) — a newly-added backend route file may return `Cannot GET` until you restart the Backend API workflow; restart before smoke-testing.
- [Build & deploy tooling reality](build-and-deploy-tooling.md) — prod runs backend on tsx (never compiled/typechecked); only real launch gate is frontend vite build; don't try to add a backend tsc gate.
- [Audit-artifact PII masking](audit-artifact-pii-masking.md) — audit/measure scripts write committed .md files; mask user emails to irreversible user_<sha256> pseudonyms before writeFileSync, or the honesty pass FAILs on PII even when the math is right.
- [Launch-validation evidence integrity](launch-validation-evidence-integrity.md) — an "evidence-derived" GO/NO-GO script still self-inflates: caught query errors masquerade as findings (SELECT only real cols), a blocker must clear on owner ATTESTATION not a SEND attempt, hardcoded PASS rows contradict "derived" (annotate static + date), "rotation occurred" from secret-presence is really "mechanism armed (restart required)", mask PII in prose too.
- [Cross-module event-driven sync](cross-module-event-sync.md) — adaptive bus already exists (connect, don't rebuild); identity-space trap: event-log user_id is BIGINT but refresh-state scope_id is TEXT, career-seeker ids are UUID — never Number()-coerce.
- [Outcome attribution baseline drift](outcome-attribution-drift.md) — net drift PER METRIC (not per axis), don't force an axis hint, realize a stored-outcome preference end-to-end, give each axis a real series or let it degrade — else it reads as fabricated causation.
- [AQ-2R runtime metadata activation](aq2r-runtime-metadata-activation.md) — a within-pool re-rank only moves dims that VARY within the pool; AQ-2 fixed signal/age/behavior/capability at tag granularity → signal/construct deltas are 0 by construction (measure differentiability, don't tune weights).
- [Concern → Signal mapping engine](concern-signal-mapping.md) — exact bridge tag is weak → token-semantic is PRIMARY; band stats over Tier-3 rows only; orphans flagged never fabricated; some Tier-3 signals legit have no composite.
- [Longitudinal consumption null-coercion trap](longitudinal-consumption-null-coercion.md) — building a trend series with Number(x) over NULLABLE score cols turns null→0 (a fake datapoint, flips consumed false→true); map null/''→NaN BEFORE the isFinite filter; 0 snapshots → 0% is the honest finding.
- [Bridge-tag coverage & resolver](bridge-tag-coverage.md) — keep ONE shared resolver (runtime + tooling import it, never copy); "covered"=has clarity rows ≠ canonical set membership; retire GENERAL_CONCERN by sibling remap, never bulk-generate questions.
- [Audit runtime fidelity](audit-runtime-fidelity.md) — an offline audit reporting a runtime behaviour must mirror the prod resolver+order (orphan tags REMAP, not literal static fallback); emit per-row resolves_to/route; regenerate artifacts, never hand-edit.
- [AQ-1 audit-engine honesty](aq1-audit-engine-honesty.md) — when the audit IS the deliverable: measure headline claims at runtime (never hardcode), code thresholds must match the prose definition, cite engine JSON not discovery notes, half-open band ranges in code, never tune the verdict.
- [Simulation harness calibration](simulation-harness-calibration.md) — black-box validator: simulant emits LIVED answers (engine reverse-scores `(-)`), concernMatch is SEMANTIC (engine remaps to master buckets), seed-404 is its own dimension, and the harness is ALLOWED to fail — never tune metrics to force a pass.
- [CAPADEX hypothesis investigation](capadex-hypothesis-investigation.md) — additive bands/governance over existing engines; DB engine flags (snake) vs static config gate (camel, default-off) — never mix; ranking surface is read-only, selection stays byte-identical.
- [IntroPhase progressive-reveal form](introphase-progressive-form.md) — formInvalid is unordered OR; CTA label is an ORDERED cascade that must mirror field reveal order; clear any name sentinel (e.g. 'Anonymous') on persona reset.
- [Concern resolver repair (RRP-1)](concern-resolver-repair.md) — IDF-weighted match is PRIMARY rank, cascade only breaks ties (concern_id LAST); honest tie metric = concern_id_fallback not weighted ties; short-intent drops the 60% gate; clamp maxIdf>0 or specificity is NaN; synonym/missing-construct are audit-only, never fabricate.
- [CAPADEX concern routing fallback](capadex-concern-routing-fallback.md) — tier-4 resolveCapadexConcern only fires when exact bank absent; derive adultness from age (>=24) not persona key alone, or empty-persona adults mis-route to student banks.
- [CAPADEX clarity-picker filters](capadex-clarity-picker-filters.md) — recover clarity-row age/persona via the BRIDGE TAG not concern_cluster (not unique → cross-tag twin); persona is SOFT (63% provider-only families) + age HARD, else students dead-end to static fallback.
- [Proxy-language reframe engine](proxy-language-engine.md) — name anchors only on a SUBJECT-position `you` (prep-objects → them, even if first); pure module + parity-detector cleanup; never auto-author abstract stems; options stay first-person by contract.
- [Confidence clarity option generation](confidence-option-generation.md) — confidence-type clarity Qs all shared one bland Very Low→Very High scale; frontend renders DB options verbatim; regenerate context-aware text by feeling-word, preserve *_score, dry-run CSV then --apply.
- [Orphan concern fallback insight](orphan-concern-fallback.md) — empty-spine concerns get ONE low-confidence general-support insight, built read-only at /explain (never seeded), so it can't inflate composites/patterns; emit only when spine fully empty.
- [Bridge-tag orphan remap leakage](bridge-tag-orphan-remap-leakage.md) — off-topic/wrong-age clarity Qs usually = orphan bridge tag mis-remapped by a GREEDY keyword rule; family-level age gate can't catch it; fix via a hand-verified ORPHAN_BRIDGE_TAG_FALLBACK override.
- [Static serving & served-doc single-sourcing](static-serving-and-docs.md) — backend/public is build output (deploy copies frontend dist into it); serve single-source docs via a symlink in frontend/public, never parallel copies.
- [CAPADEX investigation surfaces](capadex-investigation-surfaces.md) — coverage classify→NONE not default; utility self-disables when map empty + dead_end is human-review/can-be-0; strengths ONLY from CSI positive_factors/positive longitudinal, NEVER raw signal magnitude.
- [Question registry governance triage](question-registry-governance.md) — "low-signal/unused" buckets must gate on SYSTEM-WIDE usage existing (cold start = whole bank floods); status transitions human-only; bucket dedup by group key (Σ small n²) + snapshot metrics, read paginated.
- [Adaptive Questioning (Phase B)](adaptive-questioning.md) — /adaptive-next MUST rebuild the pool via the SAME analyze envelope+analyzeConcern; client response_value is a naive index distress proxy by design; arm only when no prefill; every failure falls back to the batch (200, never 500).
- [Vite HMR vs mockup-sandbox port hijack](vite-hmr-mockup-port-hijack.md) — "preview/HMR flapping (server connection lost / WebSocket closed)" = canvas mockup Vite squats externalPort 80; .replit ports can't be hand-edited & mockup workflow can't be deleted; cure is HMR clientPort = app port (5000), not 443.
- [Atomic GENERAL_CONCERN catch-all](atomic-bridge-general-concern.md) — the big "unmapped" atomic bucket is mostly POSITIVE strengths (correct, not a bug); remap ONLY negative catch-alls via hand-verified family map, flag the rest, never fabricate; runtime keys off atomic_signal_id not bridge tag.
- [CAPADEX report tone & palette](capadex-report-tone-palette.md) — help-seeking report must read hopeful/light, not near-black; preview lightened to navy→teal; keep header/CTA deep enough for white text (~4.5:1), don't revert to the old dark canon.
- [L5C bridge-tag → construct crosswalk](l5c-bridge-construct-crosswalk.md) — INERT clarity bridge_tag→construct map for Outcome Projection; honest reachability ceiling ~85.6% (UNMAPPED residual is institutional/holistic, never force); construct-reachable ≠ outcome-reachable; umbrella-token HIGH = overreach vector, re-adjudicate before runtime.
- [Clarity master_bridge_tag derivation](clarity-bridge-tag-classifier.md) — join is bucket-level not concern_id; tag is DERIVED (curated prefix→token-heuristic→UNMAPPED), not authored; a clarity CSV without the column silently defaults all rows to UNMAPPED & tanks joinability — shared classifier on import + backfill, residual UNMAPPED never fabricated.
- [CAPADEX Problem Intelligence Layer (PIL)](pil-problem-intelligence-layer.md) — strictly additive, stop-for-approval per phase; each master row = 3 framings of ONE construct so cap↔problem map is WITHIN-row; Phase 1.5 reclassifies by display_label NAME (distress-lead→Problem), similarity needs minShared≥2 to kill hub blobs.
- [Express literal vs :param route order](express-literal-vs-param-route-order.md) — /:id matches any segment incl. `export.csv`; register literal sub-paths BEFORE the catch-all /:id or you get the wrong handler ("Invalid id").
- [PIL gap-detection engine (8D)](gap-detection-engine.md) — dead-end source-capability must be DERIVED from live edges (never hardcode terminals); weak=deg1 only for CORE types; gap classes intentionally overlap (not double-count); competency orphans + problem_framing dead-ends are honest findings, never "fix" them; extend recordGraphAudit's closed event_type union for new audit events.
- [PIL Similarity engine (8C)](pil-similarity-engine.md) — same-category Jaccard over FULL neighbor sets; hub_only judged over full intersection; derived index write ≠ graph mutation; bulk-upsert (per-row 40k hangs); explainability=refs-resolve not shared>0; concern/intervention hub-dominated is a real finding.
- [PIL graph traversal engine](pil-graph-traversal.md) — read-only over materialized pil_kg_*; capped resolvers make output order-dependent, so determinism needs BOTH ORDER BY load + sorted adjacency lists; lineage is honestly partial, never fabricated.
- [kg_edges table-name collision](kg-table-name-collision.md) — `kg_edges`/`kg_` is owned by the LIVE Employability graph (20260522, ~60 rows, no kg_nodes); PIL Phase-8's same-named graph can't bootstrap here and its materialize would WIPE it — PIL must be re-namespaced (pil_kg_*).
- [CAPADEX knowledge-graph linkage map](knowledge-graph-linkages.md) — only REAL linkage rows become edges (provenance-stamp each); concern→signal is tier3-only (composite/atomic/orphan refs are traps); bridge_tag is the join hub; competency has NO link row (stays orphan, don't fabricate); cpbm.behavior_id + concern_clarity_map ARE real edges (only the competency/human_problem joins are traps); intervention_library.id is UUID → String() it, never Number().
- [Knowledge graph read-only discipline](knowledge-graph-readonly.md) — a "read-only API" must be GET-only; never expose a materialize/rebuild route that DELETEs+INSERTs on the public router; keep mutation as an internal script/job fn.
- [Recommendation Intelligence (Phase 7)](recommendation-intelligence.md) — recs anchor on ACTIVE construct (no orphan); coverage≠chain-complete; a duplicate method+path silently shadows a later one — grep before adding a route.
- [Report intelligence composition](report-intelligence-composition.md) — reporting layer composes (never produces) intelligence; statement-coverage ≠ chain completeness so cap readiness band when degraded; strengths trace off-chain; multi-id routes strict-400 bad uuids, never silently drop.
- [Runtime Intelligence Experience Layer (Phase 6B)](runtime-intelligence-experience-layer.md) — stakeholder toggle over the runtime pipeline; Parent strengths ONLY from discoverStrengths, Counselor Priority Risks ONLY active/dominant lifecycle (mirror ACTIONABLE_LIFECYCLES), pass report palette as prop.
- [Runtime Intelligence Pipeline (Phase 6A)](runtime-intelligence-pipeline.md) — read-only resolver COMPOSES the guidance engine (front half = signals + cap/problem framing); degraded must trip on ANY unresolved hop incl. middle, never just the tail; cap/problem live in one map row.
- [WC-3 Outcome runtime + L5C crosswalk](wc3-outcome-crosswalk-runtime.md) — construct cascade: hypotheses spine → DEAD pattern tier (capadex_session_patterns has no construct_key) → flag-gated crosswalk; isolate a throwing tier or a later tier never runs; folds need `@>`-guarded write-no-op UPDATEs; reachable-but-actionless is honest.
- [Runtime guidance (PIL) activation](runtime-guidance-activation.md) — archetype_concern_map has NO name col (join archetype_library); most sessions lack master_concern_pk so resolver must cascade+degrade; behavior_library statements carry {token} slots (render legibly, never filter/fabricate); flag gate before UUID guard.
- [Clarity questions xlsx import quality](clarity-xlsx-import-quality.md) — source clarity files reuse question_id across DIFFERENT questions (upsert-by-id silently drops data → de-collide), and a CW/RC subset ships with option labels shifted into polarity/reverse/anchor cols.
- [PIL Behavioral Intelligence (Phase 1.6)](pil-problem-intelligence-layer.md) — observable-behavior layer btwn capability↔problem; curated frames + generic fallbacks the quality gate REJECTS (<15) to surface weak coverage, never fabricate; severity=frequency adverb, age=slot lexicon; resolve SERIAL FK ids via post-insert SELECT.
- [PIL Archetype Intelligence (Phase 2)](pil-problem-intelligence-layer.md) — curated top-down archetypes (bottom-up clustering ruled out); fix a "name-only" architect Fail with grounding PROVENANCE + graph propagation + honest readiness reweight (don't tune); ~59% name_only is the real 6-input ceiling; idempotent ALTER guard or 2nd persist 42703s.
- [PIL Human Intelligence (Phase 3)](pil-problem-intelligence-layer.md) — curated per-archetype human packs (problems+5 stakeholders+5 emotions) = curation not fabrication; alignment lexicon must drop generic META tokens (want/people/work/professional/problem) or single-hit check inflates; dup rate GLOBAL not per-list; honest 100/0/89.7 vs 85/85/<10.
- [C-2 question enrichment (Context/Archetype)](c2-question-enrichment.md) — archetype derives from EXISTING narrative_style (~90%) & varies within-tag → near-co-dominant w/ dev_stage; context is tag-determined (low within-tag, value is ROUTING); enrich in a SEPARATE reversible table, UNCLASSIFIED-not-fabricate, same-basis before/after.
- [WC-3 layered intelligence composition](wc3-layer-composition.md) — layers compose downward (L2 Outcome takes stage from L1, never re-derives); actions library-backed ONLY (catalog construct_keys must be REAL intervention_library keys); behavioural_hypotheses.session_id is TEXT no-FK; read routes flag-gate before uuid.
- [WC-3 outcome/journey coverage ceiling](wc3-outcome-coverage-ceiling.md) — coverage capped by the construct-reachability ceiling; outcome-model expansion can only reach that ceiling, exceeding it (→90%+) needs the crosswalk UNMAPPED-reduction lever; journey==outcome (mentoring fallback); never force a residual into a wrong model.
- [PIL Intervention Intelligence (Phase 5)](pil-problem-intelligence-layer.md) — pronoun-NEUTRAL anchors so one set serves 5 stakeholder templates; don't add a 6th table to back a read endpoint — derive the dup review read-only via the pure auditor from persisted rows (thread ?refresh into its cache); habit/skill HORIZON_DAYS=0 is legit.
- [WC-L2 Forecast Intelligence](forecast-intelligence.md) — a forecast is just `last+slope` extrapolation of an EXISTING trend at its OWN confidence (no new model); coverage is data-bound (needs ≥2 sessions/user); audit denominators eligible-only & band-prose==band-fn; expansion: forecastability = depth-gate AND per-dim trendability (row-present ≠ dim-trendable; risk is sparsest), loss table must reconcile owners×layers, empty depths = "no data", Decision NOT in WC-L2 runtime API. WC-L3: NULL concern/construct linkage w/ concern_name present = STALE persistence not capture-failure → re-run existing resolvers read-only (master 1→9/9, construct 2→5/9) lifts outcome 3→9 & forecast 0→2/2 w/o new capture; mirror resolveConstructsFromClarityBank to MEASURE outcome reachability; journey NEVER null (mentoring fallback) so "routed"=100% trivial, only "confident" mirrors outcome (never call journey==outcome measured); "reachable"=structural routing, exclude 0-response from forecast.
- [WC-L4 Intervention Intelligence](wcl4-intervention-intelligence.md) — first ACTION layer COMPOSES: exactly ONE generator (library-backed outcome actions), all else ANNOTATES; degraded journey/decision = ZERO; confidence INHERITED (MAX, no blend); annotations can't generate (concern-trend-but-no-action is honest, not a bug); encode behaviour_risk/risk-forecast polarity inversion; Structural vs Activation axes separate.
- [Behaviour signal capture & backfill](behaviour-signal-backfill.md) — construct dims fed ONLY by runEvidenceRuntime 'activated' signals (NOT signal-ingest); capture runs UNCONDITIONALLY at /respond (flag gates reporting only) so old empty sessions = code-rollout gap fixable only by offline backfill→buildBehaviorGraph→persist; 0-response sessions are un-backfillable (true ceiling, don't inflate); audit artifacts mask emails at capture.
- [Behaviour trend / longitudinal behaviour](behaviour-trend-longitudinal.md) — behaviour dims live in wcl0_user_intelligence (motivation/confidence/risk/engagement/adaptability; learning_style categorical→never trended); reuse WC-L1 trend math; NULL=missing never 0; empty-spine ceiling is real (persist~100% but dim-coverage~22%, trend 0%) — never inflate; Curiosity/Consistency/Self-Regulation don't exist.
- [WC-3 state-capture audit (outcome/journey empty)](wc3-state-capture-audit.md) — 0 rows ≠ "not wired"; check the WORKFLOW command for flag overrides; never-throws hook makes "never fired" vs "fired-silently" indistinguishable (need a smoke test); populated state tables came from backfill scripts not the live hook; use updated_at for completion timing; outcome UNCLASSIFIED on empty spine, crosswalk tier unions primary_construct_key too.
- [WC-P2 consumption vs readiness](wc-p2-consumption-vs-readiness.md) — measure Consumption Rate (provenance) and Activation Readiness (data-fired) as TWO independent metrics, never merged; completed-session headline + all-session transparency; cold-start lows (anon→no persona, no repeat snapshots) are honest, don't inflate; classifier treats error/sentinel reasons as non-consuming + emit a reason-classification ledger.
- [C-1A pilot validation](c1a-pilot-validation.md) — report differentiation coverage-weighted not raw; signal backfill needs PER-QUESTION evidence (tag grounding alone → UNCLASSIFIED); flagship signal-blind tags are UNgrounded (signal backfill ≠ cheapest); context=routing value not within-tag spread; archetype is the yield winner.
- [C-1 question semantic uniformity](c1-question-semantic-uniformity.md) — question metadata is fully populated but FLAT (differentiability ~0.096, all 325 tags ≥0.80 uniform); only dev_stage differentiates within a tag; capability single-valued in 100% of tags; Context+Archetype columns DON'T EXIST (report 0%, never fabricate).
- [WC-1C-Y YELLOW signal grounding](wc1c-y-signal-grounding.md) — YELLOW grounds at moderate so Trust/AIS composites rise LESS than raw coverage (the gap IS the finding); enforce the [TAU_LOW,TAU_HIGH) band in code; mutation+verify in ONE txn with hard-gate rollback, dry-run = same path then ROLLBACK.
- [WC-1B signal grounding](wc1b-signal-grounding.md) — ground bridge tags via ADDITIVE provenance-stamped reuse-linkage tables (never mutate relational_bridge_tag); coverage=union(native OR table), baseline native-only for rerun determinism, enforce tag∈concerns_master; runtime wiring out-of-scope so forecasts are directional.
- [WC-1 ontology coverage audit](wc1-ontology-coverage-audit.md) — signal layer is the systemic gap (only 25/328 tags have signals); interventions/recs are a SEPARATE construct namespace (lexical proxy, never a real edge); capability==problem (same map row); RAG must discriminate; emit signal reconciliation or the 15,972-vs-6,761 headline reads as fabricated.
- [WC-1A signal grounding](wc1a-signal-grounding.md) — the 7.6% signal coverage is a LABELLING problem: 370 rich families filed under 28 catch-all tags, so 0 tags need new signals (re-link, don't author); match tag→FAMILY, calibrate GREEN/YELLOW vs already-grounded tags, RED=0 is "under calibrated threshold" not absolute proof (defend with sensitivity table).
- [Predictive & Outcome Intelligence (Phase 9)](predictive-intelligence.md) — predictions COMPOSE descriptive layers (never black-box); archetype levers only from resolved KG lineage (never global LIMIT); mitigation earned via shared lexicon dim (no empty-dims auto-match); no realized outcomes ⇒ outcome_coverage=0 + NO accuracy claim; routes never-500 via degraded JSON not next(err).
- [Graph validation certifier (PIL 8F)](graph-validation-certifier.md) — a coverage certifier whose empty-denominator rates default to 1 + vacuous validations will certify an EMPTY graph as READY; add a viability hard gate (nodes/edges/statements>0) FIRST; source-trace is informational not gated; determinism = canonical key-sorted JSON deep-eq.
- [PIL Graph Explainability (Phase 8E)](graph-explainability.md) — two HONEST layers: local-support (out-deg≥1, =100%) vs source-traceability; interventions reach concern/question, recommendations/runtime_interventions dead-end on the construct SINK (never fabricate the hop); audit row counts = GRAPH counts via indexSummary, not statement counts.
- [Post-launch usage audit (no deployment)](post-launch-usage-audit-no-deployment.md) — a "post-launch usage" audit collapses if the app was never deployed; check prod existence FIRST (executeSql env=production errors "Deploy your app first"); no deploy ⇒ zero usage by definition; never substitute dev/test data; frozen-evidence scripts need a re-run guard + logged errors + EXACT (redacted-not-fabricated) quotes.
- [career_seeker_profiles schema](career-seeker-profiles-schema.md) — PK is `user_id` (NOT `id`); no `id` col at all; data is JSONB; every query must use `WHERE user_id = $1`.
- [onto_* vs ont_* namespace bridge](onto-vs-ont-namespace-bridge.md) — TWO disjoint competency ontologies; user pages read onto_role_weights (curated, text ids), O*NET-derived weights live in map_role_competency (ont_*, int ids); bridgeOnetDerivedWeights maps role-by-title + comp-by-name, additive/idempotent, so the Estimated badge fires; ont_* empty in dev so badge correctly grey.
- [Role crosswalk (app role → ont_roles)](role-crosswalk.md) — bridges 3 disjoint role-id spaces (profile labels / legacy catalog ids / ONET_*-ROLE_* codes) to ont_roles.code so the O*NET library is consumed; separate /role-library endpoint NOT ROLE_PRIORITIES (COG* comps are a disjoint namespace); unresolved/no-ratings → honest null/empty, never fabricate; ont_* ships empty (seed in dev, import-onet in prod).
- [Career Passport system](career-passport.md) — 12 cp_* tables, SECTION_CONFIG whitelist for generic CRUD, syncPassportFromPlatform bridges capadex/frp/competency, section_visibility gates public share reads.
- [CI Engine enhancements (E1-E5)](ci-engine-enhancements.md) — career_seeker_goals uses JSONB data col (title/desc/category inside JSONB, not columns); role_families has no competency codes → static ROLE_CRITICAL is the only option; reassessment reminders in competency_reassessment_reminders (lazy schema).
- [Backend API workflow port-detection](backend-workflow-port-detection.md) — restart false-fails (DIDNT_OPEN_A_PORT) when .replit lacks the 8080 port mapping; .replit is uneditable, so re-register via configureWorkflow(waitForPort).
- [Super-admin login + dev MFA](python-upload-service.md) — a cross-service schema collision on shared tables breaks Node auth; super-admin login is MFA-gated and emails the code via ZOHO (absent in dev → read it from DB to test).
- [Python bulk-upload service](python-upload-service.md) — SECOND backend (FastAPI in backend-main/, `Upload Service` workflow port 8000) sharing the DB; bootstrap create_all MUST stay scoped to uploader tables; question_options links by FK not code; llm_proxy can't run on Replit; /admin/* unauthenticated.
- [SuperAdmin global /api/admin gate + RIE schema drift](superadmin-admin-gate-and-rie-drift.md) — one authoritative app.use('/api/admin',auth→superadmin) gate; a lazy ensure-schema mirroring a migration STILL 42703s if the migration drifted from handler columns.
- [SuperAdminDashboard code-split](superadmin-dashboard-codesplit.md) — lazy() the ~160 panels behind ONE Suspense inside DialogsErrorBoundary; keep always-on chrome (CrisisAlertInbox/NotificationCenter/AdminSidebar/FrameworkPanel) STATIC; static+dynamic dup-import warning = missed-opt not defect.
- [SuperAdmin perf chunking + security visibility](superadmin-perf-and-security-visibility.md) — vite manualChunks catch-all returns undefined NOT "vendor" (circular/eager footgun); audit middleware global app.use('/api/admin') fire-and-forget, writes only mutating verbs status<400, coexists w/ semantic createAdminAuditLog (dual rows intended); dev-DB drift 500s most mutations so prove positive write via POST /api/admin/roles (role_definitions exists); sessions from express_sessions.
- [Per-framework admin gate gap](per-framework-admin-gate-gap.md) — global /api/admin gate misses /api/<framework>/admin/* (lbi/sdi/competency/commercial/concerns/invoice/short-assessments); each needs inline requireAuth+requireSuperAdmin; GET reads are the easy miss; some framework reads are public-by-design.
- [onto_* vs ont_* namespace split](onto-vs-ont-namespace.md) — Role DNA endpoint reads onto_* (SQL-migration seeded, no runner); runtime ontology-seed fills disjoint ont_* and never touches DNA; seed via scripts/seed-role-dna.ts.
- [Competency Assessment Blueprint Engine (Phase 2.1)](competency-blueprint-engine-phase21.md) — role→5-dim % mix; dims are onto_competency_TYPES not domains; derive from onto_competency_type_map weights; technical sparse/future_skills empty = honest 0%/gap; table onto_blueprint_dimension_mix off onto_assessment_blueprints; same competencyRuntime flag.
- [Competency Question Blueprint Engine (Phase 2.2)](competency-question-blueprint-phase22.md) — empty competency_question_templates bank → pools honestly empty; mapQuestion syncs canonical onto_competency_question_map; micro child-validated; reuses competencyRuntime flag.
- [Competency Runtime (Phase 2)](competency-runtime-phase2.md) — flag competencyRuntime; 7-code bank crosswalks DOWN to 5 onto-domains = domain_proxy score (auto-upgrades when onto_competency_question_map populated); dom_strategic UNMEASURABLE never scored; subject_id is operator-supplied so super-admin gate (requireAuth-only = IDOR).
- [Dual scoring ledgers](competency-runtime-dual-scoring-ledger.md) — rich scorer writes onto_competency_score_runs; runtime scoreInstance writes onto_competency_profiles (1 append-only row/run) NOT score_runs; any "scoring run" count must UNION both or runtime-scored subjects read as unscored.
- [Assessment Assembly Engine (Phase 2.3)](assessment-assembly-engine.md) — disjoint per-question→one-competency assignment (highest weight, tie id-asc) makes dupes structurally impossible; competency_question_templates.source∈{manual,generated,seed}; snapshot/restore onto_question_blueprints in seeds (real FK).
- [Question type canonical keys](question-type-canonical-keys.md) — DB question_type is the CANONICAL key (multiple_choice…), not short token (mcq); any option-score/Likert-vs-best-answer logic must normalize via validateQuestionType or it mis-scores non-Likert as Likert.
- [GET-never-writes vs lazy ensure-schema](cei-readonly-get-discipline.md) — a read-only composing engine's GET handlers must NOT reach a lazy ensure*Schema (CREATE = a write); keep DDL on the POST path only, GETs use a to_regclass probe + degrade (empty/zeroed/gap, never fail).
