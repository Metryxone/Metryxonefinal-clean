# Context & Objective
We have a critical UI/UX regression in the CAPADEX assessment interface where ordinal Likert-scale questions are erroneously rendering with the rank-by-importance UI layout (requiring multi-select ranking and a 'Confirm selections' CTA), as visible in "image_d535bf.png". 

Questions evaluating a progressive intensity spectrum (such as 'No Impact' to 'Severe Impact') must instead be routed directly through single-choice single-tap selection (`handleSingleTap`) with an automated 350ms auto-advance transition.

---

## Target Files to Modify
- `frontend/src/components/assessment/phases/CapadexClarifyPhase.tsx` (or your matching frontend workspace file containing the `isSingleChoiceQuestion` evaluator logic).

---

## Detailed Technical Changes Required

### 1. Harden the Heuristic Token Evaluator (`isSingleChoiceQuestion`)
Locate your single-choice check method (which currently looks for explicit tokens like `very low...very high`, `never...always`, etc., at a 60% threshold). Update it to robustly scan for impact-based and emotional intensity ordinal scales:

1. **Expand LIKERT_TOKENS**: Add the following keyword metrics to your token array:
   - `['no impact', 'mild impact', 'moderate impact', 'major impact', 'severe impact']`
   - `['not at all', 'a little bit', 'somewhat', 'quite a bit', 'very much']`

2. **Harden Stem Key Matching**: The regex or substring evaluation array must be expanded to catch emotional level checks. Update the prefix rules to intercept:
   - Stems starting with: `"how emotionally", "how stressful", "how impactful", "how difficult", "how severe"`
   - Stems containing: `"feel to you"`, `"rate your level"`, `"on a scale of"`

### 2. Implementation Template Example for Code Alignment
Ensure your boolean function incorporates the updated checks before assigning the layout component state:

```typescript
function isSingleChoiceQuestion(question: { question_stem: string; options: string[] }): boolean {
  const stem = question.question_stem.toLowerCase().trim();
  
  // 1. Explicit Stem Patterns
  const singleChoicePrefixes = [
    'how ready', 'how prepared', 'how confident', 'how comfortable', 'how likely',
    'how emotionally', 'how stressful', 'how impactful', 'how difficult', 'how severe'
  ];
  if (singleChoicePrefixes.some(prefix => stem.startsWith(prefix)) || stem.includes('feel to you')) {
    return true;
  }

  // 2. Exact or High-Affinity Token String Matches
  const targetOptions = question.options.map(o => o.toLowerCase());
  const impactTokens = ['no impact', 'mild impact', 'moderate impact', 'major impact', 'severe impact'];
  
  const impactMatchCount = targetOptions.filter(opt => 
    impactTokens.some(token => opt.includes(token))
  ).length;

  // If at least 60% of the choices match standard impact scale signatures
  if ((impactMatchCount / question.options.length) >= 0.6) {
    return true;
  }

  // Fall back to original LIKERT_TOKENS token calculation loop logic
  return standardLikertTokenCheck(targetOptions);
}