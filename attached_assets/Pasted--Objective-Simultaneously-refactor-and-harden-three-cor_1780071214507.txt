# Objective
Simultaneously refactor and harden three core operational domains within the CAPADEX platform to resolve the layout rendering regression, eliminate the 0-row telemetry database state, fix proxy pronoun grammar anomalies, and seed missing professional concern pipelines.

---

## Technical Specification 1: Authoritative Single-Select Routing & Layout Fork
File: `frontend/src/components/assessment/phases/CapadexClarifyPhase.tsx`

1. **Enforce a Single Source of Truth**: The function `isSingleChoiceQuestion(q)` must act as the absolute, unified gatekeeper for the layout fork. Do NOT introduce a parallel `isRankingApplicable()` predicate.
2. **Implement Explicit Table Property Guard**: Insert a short-circuit evaluator at the very top of `isSingleChoiceQuestion(q)`. If `q.response_type` is present and contains a non-empty string, immediately return `true`. 
3. **Preserve Structural Heuristic Fallbacks**: Keep the token checking mechanics (`looksLikeLikertScale` >= 60% exact-token match) and regex validations *exclusively* as fallbacks to process static-fallback questions that lack structured database attributes. Ensure `LIKERT_TOKENS` accurately checks for the expanded impact and emotional ladders:
   - Impact Ladder: `['no impact', 'mild impact', 'moderate impact', 'major impact', 'severe impact']`
   - Emotional Ladder: `['a little bit', 'somewhat', 'quite a bit', 'very much']`
4. **Widen Regex Scope**: Force single-select routing if the question stem begins with phrases evaluating intensity (`"how emotionally"`, `"how stressful"`, `"how impactful"`, `"how difficult"`, `"how severe"`) or explicitly targets categorical history (`/^what have you (already )?tried\b/i`).
5. **UI Fork Execution & State Hygiene**: When `isSingleChoiceQuestion` evaluates to `true`:
   - Hide the status footer completely (except the saving spinner) and remove the manual "Confirm selections" action block.
   - One option tap must immediately invoke `handleSingleTap(option)`, replace the standard alphabetical badges (A/B/C/D) with visual checkmarks, lock inputs, and execute a 350ms rapid auto-advance translation sequence.
   - Ensure a `useEffect` keyed tightly on `currentQuestion.id` explicitly clears out `ranked`, `selected`, and `submitting` states on every question transition to eliminate cross-step contamination.

---

## Technical Specification 2: Frontend Telemetry Flush & Sync-Ordering Execution
File: `frontend/src/components/assessment/phases/FreeAssessmentModal.tsx`

1. **State Isolation First**: Modify the `doCompleteStage` handler. At the absolute top of the function call stack, capture the current global tracking metrics inside a local `timingsSnapshot` variable. 
2. **Immediate Buffer Clearance**: Immediately following the snapshot assignment—and *before* any asynchronous network calls are awaited—execute `setQuestionTimings({})`. This local cache clearance is non-negotiable to prevent downstream telemetry items from leaking into subsequent assessment phases.
3. **Synchronous Lifecycle Flight**: Wrap an outbound network call to `POST /api/signals/ingest` inside an awaited, guarded `try/catch` block. This flight must be explicitly executed *after* the UI state shifts to `capadex_result` but *before* triggering the background report generation pre-fetch.
4. **Defensive Processing & Flags**: Only fire the payload write if `feature_flags.signal_intelligence` evaluates to true. Ensure that any network failures or server rejections (`!res.ok`) are caught safely, logged as console warnings, and bypassed so the user journey never blocks. Ensure the serialized JSON payload aligns precisely with the tracking schema:
```json
   {
     "session_id": "string (UUID format)",
     "timings": [{ "item_id": "string", "response_time_ms": 1200, "backtrack_count": 0 }],
     "stage_code": "string"
   }