Act as a Principal Full-Stack Engineer. The clarity questions are still repeating because the answered question tracking is dropping out of the stateless request cycle. Let's fix this core memory leak without any schema changes by explicitly passing the history state.

Apply these two matching updates immediately:

1. UPDATE THE BACKEND PICKER TO READ REQUEST HISTORY (backend/routes/capadex-concern-intelligence.ts):
- Look at the incoming route handler for our clarity picker. Ensure it explicitly looks for an array called `answeredIds` inside the request body (`req.body.answeredIds`).
- Before selecting the next batch of questions from our in-memory array or master list, run a strict filter:
  const availableQuestions = allMasterQuestions.filter(q => !req.body.answeredIds?.includes(q.id));
- If `availableQuestions` is empty because a session is long, fall back to the full list.
- Apply a clean array shuffle over `availableQuestions` before slicing the next questions to return:
  const shuffled = availableQuestions.sort(() => 0.5 - Math.random());
- Ensure the response payload returns both the new questions and explicitly echoes back the updated tracking state if needed.

2. UPDATE THE FRONTEND PHASE TO PERSIST HISTORY (frontend/src/components/assessment/phases/CapadexClarifyPhase.tsx):
- Check our existing component or the parent phase wrapper that handles the `onSaveSelection` callback.
- Introduce or locate a running state array that tracks answered questions across the active session:
  const [historicalAnsweredIds, setHistoricalAnsweredIds] = useState<string[]>([]);
- Inside our submission handler, right before firing the API request to fetch the next step, append the current question's ID to this tracking array:
  setHistoricalAnsweredIds(prev => [...prev, currentQuestion.id]);
- MANDATORY FIX: Modify the outgoing payload to the backend to explicitly include this array:
  payload: {
    ...existingPayload,
    answeredIds: [...historicalAnsweredIds, currentQuestion.id]
  }

Verify that the type matches our existing `string[]` schema exactly, compile the code, and ensure that duplicate questions are mathematically blocked from rendering twice in the same session.