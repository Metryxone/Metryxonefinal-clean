Act as a Principal Full-Stack Engineer. We are deploying a 100% final, root-level fix for our Capadex question-repetition and irrelevant-options bug during the Career Transition funnel. 

Follow these exact architectural steps to refactor `backend/routes/capadex-concern-intelligence.ts` and ensure absolute stability across the pipeline:

### STEP 1: CONTEXT-AWARE RETRIEVAL & HISTORICAL EXCLUSION MASK
Locate the database query or function that fetches questions from our 8,000-question repository. Rewrite the retrieval logic to implement this exact logic:
- EXCLUSION MASK: Pass the current `session_id` into the query. Add an explicit filter: `AND q.id NOT IN (SELECT question_id FROM capadex_responses WHERE session_id = :sessionId)` to guarantee zero duplicate questions within an active assessment session.
- TELEMETRY CROSS-OVER MAPPING: Extract the session's running `omega_x_payload`. If the user is flagged for 'career_change' or 'transition', and their `behavioural.overthinking` or `behavioural.indecisiveness` metric is high (> 0.50), configure the query engine to dynamically fetch questions with secondary category tags matching 'ambiguity_paralysis', 'choice_overload', or 'qualification_anxiety'.
- BALANCE SELECTION: Order the query by `utilization_count ASC, random()` to pull hidden, under-utilized variations out of our 8,000-question library instead of looping through the same high-weight defaults.

### STEP 2: LOCK ATOMIC QUESTION-TO-OPTIONS JOIN (NO GENERIC FALLBACKS)
- Ensure that the query pulling from the database performs a strict relational JOIN against the question options table. 
- You must fetch the question text and its specific, contextually relevant array of options as an atomic package. Never fall back to a hardcoded generic Likert scale string array (e.g., Strongly Agree/Disagree) for operational or situational transition questions.
- Shape the output rows precisely to align with our frontend expectation:
  {
    id: string;
    evaluation_type: 'LIKERT' | 'RANKING';
    question_text: string;
    options: Array<{ key: string; option_text: string; score_weight: number; }>;
  }

### STEP 3: ARREST THE PROXY REFRAMER OPTION-MUTATION BUG
- Inspect our `rephraseForProxy(spec, subjectName)` function.
- Enforce absolute mutation protection: run a deep copy (`JSON.parse(JSON.stringify(spec))`) right at entry.
- MANDATORY CONSTRAINT: Execute our pronoun string-swapping rules (substituting the first instance of 'you' with the subject's name and subsequent ones with 'they/their') EXCLUSIVELY on the main `question_text` or `question` property string.
- Completely isolate the `options` array from the string substitution regex engines. The option keys (e.g., 'opt_1', 'opt_2') and choice text labels must remain 100% pristine and untouched so that option selection handshakes do not break on `POST /api/capadex/session/:id/respond`.

Review your generated code for type-safety, test compilation against our existing shared schema factory inside `backend/services/omega-x-payload.ts`, and make these live.