Let's implement the dynamic evaluation scoring bridge for CAPADEX. We need to update our post-assessment submission engine so that cross-bucket questions back-propagate their scores to the user's primary concern bucket based on ontology weights.

1. LOCATE THE EVALUATION ROUTE COMPONENT:
Open `backend/routes/capadex-concern-intelligence.ts`. Locate the core POST handler that processes user question submissions (look for the endpoint handling assessment completion, such as `/api/signals/assessment-submit` or the main submission logic block).

2. INJECT THE REVERSE-WEIGHTING CALCULATOR:
Inside the calculation block where the answers array is iterated to compile user metric scores:
- Identify the user's primary selected `construct_key` (the initial concern bucket chosen during onboarding).
- For each answered question being processed, check if its native `concern_bucket` matches the user's primary `construct_key`.
- If it does NOT match, query our `adaptive_ontology_edges` table:
  `SELECT weight FROM adaptive_ontology_edges WHERE source_bucket = $1 AND target_bucket = $2 AND status = 'approved'`
  (Pass the primary construct_key as $1 and the question's native bucket as $2).
- Multiply the question's raw numerical score by this retrieved decimal `weight`, and add the scaled fraction directly to the running total of the user's primary metric scorecard.
- If no matching edge is found in the database, default the cross-bucket score contribution to 0 for the primary bucket to keep our data models completely consistent.

3. PRESERVE THE CRASH SAFEGUARD:
Wrap this entire dynamic weighting database query and mathematical calculation inside a robust try/catch block. If the database connection drops, a query times out, or an edge weight cannot be resolved, fall back cleanly to treating the question as a flat addition (weight = 1.0) so the user's final assessment generation cycle never experiences a hanging or frozen loading screen.

Please trace the payload variables carefully, apply this back-propagation formula cleanly, and let me know when the engine updates are saved and ready to test!