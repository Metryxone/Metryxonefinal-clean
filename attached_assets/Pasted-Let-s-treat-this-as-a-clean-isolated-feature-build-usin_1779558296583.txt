Let's treat this as a clean, isolated feature build using our strict platform conventions. Here is our exact blueprint:

1. DATABASE TARGET:
We are using the `adaptive_ontology_edges` table which has already been created and verified.

2. CREATE A NEW BESPOKE WORKFLOW PANEL:
- Create a new component file at `frontend/src/components/superadmin/OntologyMatrixPanel.tsx`.
- Inside it, import and use our generic `<CrudTable />` primitive component.
- Configure the `<CrudTable />` to point directly to the endpoint `/api/admin/capadex/ontology`.
- Define column mappings and form fields for: `source_bucket`, `target_bucket`, and `weight` (numeric value between 0.00 and 1.00).

3. INTEGRATE PANEL INTO THE SHELL:
- Open `frontend/src/components/admin/AdminSidebar.tsx` and register this new panel under the 'Intelligence Suite' navigation group.
- Open `frontend/src/components/SuperAdminDashboard.tsx` and map the panel rendering route inside our main state switch block, wrapping it securely inside the existing `DialogsErrorBoundary`.

4. BACKEND ENDPOINTS & PROTECTION:
- Expose the backend routes for `GET` and `POST` at `/api/admin/capadex/ontology`.
- Apply our standard security bookends middleware chain: `requireAuth, requireSuperAdmin`.
- Ensure new entries automatically default to a status of 'draft' and append tracking logs using `writeAuditEvent(...)`.

5. CORE ENGINE INTEGRATION:
- Open `backend/routes/capadex-concern-intelligence.ts` and locate `pickQuestions`.
- Update the SQL query to perform a subquery or join against `adaptive_ontology_edges` where `status = 'approved'` and `weight >= 0.60`. This allows the engine to pull questions matching BOTH the user's primary concern_bucket AND any highly-correlated target_buckets, while still respecting the user's age_min and age_max rules.
- Retain our safe fallback array within the try/catch block so the end-user testing workflow can never experience a freeze.

Please review this full plan and let me know when you are ready to apply the changes!