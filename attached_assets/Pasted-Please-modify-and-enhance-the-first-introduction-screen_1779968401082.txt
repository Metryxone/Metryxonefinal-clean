Please modify and enhance the first introduction screen within our CAPADEX Free Assessment Modal component (frontend/src/components/FreeAssessmentModal.tsx). We need to refactor the static parent-oriented layout into a highly responsive, multi-persona engine that maps seamlessly to our backend Orchestration Context: (actor_persona, target_persona, relationship_type).

Please implement this update by executing the following technical refactoring steps precisely:

1. PERSONA STATE & DYNAMIC HEADING MORPHING
   - Introduce an explicit local state tracking hook (e.g., activePersona) that defaults to 'parent'. Bind this state directly to the "I AM A..." selection button row.
   - Refactor the button states using our design tokens so the active button is highly distinct visually (vibrant background weight and sharp tracking borders), smoothly transitioning inactive options to secondary ghost buttons.
   - Replace the static sub-header metadata line with an engineering-informed, configuration-driven summary layout that scales across all selections: "14,000+ audited data matrices · 20+ Core Cognitive Domains · Free & Confidential".

2. CONTEXT-AWARE INPUT MATRIX CONFIGURATION
   - Instead of hardcoded labels, dynamically swap and render text values, labels, helper captions, and icons across the form fields based on the chosen activePersona. Use this precise mapping schema:
     * If 'parent': Field 1 = "Child's Name"; Field 2 = "Child's Age"; Field 3 = "Child's School / Grade"; Concern Textarea Placeholder = "e.g., can't stop scrolling, exam stress, screen distraction". Set context payload to: actor='PARENT', target='STUDENT', relationship='parent_child'.
     * If 'teacher': Field 1 = "Student's Initials / ID"; Field 2 = "Student's Age"; Field 3 = "Class / Section"; Concern Textarea Placeholder = "e.g., low classroom engagement, learning pace bottlenecks". Set context payload to: actor='TEACHER', target='STUDENT', relationship='teacher_student'.
     * If 'professional': Field 1 = "Your Name"; Field 2 = "Your Age"; Field 3 = "Target Role / Industry"; Concern Textarea Placeholder = "e.g., career direction friction, technological disruption anxiety". Set context payload to: actor='self', target='self', relationship='direct'.
     * If 'student': Field 1 = "Your Name"; Field 2 = "Your Age"; Field 3 = "Institution / College"; Concern Textarea Placeholder = "e.g., campus adjustment pressure, examination anxiety". Set context payload to: actor='self', target='self', relationship='direct'.

3. REAL-TIME ONTOLOGY RECONCILIATION PREVIEW
   - Implement a real-time, debounced (300ms) keyword listener on the "What worries you..." text input box. 
   - Render a small, clean indicator pill sub-caption row directly underneath the input field to simulate our live backend indexing matching.
   - If keywords match "scroll", "screen", "game", or "phone", render a violet indicator tag: "[ 🔍 Mapping to: Adaptive Intelligence Domain ]".
   - If keywords match "stress", "exam", "worry", or "pressure", render: "[ 🔍 Mapping to: Stress & Resilience Lifecycle Bucket ]".
   - If the text box has content but no direct match hits, gracefully fallback to display your system sentinel string: "[ 🔍 Mapping to: General Behavioral Matrix ]". Hide the sub-caption completely if the field is completely empty.

4. INLINE VALDIATION HARDENING & SUBMIT LIFECYCLE
   - Add a soft validation filter tied to the Age input field to prevent downstream question affinity mismatches. If a professional/student selects an invalid age tier, display a subtle warning text underneath the input.
   - Transition the primary action button away from its initial disabled, grey state once all required fields are valid. Elevate it smoothly into an active slate or indigo button state, updating the button text string dynamically from "Describe your concern to begin" to "Launch Clarity Journey →".
   - Ensure that when the primary action is clicked, all derived metadata parameters along with the parsed text fields are successfully grouped into the initial state package passed down to our server-side /api/capadex/concern/analyze payload.