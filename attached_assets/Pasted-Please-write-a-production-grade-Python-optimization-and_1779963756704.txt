Please write a production-grade Python optimization and repair script named "scripts/patch_orphans.py" using pandas and regex. This script will target and permanently resolve the remaining 225 unmapped orphan questions from "Clarity Questions.csv" that failed to link to our "audited_capadex_concerns.csv" master catalog.

The script must execute the following logical phases cleanly within a robust try-except wrapper:

1. High-Fidelity Data Ingestion:
   - Load "Clarity Questions.csv" using 'latin1' or 'utf-8-sig' encoding to safeguard against smart-quote decoding anomalies (\x93 characters). 
   - Dynamically verify and realign the headers if row 0 contains text labels like 'question_id' or 'concern_id'.
   - Load the compiled master tracking catalog "audited_capadex_concerns.csv" to extract the full set of valid 'Relational_Bridge_Tag' values currently registered in the database.

2. Algorithmic Fallback & Orphan Resolution Matrix:
   - Identify rows where 'master_bridge_tag' is null, blank, or does not exist in the valid master concern tags.
   - For these unmapped lines, evaluate row data using a multi-tiered fallback architecture:
     * Tier A (Text Vector Matching): Scan 'concern_id' and the 'concern' description text using regex. Map matches containing 'STRESS', 'ANXIETY', or 'PRESSURE' to 'EXAM_STRESS'; 'CAREER', 'DIRECTION', or 'FUTURE' to 'CAREER_READINESS'; 'COLLEGE', 'CAMPUS', or 'ACADEMIC' to 'COLLEGE_ADAPT'; 'TIME', 'HABIT', or 'DISCIPLINE' to 'DISCIPLINE_HABITS'; and 'ADJUST', 'COPING', or 'BURN' to 'ADJUSTMENT_COPING'.
     * Tier B (Token Heuristics): Split the 'concern_id' by underscores and check if the initial prefix token matches a substring of any valid master tag.
     * Tier C (Schema Guard Sentinel): Default any remaining unresolved anomalies to 'GENERAL_CONCERN'.

3. Duplicate Suffix Enforcement:
   - Scan the final 'question_id' values for duplicate records (e.g., Q_COMP_001_003).
   - Preserve the first instance intact, and append an incremental suffix (e.g., '_v2', '_v3') to subsequent duplicates to guarantee primary key uniqueness and preserve local storage served-ID memory state boundaries.

4. Type Casting & Normalization:
   - Ensure all option scores ('Option A Score' through 'Option E Score') are explicitly cast as Int64.
   - Clean and strip spaces from all text in options (Option A through Option E) to prevent trailing whitespace from fracturing frontend state logic in FreeAssessmentModal.tsx.

5. Production Serialization & Output Report:
   - Save the fully resolved and aligned payload back to 'audited_clarity_questions.csv'.
   - Print an execution telemetry summary detailing: total rows scanned, total orphan records successfully matched, verification of Int64 score casts, and a 3-row preview of the patched records displaying 'question_id', 'concern_id', and 'master_bridge_tag'.