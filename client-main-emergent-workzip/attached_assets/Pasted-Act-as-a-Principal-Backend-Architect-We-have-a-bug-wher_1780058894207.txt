Act as a Principal Backend Architect. We have a bug where the system is ignoring our exclusive clarity question bank of over 12,000 questions and falling back into static question loops. 

Let's modify `backend/routes/capadex-concern-intelligence.ts` to fully harness the 12,000-question repository schema-safely, ensuring we never drop back to static arrays.

### 1. DIRECT OVERRIDE: LOCK INTO THE 12,000-QUESTION BANK
- Locate the main retrieval function or query block for clarity questions. Completely remove or bypass any code paths that fall back to hardcoded arrays of static question objects.
- Ensure that the lookup directly targets our comprehensive 12,000+ clarity question dataset (whether it's loaded in memory, indexed via a JSON module, or queried relationally).

### 2. IMPLEMENT FUZZY CONCERN-TO-REPOSITORY MAPPING
- Read the incoming concern string from the request context. 
- Implement a flexible, case-insensitive mapping strategy against the 12,000 questions so that a concern token (like 'focus_at_work' or 'career_change') accurately resolves to its corresponding domain inside the big bank.
- If an exact category match is not found in the repository tags, extract the stem words of the concern to query related thematic clusters, ensuring we are always drawing real questions from the master repository.

### 3. APPLY EXCLUSION MASK VIA UNBIASED FISHER-YATES
- Intercept the `req.body.answeredIds` array sent from the frontend modal session state.
- Filter the 12,000-question bank down to an active `eligiblePool`:
  const eligiblePool = clarityQuestionRepository.filter(q => {
    const isSeen = req.body.answeredIds?.includes(q.id);
    const matchesConcern = q.category === incomingConcern || q.tags?.includes(incomingConcern) || q.id.includes(incomingConcern);
    return !isSeen && matchesConcern;
  });
- If the `eligiblePool` runs low for highly specific sub-categories, safely expand the criteria to draw from the 'general_behavioural_cluster' or global baseline items WITHIN the 12,000-question bank.
- Preserve our verified, statistically unbiased Fisher-Yates shuffle algorithm over the filtered pool before slicing the active question batch. Do NOT overwrite this with a biased Math.random() sort.

### 4. OUTPUT SHAPE INTEGRITY
- Pass the resulting batch through our third-person proxy reframer (`rephraseForProxy`).
- Ensure the final questions object maintains its string array structure (`string[]`) for options to perfectly satisfy the frontend `isSingleChoiceQuestion(q)` validation engine.

Run a clean validation sweep with `tsc` to ensure zero type drift, and confirm that every distinct assessment run pulls an entirely unique set of dynamic questions from the library.