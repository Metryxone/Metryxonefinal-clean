Please build a comprehensive, production-grade 4-tier Behavioral Intelligence Signal Ontology Subsystem within our MetryxOne platform. This system must process, ingest, map, and expose our behavioral telemetry layers across 4 verified source files ("Domain List.xlsx", "Family List.xlsx", "Signal lList.xlsx", and "Atomic Signal.xlsx"), integrating them fully with our existing "capadex_concerns_master" and "capadex_clarity_questions" infrastructure.

Implement this entire technical stack sequentially by executing these 4 core architectural phases:

1. HIGH-DIMENSIONAL MULTI-FILE INGESTION & DATA CLEANING PIPELINE (scripts/import_signal_ontology.py)
   - Source File Routing: Ingest "Domain List.xlsx" (20 rows), "Family List.xlsx" (400 rows), "Signal lList.xlsx" (20 rows), and the massive "Atomic Signal.xlsx" (15,975 leaf rows) directly using pandas. Enforce 'latin1' or 'utf-8-sig' to prevent smart-quote (\x93) character decoding crashes.
   - Text & Header Normalization: Strip all leading/trailing white-spaces from text cells and structural header keys. Automatically map and merge parent-child relationships using 'domain_id', 'family_id', and 'signal_id'.
   - Hierarchical Unique Key Hardening: Enforce strict uniqueness constraints across keys ('domain_id', 'family_id', 'signal_id', 'atomic_signal_id'). If any row collisions occur, preserve the first instance and append an incremental numerical suffix (e.g., '_v2', '_v3') to preserve structural integrity and local storage application states.
   - Age Range Regex Splicing: Safely parse age applicator properties (e.g., 'age_applicability' text ranges like '15-30' or '18-70') using regular expressions that handle all hyphen variations (-, –, —). Split them into explicit integer columns 'age_min' and 'age_max'. Apply standard defaults (17 and 24) for blank/corrupt entries, and drop the original string column.
   - Relational Bridge Key Invariant: Generate a matching 'Relational_Bridge_Tag' column across all 4 tracking datasets using token extraction on text fields. Map 'CAREER'/'EMPLOYABILITY'/'JOB' to 'CAREER_READINESS'; 'COLLEGE'/'CAMPUS'/'ACADEMIC' to 'COLLEGE_ADAPT'; 'DISCIPLINE'/'HABIT'/'TIME' to 'DISCIPLINE_HABITS'; 'ADJUSTMENT'/'COPING'/'EMOTIONAL'/'STRESS' to 'ADJUSTMENT_COPING'; and 'EXAM'/'TEST' to 'EXAM_STRESS'. Default all other unmapped rows to 'GENERAL_CONCERN' to ensure clean JOIN resolution.
   - Micro-Null Defenses: Enforce Title Case across categorical weights ('Severity', 'Priority', 'adaptive_importance', 'signal_status'). Patch all empty routing slots with the fallback string 'UNASSIGNED_ROUTING_NODE' to protect Drizzle ORM .notNull() parameters from transaction crashes. Output files to 'audited_domains.csv', 'audited_families.csv', 'audited_signals.csv', and 'audited_atomic_signals.csv'.

2. RELATIONAL DATABASE MIGRATION & SEEDING SCRIPT
   - SQL DDL Migration (backend/migrations/20260528_signal_ontology_tables.sql): Create the schema structures for 'capadex_domains', 'capadex_families', 'capadex_signals', and 'capadex_atomic_signals' with strict foreign keys reflecting the tree hierarchy. Add performance indexes for primary IDs, parent pointers, and 'relational_bridge_tag' columns.
   - Idempotent Seeder Script (backend/scripts/seed-signal-ontology.mjs): Build an asynchronous, atomic Node.js script using chunked bulk insertions within a BEGIN/COMMIT block. Apply an automatic TRUNCATE or ON CONFLICT UPDATE layout to ensure the engine remains duplicate-tolerant across repeated test runs.

3. UNIFIED EXPRESS BACKEND ROUTING API (backend/routes/capadex-ontology-hub.ts)
   - Expose an API cluster requiring both 'requireAuth' and 'requireSuperAdmin' verification middleware. Anchor paths via fileURLToPath(import.meta.url).
   - Read Endpoints:
     * GET /api/admin/capadex/ontology-hub/stats: Returns total row aggregates across all 4 files (e.g., verifying 15,975 atomic leaf records) and current bucket distributions.
     * GET /api/admin/capadex/ontology-hub/facets: Returns parallelized facet selections for UI filtering without thread locking.
     * GET /api/admin/capadex/ontology-hub/coverage: Performs a real-time FULL OUTER JOIN query identifying structural orphans or unmapped leaf components between clarity questions, signals, and master concern fields.
     * GET /api/admin/capadex/ontology-hub/:resource (domains|families|signals|atomic-signals): Features paginated lists (pageSize 50), substring search tracking, and 6-facet filtration.
   - Write/Management Endpoints:
     * POST /api/admin/capadex/ontology-hub/:resource: Whitelist-driven row creation with strict required-field checks.
     * PATCH /api/admin/capadex/ontology-hub/:resource/:id: Partial atomic entity modification using parameter sanitization.
     * DELETE /api/admin/capadex/ontology-hub/:resource/:id: Removes target keys safely, asserting cascade protection states.

4. MASTER SUPERADMIN FRONTEND COMPONENT (frontend/src/components/superadmin/SignalOntologyHubPanel.tsx)
   - Mount a clean entry point under the 'Assessment Config' section of the dashboard navigation layout mapping to activeTab='capadex-ontology-hub'.
   - Stat Strip Metric Blocks: Display total operational nodes across all 4-tier entities alongside an alert card tracking any ongoing unmapped node counts.
   - Tab-Spliced View Layout: Implement a 4-tab interface (Domains, Families, Signals, Atomic Signals). Each view must mount a shared paginated table displaying relevant row parameters, severity status badges, and an operational action toolbar (New / Export / Refresh).
   - Filter & Query Controls: Render a 6-facet real-time filtering bar complete with typeahead combobox text searching.
   - Drilldown Details Drawer & Editor Modals: Provide an overlay drawer showing full child node counts and relational metadata hierarchies. Bind a required-field validated editor modal carrying explicit role="dialog" and aria-labelledby components for full accessibility compliance.