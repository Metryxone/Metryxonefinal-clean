The PostgreSQL table `adaptive_question_bank` has been successfully created and verified. Please update our CAPADEX and SuperAdmin codebase to fully utilize it by following our system invariants:

1. UPDATE THE CORE DESERIALIZATION LAYER:
Open `backend/routes/capadex-concern-intelligence.ts`. Locate the `pickQuestions` function (around Line 1781). Refactor it to query our live database pool:
- Query the `adaptive_question_bank` table where `concern_bucket = construct_key` AND `persona = persona` AND `status = 'approved'`.
- Map the resulting rows into the standard array format expected by the frontend assessment phases (returning an array of objects with id, question text, and standard 3-point choice options).
- Wrap the database operation in a robust try/catch block that uses a safe, hardcoded fallback array so the end-user assessment flow never crashes if the DB drops.

2. REGISTER CODELESS CMS IN CONSOLE PLUMBING:
Open `frontend/src/components/admin/framework-configs.ts`. Locate the `SDI_CONFIG` object (which governs CAPADEX). Register a brand new configuration key block named `questionsApi` mapping to `/api/admin/capadex/questions`. Define appropriate column layouts and form fields for concern_bucket, persona, and question_text. This will allow our higher-order `FrameworkPanel` and `CrudTable` primitives to automatically construct the management UI.

3. ATTACH ADMINISTRATIVE ENDPOINTS & LOGGING:
Expose the corresponding administrative backend routes for `/api/admin/capadex/questions`. Protect them securely using our auth bookends middleware chain: `requireAuth, requireSuperAdmin`.
- For POST operations, ensure newly created templates default to a status of 'draft' (preserving our Draft -> Approved workflow convention).
- Integrate the append-only logging system by invoking `writeAuditEvent(...)` after successful modifications so our security metrics trail remains valid.

Please run a verification sweep once updated to guarantee the end-to-end user flow functions optimally.