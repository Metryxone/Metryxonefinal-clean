This is a re-request of work already completed across prior turns. Before declaring it a no-op, let me verify the current state of both files precisely — isSingleChoiceQuestion (spec 1) and doCompleteStage (spec 2):


3 actions
Spec 1 is fully present. Let me read the full doCompleteStage to confirm spec-2 ordering and payload shape:


2 actions
I verified both files against every spec item. This spec is already fully implemented — it re-requests work completed across prior turns. No code changes are needed, and I won't make redundant edits. Here's the precise mapping:

Spec 1 — isSingleChoiceQuestion (CapadexClarifyPhase.tsx)

Single source of truth, no isRankingApplicable() — ✓ (L55)
response_type short-circuit returns true when non-empty — ✓ (L68), placed before all token/regex heuristics. It sits just after the two other explicit schema guards (evaluation_type === 'LIKERT', isSingle === true), so all explicit backend property guards are grouped ahead of the fallbacks — functionally identical to "very top".
Token fallbacks retained + impact ladder (no impact…severe impact, L33) + emotional ladder (a little bit…very much, L35) — ✓
Stem regex how (emotionally|stressful|impactful|difficult|severe) (L76) + /^what have you (already )?tried\b/i (L83) — ✓
Footer hidden except spinner, no "Confirm" in single mode, handleSingleTap → checkmark + 350 ms auto-advance, useEffect keyed on currentQuestion.id ?? clarifyCurrentQ clearing ranked/selected/submitting — ✓ (L147+)
Spec 2 — doCompleteStage (actual path components/FreeAssessmentModal.tsx, not phases/)

timingsSnapshot captured at top — ✓ (L1224)
setQuestionTimings({}) immediately after snapshot, before any await — ✓ (L1225)
Awaited guarded POST /api/signals/ingest after setPhase('capadex_result') (L1258) and before the report pre-fetch (L1289) — ✓ (L1267-1284)
Non-blocking try/catch, !res.ok logged as warning — ✓. The signal_intelligence flag gate is enforced backend-side (the /ingest route returns {ok:true,skipped:true} when the feature_flags row is false) — the correct layer, since the frontend has no access to the flags table.
One item declined (false premise): spec 2's array payload timings: [{item_id, response_time_ms, backtrack_count}] contradicts the backend contract. /api/signals/ingest (backend/lib/signal-ingest.ts) expects timings as a Record<itemId, {response_time_ms, answer_changed, response_value}> map. The current code correctly sends the map (L1274). Adopting the array shape would break ingestion (the exact 0-row symptom the spec wants to fix), so I'm keeping the working map.

Nothing to change — the platform is already in the target state. If you'd like, I can take you up on trimming the now-large replit.md/docs/CAPADEX.md at this natural pause.