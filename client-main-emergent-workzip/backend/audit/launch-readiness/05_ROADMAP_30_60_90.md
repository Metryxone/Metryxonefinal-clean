# Roadmaps (Deliverables 13–15)

Sequenced to the audit's causal structure: **(1) stop showing un-real intelligence, (2) fix the Outcome
keystone so the chain produces real state, (3) make activation/commercial durable, (4) acquire the user
base that unlocks longitudinal/forecast/enterprise.** Every item is scoped to the gaps proven in
`03_BLOCKERS_AND_GAPS.md`. None of this is implemented here — this is the plan to *reach* readiness.

> Honesty note: items are ordered by leverage, not by ease. The biggest single unlock is CB-1 (Outcome),
> which is mostly config/data, not re-architecture.

---

## 13 · 30-Day Roadmap — "Make the beta honest & launchable"

**Goal:** ship the Conditional-Go Free Assessment Beta without displaying any empty/degraded intelligence.

1. **Truth-in-UI gate (HG-2, CB-1).** Hide every Outcome/Journey/forecast/benchmark/"intelligence"
   surface from end users until it produces real, confident state. Ship assessment → clarity → concern →
   **Stage** + report only.
2. **Fix the Outcome keystone (CB-1).** Either (a) populate the behavioural spine so
   `loadSessionConstructs` returns constructs, or (b) enable `FF_WC3_OUTCOME_CROSSWALK` and validate the
   clarity-bank crosswalk resolves constructs. Backfill `wc3_outcome_state` for the 9 completed sessions;
   re-derive Journey and confirm non-degraded rows appear.
3. **Active-vs-aspirational table inventory (HG-6).** Publish the canonical list of the ~50 tables that
   are actually live; mark the other ~530 as aspirational/seed so nobody mistakes schema for capability.
4. **Fix metrics hygiene (HG-8).** Schedule `ANALYZE`/autovacuum so any stat-based dashboard stops
   mis-reporting (estimates were off by 27 sessions).
5. **Instrument acquisition.** Add completion-funnel + per-user session-count tracking so progress toward
   the data-sufficiency thresholds (CB-4) is measurable from day one.

**Exit criteria:** beta live; zero un-real intelligence shown; Outcome state > 0 for completed sessions.

---

## 14 · 60-Day Roadmap — "Make the intelligence chain real & durable"

**Goal:** the assessment→outcome→journey→decision→product chain produces *persisted, confident* state.

1. **Persist the activation chain (CB-2).** Flip Growth Plan to `persist=true`; add persistence for
   Product/Mentor/Subscription activation so durable state accumulates (it also feeds longitudinal/forecast).
2. **Stand up the commercial substrate (CB-3).** Seed real `subscription_packages`, create the missing
   `wc7c_*` persistence, and record offer/conversion/entitlement state — *before* enabling any paid tier.
   Keep `revenueIntelligence` honest (no projections off 6 payments).
3. **Wire or retire question-intelligence (HG-1).** Decide: connect L5A–L5D to runtime (turn on
   `wc3QuestionIntel`/`wc3ContextIntel` with a live consumer) or formally shelve the 30,638-row build so
   it stops reading as a live capability.
4. **Turn on Longitudinal/Trend in-workflow (HG-5)** once Outcome is real and a handful of users have ≥2
   sessions; report trends with confidence bands, never as certainty.
5. **Activate User Intelligence Foundation properly (HG-3)** — run it under its flag rather than relying
   on backfilled rows.

**Exit criteria:** Outcome/Journey non-degraded for real sessions; activation + commercial state
persisted; no dark/orphaned subsystems advertised as live.

---

## 15 · 90-Day Roadmap — "Earn longitudinal, forecast & enterprise readiness"

**Goal:** convert mechanism into measured capability as the user base grows past the statistical floors.

1. **Drive the user base past thresholds (CB-4).** Targets: ≥30 users with ≥2 sessions (trend/cohort
   floor), ≥2 trajectory snapshots/user (forecast floor). Until then, keep these surfaces in
   "preview/insufficient-data" states — do not fabricate.
2. **Make Forecast real (HG-4).** Replace heuristic forecasts with trajectory-driven ones once
   `m4_capability_trajectories` has history; ship Future products (Navigator/Skills Planner) only after a
   live backend route exists — not from mockups.
3. **De-stub Enterprise (CB-5).** Replace `iil-core` `rnd()` values with real session→institution
   aggregation; gate any B2B dashboard behind k≥30 cohort suppression; never expose random values.
4. **Cohort personalization (HG-7).** Enable report/rec personalization once cohorts are large enough to
   calibrate; keep k-anonymity (k≥30) enforced.
5. **Re-audit & re-score.** Re-run this exact census + wiring audit; promote scores only where data and
   persistence now justify them. The scorecard should move *because reality moved*, not because targets
   were assumed.

**Exit criteria:** Longitudinal/Forecast/Enterprise scores rise on the back of real population + persisted
history; a second Go/No-Go can be made on evidence, not aspiration.
