# MetryxOne — Behavioral Intelligence SaaS Platform

## Stack
- **Frontend**: React + Vite (port 5000) — `frontend/`
- **Backend**: Node.js + Express + tsx (port 8080) — `backend/`
- **Database**: PostgreSQL via Drizzle ORM (`DATABASE_URL` env var)
- **Proxy**: `/api/*` → `http://localhost:8080` (`frontend/vite.config.ts`)
- **Email**: `ZOHO_EMAIL` + `ZOHO_APP_PASSWORD` env vars (`backend/email.ts`)

## Workflows
- `Backend API`: `cd backend && npm run dev:server`
- `Start application`: `cd frontend && npm run dev`

## Deployment (canonical production path — single source of truth)
**Production = Google Cloud + Firebase**, deployed via `scripts/deploy-gcp.sh` (one-shot). This is the ONLY canonical production topology:
- **Node API** (`backend/`) → Cloud Run service `metryxone-api`.
- **FastAPI upload** (`backend-main/`) → Cloud Run service `metryxone-bulk-upload`. The deploy sets the Node API's `FASTAPI_URL` to this service URL — **this is why file uploads work in prod** (uploads proxy through the Node API to FastAPI).
- **Frontend** (`frontend/`) → Firebase Hosting (`frontend/firebase.json` + `.firebaserc`), with `/api/**` rewritten to `metryxone-api`. Public domain `https://metryx.one`.
- Region `asia-south1`. Required envs (stored in Secret Manager): `GCP_PROJECT`, `DATABASE_URL`, `MONGODB_URI`, `EMERGENT_LLM_KEY`; `SECRET_KEY` auto-generated. Verify `SESSION_SECRET` (backend fails fast in prod without it) and `ZOHO_EMAIL`/`ZOHO_APP_PASSWORD` (super-admin MFA email delivery) are set.

**`.replit` `[deployment]` (autoscale) is DEV / WORKSPACE PREVIEW ONLY — NOT production.** It starts only the Node backend (no FastAPI), so file uploads do NOT work under it unless you separately publish the FastAPI service and set `FASTAPI_URL` + a real `UPLOAD_SERVICE_TOKEN` (the default token is a dev placeholder). Do not treat Replit autoscale as the production deploy.

**Env vars**: `docs/ENVIRONMENT.md` is the authoritative reference (required vs recommended vs optional, per service, where to set). The Node backend runs a boot-time preflight (`backend/lib/env-preflight.ts`) that aborts in prod on missing REQUIRED vars (`SESSION_SECRET`, `DATABASE_URL`) and warns loudly on feature-degrading ones (`ZOHO_*`, `FASTAPI_URL`, `UPLOAD_SERVICE_TOKEN`, `OPENAI_API_KEY`); no-op in dev.

## Key Files
- `backend/index.ts` — Express entry, port 8080
- `backend/routes.ts` — Main route file (~13.2k lines)
- `backend/shared/schema.ts` — Drizzle schema (symlinked: `/home/runner/workspace/shared` → `backend/shared`)
- `frontend/src/App.tsx` — React Router + Screen enum
- `frontend/src/components/SuperAdminDashboard.tsx` — Super admin UI shell (panels in `components/admin/*` + `components/superadmin/*`)
- `frontend/src/pages/CareerBuilderPage.tsx` — Career Builder monolith (~7.8k lines)
- `frontend/src/components/FreeAssessmentModal.tsx` — CAPADEX assessment flow (~4.1k lines)

## Super Admin
- Login: `support@metryxone.com` / `admin123` (role `super_admin`; seeded by `storage.ts` `seedSuperAdmin`, `SUPER_ADMIN_EMAIL`) · `SuperAdminLogin.tsx` → SPA nav to `screen = 'admin-dashboard'`.
- Super-admin login is **always 2FA-gated** (MX-301I §G4 — dev bypass removed): `POST /api/login` returns `{mfaRequired:true}` + writes a `mfa_codes` row; complete via `POST /api/admin/mfa/verify`. A password alone is **never** sufficient (no `mfaBypassed` path).
- MFA delivery: code is **emailed via Zoho** (`ZOHO_EMAIL`/`ZOHO_APP_PASSWORD`) when configured. In dev with no email channel, the code is still issued + persisted and, in non-production only, **logged to the `Backend API` workflow console** (`[DEV MFA] …`) — never returned in the HTTP response. To complete dev login, read the 6-digit code from the workflow logs (or `SELECT code FROM mfa_codes WHERE used=false ORDER BY created_at DESC LIMIT 1`) and enter it in the login screen.

## Feature flags (two distinct systems)
- **File registry** `backend/config/feature-flags.ts` — every additive V2 phase ships behind a flag; flag-off → protected routes 503 + UI panel hides; flag-off path is byte-identical to legacy. All default OFF.
- **DB table** `feature_flags` (distinct) — gates `POST /api/signals/ingest` (`signal_intelligence`) + engine flags read by `services/feature-flags.ts`. If signal tables look empty, check this table first.

## User preferences
- **Audits & additive phases STOP for approval** before merge/deploy; never auto-deploy.
- **Honesty over optimism** — never assume, fabricate, or inflate. Orphans/gaps are honest findings. Report Coverage (data exists) and Confidence (trustworthy/sufficient) as separate axes.
- **Additive & flag-gated** — new phases re-shape already-computed data behind a flag; flag-off is byte-identical to legacy.

## Documentation (single sources of truth — keep DETAIL there, not in this file)
- `docs/phase-history.md` — build logs + **Phase Index Tables** (canonical for namespaces, migrations, deep-links, API bases, engine internals, Adaptive Career Intelligence phase index).
- `docs/CAPADEX.md` — consolidated CAPADEX doc (overview · runtime Modules 15–26 · API reference · relevance-audit).
- `docs/SUPERADMIN.md`, `docs/CAREER_BUILDER.md`, `docs/COMPETENCY_ASSESSMENT.md`, `docs/COMPETENCY_AND_ADAPTIVE_INTELLIGENCE.md`, `docs/EMPLOYABILITY_INDEX.md`, `docs/EMPLOYABILITY_PASSPORT.md`, `docs/MICRO_ACCURATE_STAGE_GUIDANCE.md`, `docs/peer-benchmarking.md`, `docs/integration-map.md`.
- `.agents/memory/*` — durable engineering lessons per subsystem (read before touching that subsystem).
- Audit deliverables regenerate to `backend/audit/<phase>/` (e.g. `audit/aq-1/`, `audit/launch-readiness/`).

---

## Global conventions (apply platform-wide)
- **Additive / read-only / never-throws**: V2 phases re-shape already-computed data; flag-off or absent data → byte-identical prior behaviour. Never fabricate.
- **Canonical migration + lazy ensure-schema**: most newer tables have a migration file AND a lazy `ensure*Schema()` that mirrors it (no migration runner). Filenames in `docs/phase-history.md`.
- **New backend route → restart `Backend API`** before smoke-testing (`Cannot GET` otherwise).
- **Express route order**: register literal sub-paths (`/export.csv`, etc.) BEFORE the catch-all `/:id` or the param handler swallows them. → `.agents/memory/express-literal-vs-param-route-order.md`.
- **Strengths canon**: strengths come ONLY from CSI `positive_factors` / positive longitudinal growth — NEVER from raw concern-signal magnitude (signals are concern-DIAGNOSTIC).
- **Admin APIs**: `requireAuth` + `requireSuperAdmin`, 60s cache, `?refresh=1` to bust.
- **PIL knowledge graph namespace**: `pil_kg_*` — NEVER bare `kg_*` (bare `kg_*` is the live Employability graph; PIL materialize against it would WIPE it). → `.agents/memory/kg-table-name-collision.md`.
- **Lifecycle-stage read parity**: every stored-stage reader must route through `normalizeStoredStage` (`backend/lib/lifecycle.ts`); drift is caught automatically by the **`lifecycle-stage-parity`** validation step (`backend/scripts/verify-lifecycle-stage-normalization.ts`). Add a matching assertion when you add a new stage read. The companion **`lifecycle-stage-writers`** validation step (`backend/scripts/verify-canonical-stage-writers.ts`) proves no writer can persist a non-canonical `canonical_stage` value (via `toCanonicalStoredStage`/`isCanonicalStoredStage`) — add a matching assertion when you add a new writer of a stage column. → `.agents/memory/lifecycle-stage-read-normalization.md`.

---

## Feature Map (stable — do not redesign; condense, don't expand)
> Each entry is a navigation pointer (UI panel · route · table). Engine internals, algorithms, and gotchas live in `docs/CAPADEX.md` + `.agents/memory/*` — go there for detail.

### Assessment configuration
- **Persona Model** (CAPADEX 3.0 Phase 1.2, flag `personaModelAlignment`/`FF_PERSONA_MODEL_ALIGNMENT`, OFF byte-identical incl. schema) multi-axis persona model (L1 market ⟂ L6 auth, L2 6-key runtime `PersonaKey` is the assessment SSoT). Alignments: `SUB_PERSONA_QUESTION_BANKS`+`resolveQuestionBank` in `frontend/src/lib/behavioural-insights.ts` (borrowed banks carry provenance), IntroPhase competitive-aspirant→JEE/NEET/CUET/UPSC split (all `legacyKey:'student'`), `cohort-gating.ts` flag-gated `ALIGNMENT_SUB_PERSONA_TO_TRACK`, `pil/runtime-guidance-engine.ts` campus/jobseeker lenses + counsellor `stakeholder_provenance` (null OFF), `CapadexRegisterPhase.tsx` canonical label map. Flag exposed via `/api/capadex/public-config` `persona_model_alignment`. G-M3 data seed `scripts/seed-adaptive-question-bank-personas.ts` writes campus/jobseeker/teacher as DRAFT only (runtime serves `approved` → byte-identical; promotion human + adaptive-flag-gated, NOT this flag). Future/DO-NOT-CLAIM verticals (govt/healthcare/clinical) excluded. → `docs/PERSONA_MODEL.md`.
  - **Persona Model EXPANSION** (CAPADEX 3.0 deliverable-10 gaps G-F1..G-F6, SECOND independent flag `personaModelExpansion`/`FF_PERSONA_MODEL_EXPANSION`, default OFF byte-identical incl. schema — zero DDL; composes additively with alignment, neither depends on the other; public-config `persona_model_expansion`). G-F1 enterprise sub-personas (`people_manager`/`senior_leadership`/`learning_development`, legacyKey `professional`) + G-F2 `higher_ed_faculty` (legacyKey `teacher`) added to `SUB_PERSONA_QUESTION_BANKS`+IntroPhase (flag-gated spreads), `cohort-gating.ts` `EXPANSION_SUB_PERSONA_TO_TRACK`, PIL `mapStakeholder`/`classifyReaderLens` new tokens only. **G-F3/G-F4 ALREADY EXIST via `journeyTailCompletion` — verified, NOT rebuilt.** G-F5 `routes/persona-expansion.ts`+`services/persona-expansion-engine.ts` read-only composer: `/outcomes` per-persona assessment-coverage (REAL, `capadex_user_profiles.persona`) ⟂ realized-outcome breakdown composing MX-102X `composeOverview` — outcomes honest-NULL (`linkage_present:false`, substrate has no persona dim, abstain <k_min=30), Coverage⟂Outcomes⟂Confidence never composited; G-F6 `/verticals` NON-CLINICAL scaffold registry (govt/healthcare/clinical_psychology, `validated:false`/`clinical_use:false`, "not validated / not for clinical or diagnostic use" disclaimers ONLY — boundary marker not a product). `/enabled` probe; `/outcomes`+`/verticals` requireAuth+requireSuperAdmin, never-throws, flag-gate 503 before work/auth/DDL. Deliverable `backend/audit/capadex-3.0-persona-implementation/impl/persona-model-expansion-implementation.md`. → `docs/PERSONA_MODEL.md`.
- **Assessment Framework Completion** (CAPADEX 3.0 Phase 1.3, flag `assessmentFrameworkCompletion`/`FF_ASSESSMENT_FRAMEWORK_COMPLETION`, default OFF byte-identical incl. schema — zero DDL) the ONE canonical Assessment Framework: pure-data registry `config/assessment-framework.ts` (FROZEN 10-type taxonomy Entry/Baseline/Diagnostic/Behaviour/Competency + Progress/Exit/Continuous=IMPLEMENTED, Learning/Performance=PARTIAL + 19-name spec crosswalk + 8-axis defs + KNOWN_OVERLAPS) ⟂ read-only composer/verifier `services/assessment-framework-engine.ts` (`composeCoverage`/`composeSummary`+`composeLifecycleClosure`(adoption)+`composePersonaOutcomeLinkage`(read-time join, k_min=30)+`ASSESSMENT_GAPS`) ⟂ routes `routes/assessment-framework.ts` (`/api/assessment-framework/enabled` ungated probe + super-admin `/framework`,`/coverage`,`/gaps`,`/summary`,`/lifecycle-closure`,`/outcomes/persona`, flag-gate 503→auth→superadmin, never-throws). **Close-the-loop completed via REUSE (no new engine/table/DDL):** Progress/Exit/Continuous instrumented through the pre-existing `services/capadex/progression-outcome-capture.ts` (`captureProgressionOutcome` stage_completion/reached_mastery milestones in `validation_loop_outcomes` keyed by `ref_id` prefix; `getReassessmentSignal` read-derived freshness `REASSESSMENT_FRESHNESS_DAYS=180`, NO cron) — gated by the SEPARATE `longitudinalOutcomeCapture` flag; frozen taxonomy STRUCTURE unchanged, only per-type STATUS moved. SSoT scan `scripts/capadex-1.3-assessment-framework-scan.ts`→`audit/capadex-3.0-assessment-framework/scan.json` (now embeds `lifecycle_closure`+`persona_linkage`); deliverables generator reads scan.json so docs never drift (12 reports + completion-certification). public-config `assessment_framework_completion`. Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (loop CODE-complete → 8 IMPLEMENTED·2 PARTIAL·0 MISSING; what remains is ADOPTION not engineering — capture flag-gated, real re-administration volume currently honest 0; 1 Medium content-breadth residual (Learning+learner Performance), 0 Launch-Critical gaps). Coverage⟂Confidence⟂Outcome⟂Adoption never composited; null≠0. LBI⟂Competency stay separate; overlaps are recommend-only consolidation candidates. → `docs/ASSESSMENT_FRAMEWORK.md` · `.agents/memory/assessment-framework-completion.md`.
- **Customer Journey Completion** (CAPADEX 3.0 Phase 1.4, flag `customerJourneyCompletion`/`FF_CUSTOMER_JOURNEY_COMPLETION`, default OFF byte-identical incl. schema — zero DDL) the ONE canonical Customer Journey Model (mirrors 1.3 EXACTLY): pure-data registry `config/customer-journey.ts` (FROZEN 8-step canonical spine `registration→entry_assessment→ai_diagnose→recommend→learn_act_grow→reports→outcome_capture→re_measure` + 5 reusable templates T1–T5 + 12 per-persona journeys + 8-axis defs persona/lifecycle/assessment/AI/reports/dashboards/outcomes/KPIs + `DUPLICATE_ENTRANCES` KEEP_ALL decisions) ⟂ read-only composer/verifier `services/customer-journey-engine.ts` (`composeCoverage`(+spine reachability)/`composeSummary`+`composeOutcomeTailAdoption`(adoption)+`composePersonaOutcomeLinkage`(read-time join, k_min=30)+`JOURNEY_GAPS` GAP-J1..J6) ⟂ routes `routes/customer-journey.ts` (`/api/customer-journey/enabled` flag probe + super-admin `/model`,`/coverage`,`/gaps`,`/summary`,`/outcome-tail`,`/outcomes/persona`, flag-gate 503→auth→superadmin, never-throws). **Frozen-spine honesty:** plan said "23-step" — the MEASURED canonical spine is 8 steps; never pad a taxonomy to a round number. **Close-the-loop via REUSE (no new engine/table/DDL):** universal outcome tail (`outcome_tail` journey=PARTIAL) instrumented through the pre-existing `services/capadex/progression-outcome-capture.ts` gated by the SEPARATE `longitudinalOutcomeCapture` flag (reuses `REASSESSMENT_FRESHNESS_DAYS`). Registry evidence cites `to_regclass`-verified table names (`wc3_outcome_models` NOT `outcome_models`). SSoT scan `scripts/capadex-1.4-customer-journey-scan.ts`→`audit/capadex-3.0-customer-journey/scan.json` (embeds spine/templates/journeys/coverage/gaps/summary/outcome_tail/persona_linkage); deliverables generator `scripts/capadex-1.4-generate-deliverables.ts` reads ONLY scan.json so docs never drift (12 reports + completion-certification). public-config `customer_journey_completion`. **Phase 1.4 BUILD — all six journey gaps ENGINEERING-CLOSED via REUSE-before-build, each gated by `customerJourneyCompletion` (byte-identical OFF):** J1 teacher/counsellor DEAD_END→PARTIAL (frontend `ObservationFollowUpQueue` → GET `/api/journey-tail/counsellor/follow-up-queue` + PATCH `/observations/:id/follow-up`; resolution fires `captureJourneyTailMilestone` into the outcome tail), J2 faculty first-class batch-scoped heatmap/gaps (institutional-intelligence grants faculty role batch-confined access; FE tab auto-show/hide) + parent/mentor tail wiring, J3 universal outcome tail WIRED per-journey via `captureJourneyTailMilestone` at observation_resolved/mentor_milestone/parent_action_done (gated `longitudinalOutcomeCapture`, zero new engine/table/DDL), J4 next-step CTAs (ResultsSummary/GapAnalysisPage/RoleTransitionPage→career-builder), J5 ParentConsentApprovePage→unified-parent-dashboard redirect, J6 gamification card connected into `StudentDashboard` journey nav. Engine now has `JOURNEY_GAPS=[]` (OPEN engineering gaps = 0) + `RESOLVED_JOURNEY_GAPS` (6 entries) + `summary.resolved_gap_count`; scan emits `resolved_gaps`; generator reads it. Verdict stays `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (5 SUPPORTED·7 PARTIAL·0 DEAD_END·0 MISSING; outcome-tail CODE-complete → remaining axis is ADOPTION not engineering, real volume honest 0 — a usage axis reported SEPARATELY, NEVER a gap). Coverage⟂Confidence⟂Outcome⟂Adoption never composited; null≠0; duplicate entrances KEEP_ALL not duplicates. → `docs/CUSTOMER_JOURNEY.md` · `.agents/memory/customer-journey-completion.md`.
- **Progression Engine / Continuous Growth** (CAPADEX 3.0 Phase 1.5, flag `progressionEngineCompletion`/`FF_PROGRESSION_ENGINE_COMPLETION`, default OFF byte-identical incl. schema — ZERO DDL; this phase has NO migration, it COMPOSES existing substrate so OFF creates 0 tables) answers "Is CAPADEX capable of measurable, continuous customer growth?" — the ONE canonical Progression Engine, enhancement-only / reuse-before-build / no V2, mirrors 1.3+1.4 EXACTLY: pure-data registry `config/progression-model.ts` (FROZEN 15-step canonical growth spine `assessment→evidence_collection→ai_interpretation→recommendation→learning_plan→practice_activity→behaviour_reinforcement→competency_development→personalized_intervention→progress_measurement→reassessment→improvement_validation→outcome_achievement→promotion→continuous_development` re-enters loop — MEASURED not padded; each step `reuses` a verified EXISTING engine+table; 4 loop-closure invariants, 4 lifecycle promotion rules, 9 per-persona PROGRESSION_MODEL paths, 8 axes, PROGRESSION_DECISIONS, PROGRESSION_GAPS + RESOLVED_PROGRESSION_GAPS) ⟂ read-only composer `services/progression-engine.ts` (`composeCoverage`(verify evidence vs live FS+DB)/`composeSummary`/`composeLoopClosure`/`composeProgressionAdoption`(reuse validation_loop_outcomes/wc3_longitudinal_snapshots/longitudinal_patterns)/`composePersonaProgressionLinkage`(read-time join k_min=30); GET-only, never-throws, `readScalar` null-on-error/0-on-no-rows) ⟂ routes `routes/progression.ts` (`/api/progression/enabled` flag probe + super-admin `/model`,`/coverage`,`/matrices`,`/loop-closure`,`/gaps`,`/summary`,`/adoption`,`/personas`, flag-gate 503→requireAuth→requireSuperAdmin, never-throws). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `progression_engine_completion` must IMPORT `isProgressionEngineCompletionEnabled` or the endpoint 500s (no tsc here). SSoT scan `scripts/capadex-1.5-progression-scan.ts`→`audit/capadex-3.0-progression/scan.json`; generator `scripts/capadex-1.5-generate-deliverables.ts` reads ONLY scan.json so docs never drift (12 reports + completion-certification). Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (4 SUPPORTED·5 PARTIAL·0 DEAD_END·0 MISSING; loop 4/4 closed = Coverage; gaps 0 Launch-Critical·1 Medium GAP-P1 uniform per-persona promotion gate·1 Low·1 Future). **Engineering closure ⟂ Adoption:** loop CODE-complete but real re-administration volume honest 0 — a usage axis reported SEPARATELY, NEVER a gap. Coverage⟂Confidence⟂Outcome⟂Adoption never composited; null≠0; never fabricate. → `docs/PROGRESSION_ENGINE.md` · `.agents/memory/progression-engine-completion.md`.
- **Outcome Framework / KPI Engine** (CAPADEX 3.0 Phase 1.6, flag `outcomeFrameworkKpiEngine`/`FF_OUTCOME_FRAMEWORK_KPI_ENGINE`, default OFF byte-identical incl. schema — ZERO DDL, NO migration; READS only so OFF creates 0 tables) answers "assessment → intervention → MEASURABLE OUTCOME → KPI." — the ONE canonical Outcome & KPI Model, enhancement-only / reuse-before-build / NO new outcome or KPI engine / no V2, mirrors 1.3/1.4/1.5 EXACTLY: pure-data registry `config/outcome-kpi-model.ts` (FROZEN 12-step outcome spine `assessment→evidence_collection→ai_interpretation→recommendation→intervention→learning→practice→reassessment→improvement→measured_outcome→kpi_update→continuous_optimization` re-enters loop, each step `reuses` a verified EXISTING engine+table; 11 outcome-tracking types, 10 KPI families Individual/Persona/Lifecycle/Assessment/Journey/AI/Learning/Business/Organizational/Platform — KPIs COMPUTED by EXISTING enterprise-analytics+benchmark+mei/employability engines, NO new KPI engine; 4 CAP_* lifecycle outcome rules, 9 per-persona OUTCOME_MODEL paths, 8 axes, OUTCOME_KPI_DECISIONS, OUTCOME_KPI_GAPS + RESOLVED_OUTCOME_KPI_GAPS) ⟂ read-only composer `services/outcome-kpi-engine.ts` (`composeCoverage`(verify evidence vs live FS+DB)/`composeOutcomeTypeCoverage`/`composeKpiCoverage`/`composeEffectiveness`(rec+intervention SUBSTRATE measured + loop-level `calibration` block **WIRED via REUSE** of the validation-loop calibration mechanism `calibrationFromRows`/`toCalibrationPairs` fed by `recordValidationOutcome`'s `predicted_prob_at_decision` — effectiveness_rate stays **null** while cold_start/provisional and lights up ONLY at `calibrated`≥k_min=30 real non-demo pairs, never fabricated; per-channel rates stay null as predictions are loop-level; zero-DDL via never-throws `readRows`, CONFIDENCE⟂Coverage)/`composeOutcomeAdoption`(reuse validation_loop_outcomes/wc3_longitudinal_snapshots/anl_kpi_daily)/`composePersonaOutcomeLinkage`(read-time join k_min=30)/`composeSummary`; GET-only, never-throws, `readScalar` null-on-error/0-on-no-rows) ⟂ routes `routes/outcome-kpi.ts` (`/api/outcome-kpi/enabled` flag probe + super-admin `/model`,`/coverage`,`/outcomes`,`/kpis`,`/matrices`,`/effectiveness`,`/personas`,`/gaps`,`/summary`,`/outcomes/persona`, flag-gate 503→requireAuth→requireSuperAdmin, never-throws). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `outcome_framework_kpi_engine` must IMPORT `isOutcomeFrameworkKpiEngineEnabled` or the endpoint 500s (no tsc here). **14 deliverables NOT 12** (effectiveness split rec/intervention + outcome/kpi inventories + lifecycle-outcome/dashboard/frontend). **Summary has NO loop_closure field** (unlike 1.5) — generator reads outcome_type_rollup/kpi_family_rollup/effectiveness/adoption instead. SSoT scan `scripts/capadex-1.6-outcome-kpi-scan.ts`→`audit/capadex-3.0-outcome-kpi/scan.json`; generator `scripts/capadex-1.6-generate-deliverables.ts` reads ONLY scan.json so docs never drift (14 reports + completion-certification). Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (4 SUPPORTED·5 PARTIAL·0 DEAD_END·0 MISSING; outcome-types 7 SUPPORTED·4 PARTIAL; KPI-families 5 SUPPORTED·5 PARTIAL; **OPEN engineering gaps = 0** — `OUTCOME_KPI_GAPS=[]`, `RESOLVED_OUTCOME_KPI_GAPS`=6: former GAP-O1 mechanism-CLOSED via wired calibration reuse (MECH-EFFECTIVENESS-CALIBRATION-WIRED), former GAP-O2/O3 RECLASSIFIED as AXIS-PERSONA-KPI-ARCHITECTURE (zero-DDL read-time join, closing it needs DDL=contract violation) + AXIS-PLATFORM-KPI-ADOPTION (usage-driven), reported on their own axes never as gaps). **Engineering closure ⟂ Adoption ⟂ Confidence:** chain CODE-complete via REUSE but real outcome/KPI volume honest-low (rec/int substrate 0, anl_kpi_daily seeded) + effectiveness calibration cold_start (abstained until k_min) — usage/data axes reported SEPARATELY, NEVER a gap. Revenue stays in `capadex_payments` (never composited into outcome KPIs). Coverage⟂Confidence⟂Outcome⟂Adoption never composited; null≠0; never fabricate. → `docs/OUTCOME_KPI_FRAMEWORK.md` · `.agents/memory/outcome-kpi-framework.md`.
- **AI Recommendation Report Orchestration** (CAPADEX 3.0 Phase 1.7, flag `aiRecommendationReportOrchestration`/`FF_AI_RECOMMENDATION_REPORT_ORCHESTRATION`, default OFF byte-identical incl. schema — ZERO DDL, NO migration; READS only so OFF creates 0 tables) answers "assessment → AI analysis → confidence → explainability → recommendation → intervention → outcome-validation → report → KPI." — the ONE canonical AI Orchestration Model, enhancement-only / reuse-before-build / NO new AI/recommendation/report engine / no V2, mirrors 1.3/1.4/1.5/1.6 EXACTLY: pure-data registry `config/ai-orchestration-model.ts` (FROZEN 12-step AI orchestration spine `assessment→evidence_collection→ai_analysis→confidence→explainability→recommendation→intervention→learning_plan→progress→outcome_validation→report→kpi_update`, each step `reuses` a verified EXISTING engine+table; 12 AI capabilities analysis/reasoning/recommendation/intervention/explainability/report/analytics/orchestration; 6 recommendation-completeness criteria, 6 explainability criteria, 8 report sections, 8 dashboard surfaces by audience; 9 per-persona AI paths, 8 axes persona/lifecycle/assessment/ai_analysis/explainability/recommendation/report/kpi, AI_ORCHESTRATION_DECISIONS, AI_ORCHESTRATION_GAPS + RESOLVED_AI_ORCHESTRATION_GAPS) ⟂ read-only composer `services/ai-orchestration-engine.ts` (`composeCoverage`(verify evidence vs live FS+DB)/`composeCapabilityInventory`/`composeRecommendationCompleteness`/`composeExplainability`/`composeReportValidation`/`composeDashboardValidation`/`composeEffectiveness`(rec+intervention SUBSTRATE + loop-level `calibration` **WIRED via REUSE** of validation-loop `calibrationFromRows`/`toCalibrationPairs` fed by `recordValidationOutcome`'s `predicted_prob_at_decision` — rate stays **null** while cold_start/provisional, lights up ONLY at `calibrated`≥k_min=30 real non-demo pairs, per-channel rates null as predictions are loop-level)/`composeAdoption`(reuse ai_reasoning_chains/development_recommendations/capadex_interventions/capadex_reports/validation_loop_outcomes/wc3_longitudinal_snapshots/anl_kpi_daily)/`composePersonaAiLinkage`(read-time join validation_loop_outcomes→capadex_user_profiles k_min=30)/`composeSummary`; GET-only, never-throws, `readScalar` null-on-error/0-on-no-rows) ⟂ routes `routes/ai-orchestration.ts` (`/api/ai-orchestration/enabled` flag probe + super-admin `/model`,`/coverage`,`/capabilities`,`/recommendations`,`/explainability`,`/reports`,`/dashboards`,`/matrices`,`/effectiveness`,`/adoption`,`/gaps`,`/summary`,`/personas/linkage` — note 1.7 uses `/personas/linkage` not 1.6's `/outcomes/persona`; flag-gate 503→requireAuth→requireSuperAdmin, never-throws). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `ai_recommendation_report_orchestration` must IMPORT `isAiRecommendationReportOrchestrationEnabled` or the endpoint 500s (no tsc here). **Spine row shape differs from 1.6:** rows use `step`/`label`/`description` + `reuses:{services,tables}` (object, NOT 1.6's `key`/`definition`+string) → generator sanity-check + render must read those fields (a renamed registry field silently corrupts the inventory). **13 deliverables + completion-certification** (NOT 1.6's 14). SSoT scan `scripts/capadex-1.7-ai-orchestration-scan.ts`→`audit/capadex-3.0-ai-orchestration/scan.json`; generator `scripts/capadex-1.7-generate-deliverables.ts` reads ONLY scan.json so docs never drift. Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (9 paths: 5 SUPPORTED·4 PARTIAL·0 DEAD_END·0 MISSING; capabilities 10 SUPPORTED·2 PARTIAL of 12; evidence verified present services 22/22·routes 11/11·frontend 12/12·tables 21/21; spine reachability 63/108; **OPEN engineering gaps**: GAP-AI-1 Medium explainability-depth + GAP-AI-2 Low report-engagement, 6 RESOLVED via reused mechanism). **Engineering closure ⟂ Adoption ⟂ Confidence:** chain mechanism-complete via REUSE but real reasoning-chain/recommendation/intervention/report volume honest 0 (anl_kpi_daily seeded 10) + effectiveness calibration cold_start (abstained until k_min) — usage/confidence axes reported SEPARATELY, NEVER a gap. Coverage⟂Confidence⟂Outcome⟂Adoption never composited; null≠0; engines read by existence/persisted-output NEVER invoked; never fabricate. → `docs/AI_ORCHESTRATION.md` · `.agents/memory/ai-orchestration-framework.md`.
- **Assessment Architecture Completion** (CAPADEX 3.0 Program 3 · Phase 3.1, flag `assessmentArchitectureCompletion`/`FF_ASSESSMENT_ARCHITECTURE_COMPLETION`, default OFF byte-identical incl. schema — **DDL ONLY on the flag-gated write paths (mechanism POSTs), never at read time**) a CERTIFICATION deliverable (mirrors 1.3–1.7) that ALSO **engineering-closes all 9 capability gaps (AP-1..AP-9) via REUSE-before-build**. Pure-data registry `config/assessment-architecture.ts` composing the frozen `assessment-framework.ts` (FROZEN 13-layer architecture foundation→question_platform→authoring→delivery→scoring→norms→standardization→benchmarking→ai_interpretation→report_intelligence→visualization→analytics→administration; 2 families CAPADEX behavioural-signal + CAF competency; 10-type taxonomy + 22-entry crosswalk; ONE 10-state lifecycle mapped onto existing per-artifact states; 7 governance controls; 18-field metadata standard + per-source coverage crosswalk; 15-step Question→Outcome mapping model; ARCHITECTURE_DECISIONS, OVERLAP_DECISIONS, `ARCHITECTURE_GAPS=[]` + `RESOLVED_ARCHITECTURE_GAPS`=9) ⟂ never-throws read-only composer `services/assessment-architecture-engine.ts` (`composeArchitecture`/`composeLifecycle`/`composeGovernance`/`composeMetadata`/`composeRepositoryAlignment`/`composeSummary` + `classifiedGaps` returning `resolved_gaps`/`resolved_gap_counts`/`resolved_gap_count`; `readScalar`/`readRows` null-on-error, `to_regclass` + fs probes only) certifying **FIVE INDEPENDENT axes — architecture · lifecycle · governance · metadata · repository-alignment — NEVER composited** ⟂ routes `routes/assessment-architecture.ts` (`/api/assessment-architecture/enabled` flag probe + super-admin cert GETs `/model`,`/architecture`,`/lifecycle`,`/governance`,`/metadata`,`/repository-alignment`,`/gaps`,`/summary` + mechanism `/standardization`,`/norm-groups`(+POST `/compute`),`/bloom`(+POST `/classify`),`/prompts`(+POST `/register`),`/country-cohorts`(+POST `/register`) — **POSTs are the ONLY DDL sites**, flag+super-admin gated; flagGate 503-before-auth on `/enabled`, super-admin `/api/admin/*` return 401 OFF via the GLOBAL `/api/admin` gate — OFF smoke ∈ {401,403,503}). Mechanisms via REUSE: `services/assessment-architecture-mechanisms.ts` (`computeGroupNorms`/`classifyClarityBloom`/`registerCountryCohort`, `ASSESSMENT_NORM_K_MIN=30`), `services/psychometric-standardization.ts` (`standardScoresFromZ`: T M=50/SD=10, stanine 1–9, sten 1–10; legacy SD=15 honestly relabelled `deviation_score`), `services/prompt-registry-activation.ts` (`registerCodeEmbeddedPrompts`/`resolvePrompt` over EXISTING `aig_prompts`/`aig_prompt_versions` with code-literal fallback), `frontend/src/lib/offline.ts`+`public/sw.js` (AP-2) + `frontend/src/lib/accessibility.ts` (AP-3) wired flag-gated in `main.tsx`+`FreeAssessmentModal.tsx`. **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `assessment_architecture_completion` must IMPORT `isAssessmentArchitectureCompletionEnabled` or the endpoint 500s. SSoT scan `scripts/capadex-3.1-assessment-architecture-scan.ts`→`audit/program-3-phase-3.1-assessment-architecture/scan.json` (embeds resolved_gaps); generator `scripts/capadex-3.1-generate-deliverables.ts` reads ONLY scan.json → 10 numbered docs + completion-certification. Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (architecture **13/13 SUPPORTED**; lifecycle 3 SUPPORTED·3 PARTIAL; governance 7 SUPPORTED; metadata 16/18 covered·6 sources; repo-align svc 37/38·rt 14/14·fe 5/8·tbl 38/38). **`ARCHITECTURE_GAPS=[]` (0 OPEN) · 9 RESOLVED via reuse** (former GAP-AP-1..9: 0 Launch-Critical·0 High·5 Medium·3 Low·1 Future — `audit/program-3-assessment-platform-blueprint/18-capability-gap-register.md` reports RESOLVED). **Engineering closure ⟂ Adoption:** the mechanism EXISTS for every gap but real norm/offline/audit/prompt DATA volume is honest-low/0 — a usage axis reported SEPARATELY, NEVER a gap. Coverage⟂Confidence⟂Adoption never composited; null≠0; norms/benchmarks ABSTAIN (dimension_source_absent/gender ethics_gated_off via `ASSESSMENT_GENDER_NORMS_ENABLED`/k_min=30), never fabricate. → `.agents/memory/assessment-architecture-completion.md`.
- **Enterprise Question Management Platform** (CAPADEX 3.0 Program 3 · Phase 3.2, flag `questionManagementPlatform`/`FF_QUESTION_MANAGEMENT_PLATFORM`, default OFF byte-identical incl. schema — **DDL ONLY on the flag-gated write paths (mechanism POSTs), never at read time**) the ONE canonical Question Management Platform: a single certified layer COMPOSING the EXISTING question services under one registry (`capadex_question_registry`) + an additive `qmp_*` overlay — **NO duplicate platform, NO V2, NO breaking change** — that ALSO **engineering-closes all 8 gaps (QM-1..QM-8) via REUSE-before-build**. Mirrors 1.3–1.7 / 3.1 EXACTLY. pure-data registry `config/question-management-platform.ts` (8 `QMP_DIMENSIONS` platform·library·metadata·governance·version_management·workflow·apis·frontend; 29-type `QUESTION_TYPES`=14 SUP/15 PART — types without a dedicated renderer honestly PARTIAL; 36-field `METADATA_STANDARD`+4 sources; 9 `LIFECYCLE_STATES`+`LIFECYCLE_MAPPING` onto the existing 6-state CHECK no-break; `GOVERNANCE_CONTROLS`8/`VERSION_CAPABILITIES`7/`WORKFLOW_STAGES`7/`SEARCH_CAPABILITIES`5/`BULK_OPERATIONS`5; 6 `LIBRARY_SCOPES`; 8-step `MAPPING_MODEL`; 5 `QMP_DECISIONS`; `QMP_GAPS=[]` + `RESOLVED_QMP_GAPS`=8) ⟂ never-throws read-only composer `services/question-management-engine.ts` (`composeDimensions`/`composeTypeCatalog`/`composeMetadata`/`composeLifecycle`/`composeGovernance`/`composeVersioning`/`composeWorkflow`/`composeSearch`/`composeBulkOps`/`composeLibrary`/`composeRepositoryAlignment`/`composeAdoption`/`composeSummary`+`classifiedGaps`; `readScalar` null-on-error/0-on-no-rows, `to_regclass`+fs probes only) certifying **EIGHT INDEPENDENT dimensions NEVER composited** ⟂ mechanisms `services/question-management-mechanisms.ts` (**the ONLY DDL sites** — `qmp_question_metadata`/`qmp_question_versions`/`qmp_workflow`/`qmp_collections`/`qmp_saved_searches`/`qmp_bulk_jobs`, all flag+super-admin gated) ⟂ routes `routes/question-management.ts` (`/api/question-management/enabled` flag probe 503-before-auth OFF + super-admin cert GETs `/model`,`/dimensions`,`/types`,`/metadata`,`/lifecycle`,`/governance`,`/versioning`,`/workflow`,`/search`,`/bulk-ops`,`/library`,`/repository-alignment`,`/adoption`,`/gaps`,`/summary` + mechanism GET/POST `/metadata/upsert`,`/versions/*`,`/collections/*`,`/searches/*`,`/bulk/*`,`/workflow/transition` — POSTs are the ONLY DDL sites; super-admin `/api/admin/*` return 401 OFF via the GLOBAL `/api/admin` gate — OFF smoke ∈ {401,403,503}). Frontend `components/superadmin/QuestionManagementPanel.tsx` read-only console (conditional-spread nav in `SuperAdminDashboard.tsx`, tab hidden OFF). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `question_management_platform` must IMPORT `isQuestionManagementPlatformEnabled` or the endpoint 500s. SSoT scan `scripts/capadex-3.2-question-management-scan.ts`→`audit/capadex-3.2-question-management/scan.json` (SCAN_HASH sha256); generator `scripts/capadex-3.2-generate-deliverables.ts` reads ONLY scan.json → **14 deliverables** (13 numbered + completion-certification). Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (**all 8 dimensions SUPPORTED**; types 14 SUP·15 PART; metadata 15/36 covered·4 sources; lifecycle 9 SUP; governance 8 SUP; version 7 SUP; workflow 7 SUP; search 5 SUP·bulk 5 SUP·6 library scopes; repo-align svc 20/20·rt 15/15·fe 15/15·**tbl 9/15 — the 6 `qmp_*` overlay tables read ABSENT until the flag-gated mechanism POSTs run, which is HONEST not a defect**). **`QMP_GAPS=[]` (0 OPEN) · 8 RESOLVED via reuse** (former QM-1..8: 0 Launch-Critical·3 High·4 Medium·1 Low). **Engineering closure ⟂ Adoption:** the mechanism EXISTS for every gap but real authored-question VOLUME across the overlay is honest-low/0 — a usage axis reported SEPARATELY, NEVER a gap, NEVER fabricated as adopted. Coverage⟂Confidence⟂Adoption never composited; null≠0; never fabricate. STOP for approval (flag stays OFF, no merge/enable/deploy). → `docs/QUESTION_MANAGEMENT.md` · `.agents/memory/question-management-platform.md`.
- **Persona Experience / Intelligent Journey Router** (CAPADEX 3.0 Program 3 · Phase 3.2A, flag `personaJourneyRouter`/`FF_PERSONA_JOURNEY_ROUTER`, default OFF byte-identical incl. schema — ZERO DDL, FRONTEND-led) replaces the generic single-page persona selector (`IntroPhase`) with a progressive 5-step onboarding wizard (Who → Refine → Goal → Personalize → AI Journey Router) **only when ON**; OFF renders the unchanged `IntroPhase`. **Single-taxonomy-no-fork:** both the wizard and `IntroPhase` consume the ONE shared `frontend/src/lib/persona-taxonomy.ts` (`buildTrackGroups({alignment,expansion})`, extracted verbatim from the old inline `IntroPhase` data — `buildTrackGroups(false,false)`==legacy; flag-gated alignment/expansion spreads preserved). New `frontend/src/components/assessment/PersonaJourneyWizard.tsx` (search/keyboard/a11y/localStorage-resume `capadex_persona_journey_wizard_v1`, B2B/admin personas route OUT to EXISTING employer-login/login/admin-login, Step-5 renders `journey.statusNote` VERBATIM) mounted in `FreeAssessmentModal.tsx` behind `persona_journey_router` (public-config). Backend `routes/persona-journey.ts` — public GET-only PURE never-throws resolver (ZERO DDL, no DB reads) composing `config/customer-journey.ts`+`config/assessment-framework.ts`+`lib/lifecycle.ts`: `/enabled` (503 OFF) + `/route` deterministic (sub-persona→journey, legacyKey fallback: student/campus→student_career·jobseeker→fresher_placement·professional→professional_progression·parent→parent_support·teacher→faculty_students; unmapped→`resolved:false` `no_assessment_journey`, NEVER fabricated). ON verified in-process via `scripts/task350-persona-journey-verify.ts` (env set BEFORE import, never touches live flag); FE validated via esbuild parse. 6 deliverables `audit/capadex-3.2a-persona-journey-router/`. Coverage⟂Confidence⟂Adoption never composited; null≠0. → `.agents/memory/persona-journey-router.md`.
- **Enterprise Assessment Builder** (CAPADEX 3.0 Program 3 · Phase 3.3, flag `assessmentBuilder`/`FF_ASSESSMENT_BUILDER`, default OFF byte-identical incl. schema — **DDL ONLY on the flag-gated write paths (mechanism POSTs), never at read time**) the ONE canonical **AUTHORING** layer — **scope is design/compose/configure/validate/version/approve/publish; NOT delivery/scoring/psychometrics** — COMPOSING the EXISTING assessment services (CAF builder / blueprint-builder / assembly / writer / architecture) under one registry + an additive `ab_*` overlay (**never forks `caf_assessments`, NO duplicate builder, NO V2, NO breaking change**) that ALSO **engineering-closes all 7 gaps (AB-1..AB-7) via REUSE-before-build**. Mirrors 3.2 EXACTLY. pure-data registry `config/assessment-builder.ts` (7 `AB_DIMENSIONS` builder·blueprint·validation·version_management·publishing·apis·frontend; 7 `DESIGNER_ACTIONS`; 10 `STRUCTURE_LEVELS`; 8 `COMPOSITION_CAPS`; 12 `REUSABLE_TEMPLATES`; 8 `BLUEPRINT_CAPS`; 10 `RULE_TYPES`(9 SUP·1 PART); 8 `CONFIG_OPTIONS`; 7 `VERSION_CAPABILITIES`; 7 `VALIDATION_CHECKS`; 7 `WORKFLOW_STATES` draft→review→approved→published→active→deprecated→archived; `MAPPING_MODEL`; `AB_DECISIONS`; `AB_GAPS=[]` + `RESOLVED_AB_GAPS`=7) ⟂ never-throws read-only composer `services/assessment-builder-engine.ts` (`composeDimensions`/`composeDesignerActions`/`composeStructureLevels`/`composeCompositionCaps`/`composeTemplates`/`composeBlueprintCaps`/`composeRuleTypes`/`composeConfigOptions`/`composeVersioning`/`composeValidationChecks`/`composeWorkflow`/`composeMapping`/`composeRepositoryAlignment`/`composeAdoption`/`composeSummary`+`classifiedGaps`; `readScalar` null-on-error/0-on-no-rows, `to_regclass`+fs probes only) certifying **SEVEN INDEPENDENT dimensions NEVER composited** ⟂ mechanisms `services/assessment-builder-mechanisms.ts` (**the ONLY DDL sites** — `ab_assessments`/`ab_assessment_versions`/`ab_blueprints`/`ab_templates`/`ab_validation_runs`/`ab_workflow`, all flag+super-admin gated) ⟂ routes `routes/assessment-builder.ts` (`/api/assessment-builder/enabled` flag probe 503-before-auth OFF + super-admin cert GETs `/model`,`/dimensions`,`/blueprint`,`/rules`,`/config`,`/versioning`,`/validation`,`/workflow`,`/mapping`,`/adoption`,`/gaps`,`/summary` + mechanism GET/POST — POSTs are the ONLY DDL sites; super-admin `/api/admin/*`-style routes return 401/403 OFF via the GLOBAL gate — OFF smoke ∈ {401,403,503}). Frontend `components/superadmin/AssessmentBuilderPanel.tsx` read-only console (conditional-spread nav in `SuperAdminDashboard.tsx`, tab probes `/enabled`, hidden OFF). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `assessment_builder` must IMPORT `isAssessmentBuilderEnabled` or the endpoint 500s. SSoT scan `scripts/capadex-3.3-assessment-builder-scan.ts`→`audit/capadex-3.3-assessment-builder/scan.json` (SCAN_HASH sha256); generator `scripts/capadex-3.3-generate-deliverables.ts` reads ONLY scan.json → **14 deliverables** (13 numbered + completion-certification). Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (**all 7 dimensions SUPPORTED**; blueprint 8 SUP; validation 7 SUP; version 7 SUP; workflow 7 SUP; rules 9 SUP·1 PART; config 8 SUP; repo-align svc 15/15·rt 11/11·fe 13/13·**tbl 8/13 — the 5 `ab_*` overlay tables read ABSENT until the flag-gated mechanism POSTs run, which is HONEST not a defect**). **`AB_GAPS=[]` (0 OPEN) · 7 RESOLVED via reuse** (former AB-1..7: 0 Launch-Critical·4 High·2 Medium·1 Low). **Engineering closure ⟂ Adoption:** the mechanism EXISTS for every gap but real authored-assessment VOLUME across the overlay is honest-low/0 — a usage axis reported SEPARATELY, NEVER a gap, NEVER fabricated as adopted. Coverage⟂Confidence⟂Adoption never composited; null≠0; never fabricate. STOP for approval (flag stays OFF, no merge/enable/deploy). → `docs/ASSESSMENT_BUILDER.md` · `.agents/memory/assessment-builder.md`.
- **Enterprise Assessment Delivery Engine** (CAPADEX 3.0 Program 3 · Phase 3.4, flag `assessmentDelivery`/`FF_ASSESSMENT_DELIVERY`, default OFF byte-identical incl. schema — **DDL ONLY on the flag-gated write paths (mechanism POSTs), never at read time**) the ONE canonical **CANDIDATE-EXPERIENCE** platform — **scope is launch→final-submission ONLY; explicitly NOT scoring/psychometrics/norms/standardization/benchmarking/AI-interpretation/reports/analytics = Phase 3.5+** — COMPOSING the EXISTING assessment runtimes (`adaptive-assessment`/`caf-runtime`/`dynamic-assessment-runtime`) + cohort-gating + notification-engine + unified-audit-trail + security-middleware under one registry + an additive `ad_*` overlay (**NO duplicate delivery engine, NO V2, NO breaking change**), engineering-closing 7 gaps (AD-1..AD-7) via REUSE-before-build. Mirrors 3.3 EXACTLY **except**: **EXACTLY 12 deliverables** (not 3.3's 14) `01-executive-summary`…`12-phase-3.4-certification`; 7 DELIVERY dimensions **delivery_engine·candidate_experience·session_management·accessibility·security·apis·frontend**; summary field names + adoption sub-objects differ (see memory). pure-data registry `config/assessment-delivery.ts` (7 `AD_DIMENSIONS`; 11 candidate-experience steps; 6 delivery modes 3 SUP·3 PART; 7 question-delivery modes 6 SUP·1 PART; 6 launch modes; 9 session caps; 6 timing caps; 6 response caps; 7 accessibility caps; 6 security controls 5 SUP·1 PART; 6 notification types; `MAPPING_MODEL` launch→submission; `AD_DECISIONS`; `AD_GAPS` OPEN + `RESOLVED_AD_GAPS`=7) ⟂ never-throws read-only composer `services/assessment-delivery-engine.ts` (`compose*` per dimension + `composeRepositoryAlignment`/`composeAdoption`/`composeSummary`+`classifiedGaps`; `readScalar`/`readRows` null-on-error, `to_regclass`+fs probes only, includes `ready_for_phase_3_5`) certifying **SEVEN INDEPENDENT dimensions NEVER composited** ⟂ mechanisms `services/assessment-delivery-mechanisms.ts` (**the ONLY DDL sites** — `ad_*` overlay launches/sessions/responses/events/notifications, flag+super-admin gated) ⟂ routes `routes/assessment-delivery.ts` (`/api/assessment-delivery/enabled` flag probe 503-before-auth OFF + super-admin cert GETs `/model`,`/dimensions`,`/candidate-experience`,`/delivery-modes`,`/question-delivery`,`/launch`,`/session`,`/timing`,`/response`,`/accessibility`,`/security`,`/notifications`,`/mapping`,`/repository-alignment`,`/adoption`,`/gaps`,`/summary` + mechanism GET/POST launches/sessions/responses/events/notifications — POSTs the ONLY DDL sites; `/api/admin/*` 401 OFF via GLOBAL gate — OFF smoke ∈ {401,503}). Frontend `components/superadmin/AssessmentDeliveryPanel.tsx` read-only console (conditional-spread nav in `SuperAdminDashboard.tsx`, probes `/enabled`, hidden OFF). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `assessment_delivery` must IMPORT `isAssessmentDeliveryEnabled` or the endpoint 500s. SSoT scan `scripts/capadex-3.4-assessment-delivery-scan.ts`→`audit/capadex-3.4-assessment-delivery/scan.json` (SCAN_HASH sha256); generator `scripts/capadex-3.4-generate-deliverables.ts` reads ONLY scan.json → **12 deliverables** (asserts EXACTLY 12 by name) — **re-run BOTH scan+generator AFTER creating the panel** or frontend dim stays 4/5. Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (**all 7 dimensions SUPPORTED**; repo-align svc 11/11·rt 11/11·fe 15/15·**tbl 8/13 — the 5 `ad_*` overlay tables read ABSENT until the flag-gated mechanism POSTs run, HONEST not a defect**; gaps 0 Launch-Critical·1 Low·3 Future = 4 OPEN + 7 RESOLVED via reuse; **ready_for_3.5: YES**). PARTIAL delivery modes (coding/video/simulation) + question-delivery (real adaptive routing DEPENDS ON 3.5) + security (browser lockdown/proctoring) are honest Future deferrals. **Engineering closure ⟂ Adoption:** real delivered-session VOLUME is honest 0 — a usage axis reported SEPARATELY, NEVER a gap. Coverage⟂Confidence⟂Adoption never composited; null≠0; never fabricate. STOP for approval (flag stays OFF, no merge/enable/deploy). → `.agents/memory/assessment-delivery.md`.
- **Assessment Science / Psychometrics / Item Intelligence** (CAPADEX 3.0 Program 3 · Phase 3.6, flag `assessmentScience`/`FF_ASSESSMENT_SCIENCE`, default OFF byte-identical incl. schema — **DDL ONLY on the flag-gated write paths (mechanism POSTs), never at read time**) the ONE canonical **INSTRUMENT-QUALITY** layer — **scope is item analysis · reliability · validity · quality governance · blueprint validation; it measures how GOOD the assessment/question is and NEVER scores or interprets a candidate; explicitly NOT norms/standardization/benchmarking/AI-interpretation/reports/candidate-performance = Phase 3.7** — COMPOSING the EXISTING psychometric services (`psychometric-intelligence-engine`/`sci-psychometric-engine`/`reliability-engine`/`quality-validator`/`assessment-blueprint-engine`) under one registry + an additive `asci_*` overlay (**NO duplicate psychometric engine, NO V2, NO breaking change**), engineering-closing 6 gaps (GAP-ASCI-1..6) via REUSE-before-build. Mirrors 3.3/3.4/3.5. pure-data registry `config/assessment-science.ts` (8 `ASCI_DIMENSIONS` item_analysis·reliability·validity·quality_governance·blueprint_validation·frontend·ux·apis; 9 `ITEM_ANALYSIS_METRICS` 6 SUP·3 PART; 7 `RELIABILITY_TYPES` 6 SUP·1 PART; 8 `VALIDITY_TYPES` 5 SUP·3 PART; 6 `QUALITY_CHECKS` 6 SUP; 7 `GOVERNANCE_STAGES` 6 SUP·1 PART; 8 `BLUEPRINT_COVERAGE` 7 SUP·1 PART; 10-step `MAPPING_MODEL` 9 SUP·1 PART; `ASCI_DECISIONS` D1–D5; `ASCI_K_MIN=30`; `ASCI_GAPS=[]` + `RESOLVED_ASCI_GAPS`=6) ⟂ pure mechanisms `services/assessment-science-mechanisms.ts` (`computeItemAnalysis`/`computeReliability`/`computeValidity`/`validateQuestionQuality`/`validateBlueprint` reusing pearsonR/variance/irt3PL/adverseImpact/cronbachAlpha/cohensKappa/constructValidity/factorLoading + the `asci_*` overlay ensure-schema/save — **the ONLY DDL sites**; item-level stats ABSTAIN < k_min=30) ⟂ never-throws read-only composer `services/assessment-science-engine.ts` (`composeDimensions`/`composeMapping`/`composeRepositoryAlignment`/`composeAdoption`/`composeSummary` + `classifiedGaps`; catalog composers are INTERNAL to `composeSummary`; `readScalar`/`readRows` null-on-error, `to_regclass`+fs probes only) certifying **EIGHT INDEPENDENT dimensions NEVER composited** ⟂ routes `routes/assessment-science.ts` (`/api/assessment-science/enabled` flag probe 503-before-auth OFF + super-admin cert GETs + pure mechanism POSTs `compute/{item-analysis,reliability,validity,item-information,item-dif}`·`validate/{question-quality,blueprint}` + overlay writes — POSTs the ONLY DDL sites; `/api/admin/*` 401 OFF via GLOBAL gate — OFF smoke ∈ {401,403,503}). Frontend `components/superadmin/AssessmentSciencePanel.tsx` + interactive `components/science/PsychometricsWorkbench.tsx` (conditional-spread nav in `SuperAdminDashboard.tsx`, probes `/enabled`, hidden OFF; ABSTAIN/empty/loading/error states). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `assessment_science` must IMPORT `isAssessmentScienceEnabled` or the endpoint 500s. SSoT scan `scripts/capadex-3.6-assessment-science-scan.ts`→`audit/capadex-3.6-assessment-science/scan.json` (SCAN_HASH sha256; scan computes catalog status_counts itself + embeds full registry); generator `scripts/capadex-3.6-generate-deliverables.ts` reads ONLY scan.json → **13 deliverables** (separate `12-adoption-report` + `13-phase-3.6-certification`; asserts EXACTLY 13 by name). **Summary field is `ready_for_phase_3_7` (NOT 3_6), NO `loop_closure` field.** Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (**all 8 `axis_dimensions` SUPPORTED**; repo-align svc 19/19·rt 6/6·fe 10/10·**tbl 6/13 — the 7 `asci_*` overlay tables read ABSENT until the flag-gated mechanism POSTs run, HONEST not a defect**; `ASCI_GAPS=[]` 0 OPEN + 6 RESOLVED via reuse = 3 High·3 Medium; **ready_for_3.7: YES**). **Engineering closure ⟂ Adoption:** the mechanism EXISTS for every gap but real analysed-item / response VOLUME across the overlay is honest-low/0 — a usage axis reported SEPARATELY, NEVER a gap, NEVER fabricated as adopted; item-level stats ABSTAIN < k_min=30. Phase-3.7 boundaries (norms/standardization/benchmarking/AI/reports/candidate-performance) are scope boundaries reported in-line, NOT gaps. Coverage⟂Confidence⟂Adoption never composited; null≠0; never fabricate. STOP for approval (flag stays OFF, no merge/enable/deploy). → `docs/ASSESSMENT_SCIENCE.md` · `.agents/memory/assessment-science.md`.
- **Assessment Intelligence / Interpretation & Reporting** (CAPADEX 3.0 Program 3 · Phase 3.7, flag `assessmentIntelligence`/`FF_ASSESSMENT_INTELLIGENCE`, default OFF byte-identical incl. schema — **DDL ONLY on the flag-gated write paths (mechanism/overlay POSTs), never at read time**) the ONE canonical **INTERPRETATION & REPORTING** layer — **scope turns a SCORED + VALIDATED result (3.5 Scoring + 3.6 Science) into MEANING (norm-referencing · standardization · benchmarking · AI-interpretation · report intelligence · candidate performance); it NEVER re-scores or re-validates the instrument; realized outcomes/KPI roll-up are downstream Outcome/KPI scope** — COMPOSING the EXISTING interpretation services (`psychometric-standardization`/`benchmark-engine`/`peer-benchmark`/`intelligence-narrative-engine`/`ai-reasoning-engine`/`dynamic-report`) under one registry + an additive `aint_*` overlay (**NO duplicate interpretation/benchmark/narrative/report engine, NO V2, NO breaking change**), engineering-closing 6 gaps (GAP-AINT-1..6) via REUSE-before-build. Mirrors 3.3/3.4/3.5/3.6. pure-data registry `config/assessment-intelligence.ts` (8 `AINT_DIMENSIONS` norms·standardization·benchmarking·ai_interpretation·report_intelligence·candidate_performance·frontend·apis — **NO `ux` dim, unlike 3.6**; 7 `NORM_TYPES` 4 SUP·3 PART; 8 `STANDARD_SCORE_TYPES` 6 SUP·2 PART; 6 `BENCHMARK_SCOPES` 4 SUP·2 PART; 6 `AI_INTERPRETATION_CAPABILITIES` 5 SUP·1 PART; 8 `REPORT_SECTIONS` 7 SUP·1 PART; 8 `PERFORMANCE_METRICS` 6 SUP·2 PART; 9-step `MAPPING_MODEL` 8 SUP·1 PART; `AINT_DECISIONS` D1–D5; `AINT_K_MIN=30`; `AINT_GAPS=[]` + `RESOLVED_AINT_GAPS`=6) ⟂ pure mechanisms `services/assessment-intelligence-mechanisms.ts` (`computeNormReference`/`computeStandardScores`/`computeBenchmark`/`computeInterpretation`/`computeReport`/`computePerformance` reusing peer-benchmark/benchmark-engine/psychometric-standardization/intelligence-narrative-engine/ai-reasoning-engine/dynamic-report + longitudinal snapshots + the `aint_*` overlay ensure-schema/save — **the ONLY DDL sites**; norm-referenced stats + benchmarks ABSTAIN < k_min=30, AI narrative confidence honest-null while cold-start) ⟂ never-throws read-only composer `services/assessment-intelligence-engine.ts` (`composeDimensions`/`composeMapping`/`composeRepositoryAlignment`/`composeAdoption`/`composeSummary` + `classifiedGaps`; catalog composers are INTERNAL to `composeSummary`; `readScalar`/`readRows` null-on-error, `to_regclass`+fs probes only) certifying **EIGHT INDEPENDENT dimensions NEVER composited** ⟂ routes `routes/assessment-intelligence.ts` (`/api/assessment-intelligence/enabled` flag probe 503-before-auth OFF + super-admin cert GETs + pure mechanism POSTs `compute/{standard-scores,norm-reference,benchmark,interpretation,report,performance}` + overlay writes `{norm-tables,standard-scores,benchmarks,interpretations,reports,performance,repository}/save` + list GETs — POSTs the ONLY DDL sites; `/api/admin/*` 401 OFF via GLOBAL gate — OFF smoke ∈ {401,403,503}). Frontend `components/superadmin/AssessmentIntelligencePanel.tsx` + interactive `components/intelligence/InterpretationWorkbench.tsx` (conditional-spread nav in `SuperAdminDashboard.tsx`, probes `/enabled`, hidden OFF; ABSTAIN/empty/loading/error states). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `assessment_intelligence` must IMPORT `isAssessmentIntelligenceEnabled` or the endpoint 500s. SSoT scan `scripts/capadex-3.7-assessment-intelligence-scan.ts`→`audit/capadex-3.7-assessment-intelligence/scan.json` (SCAN_HASH sha256; scan computes catalog status_counts itself + embeds full registry); generator `scripts/capadex-3.7-generate-deliverables.ts` reads ONLY scan.json → **13 deliverables** (separate `12-adoption-report` + `13-phase-3.7-certification`; asserts EXACTLY 13 by name). **Summary field is `ready_for_certification` (NOT `ready_for_phase_3_7`, unlike 3.6) + `enterprise_ready`; NO `loop_closure` field.** Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (**all 8 `axis_dimensions` SUPPORTED**; repo-align svc 20/20·rt 7/7·fe 9/9·**tbl 6/14 — the `aint_*` overlay tables read ABSENT until the flag-gated mechanism POSTs run, HONEST not a defect**; `AINT_GAPS=[]` 0 OPEN + 6 RESOLVED via reuse = 3 High·3 Medium; **ready_for_certification: YES**). **Engineering closure ⟂ Adoption:** the mechanism EXISTS for every gap but real interpreted/benchmarked/reported VOLUME across the overlay is honest-low/0 — a usage axis reported SEPARATELY, NEVER a gap, NEVER fabricated as adopted; norm-referenced stats ABSTAIN < k_min=30. PARTIAL catalog entries (age/national/custom norms, NCE/scaled scores, institution/national benchmarks, interpretation confidence, next-steps plans, consistency/timing) are data-availability / first-class-objective boundaries reported in-line, NOT gaps. Coverage⟂Confidence⟂Adoption never composited; null≠0; never fabricate. STOP for approval (flag stays OFF, no merge/enable/deploy). → `docs/ASSESSMENT_INTELLIGENCE.md` · `.agents/memory/assessment-intelligence.md`.
- **Program-1 Product Certification** (CAPADEX 3.0 Phase 1.8 — Program-1 capstone, flag `productTraceabilityCertification`/`FF_PRODUCT_TRACEABILITY_CERTIFICATION`, default OFF byte-identical incl. schema — ZERO DDL, owns 0 tables; READS only) the READ-ONLY flag-gated certification composer that audits + certifies everything built in Phases 1.1–1.7 against the FROZEN Product Blueprint (`audit/capadex-3.0-product-blueprint-final/{15_PRODUCT_TRACEABILITY_MATRIX,16_PRODUCT_BLUEPRINT_FINAL}.md`). Mirrors MX-800 2.14 / MX-700 1.43 EXACTLY. STRICT: Repository-First/Blueprint-First/Validation-First/Enhancement-Only/NO new architecture/NO new features/NO duplicate logic/NO breaking change/byte-identical-OFF/zero-DDL/Human Approval Required. pure-data registry `config/program1-certification-model.ts` (PROGRAM1_PHASES 1.1–1.7 w/ flag/publicConfigKey/config/service/routeFile/registerFn/getterKey/auditDir + PROGRAM1_FREEZE + 21-node TRACEABILITY_CHAIN + 13 BUSINESS_DOMAINS D13 Outcome&KPI keystone + PERSONAS + 4 LIFECYCLE_STAGES + 4 CERTIFICATION_DIMENSIONS + GAP_SEVERITIES + PROGRAM1_GAPS + HONESTY_CONTRACT) ⟂ read-only composer `services/program1-certification-engine.ts` (`gatherPhaseGetters` invokes each phase getter EXACTLY ONCE in parallel via `safe()`; fs existence scans; wiring index reads routes.ts/capadex.ts ONCE; duplicate scan; per-node traceability matrix INTACT/PARTIAL/BREAK; 4 axes; `composeCertification`+`composeCertificationSummary`; GET-only never-throws no DDL; engines read by existence/persisted-output NEVER invoked) ⟂ routes `routes/program1-certification.ts` (`/api/program1-certification/enabled` UNGATED 200 probe + super-admin `/feature-flag`,`/model`,`/certification`,`/traceability`,`/gaps`,`/summary`, flag-gate 503→requireAuth→requireSuperAdmin, never-throws). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `product_traceability_certification` must IMPORT `isProductTraceabilityCertificationEnabled` or the endpoint 500s (no tsc). **FOUR INDEPENDENT axes NEVER composited:** Structural Completeness ⟂ Functional Integration (Integrated≠Activated) ⟂ Product Maturity (ceiling Managed/L3, L4–5 WITHHELD) ⟂ Enterprise Launch Readiness (null — WITHHELD by design, needs runtime adoption + realized outcomes). 1.1 has NO getter (lifecycle substrate, registered/pcfg/getter honest null=`—` not false). SSoT scan `scripts/capadex-1.8-program1-certification-scan.ts` (self-enables 7 FF_* in-process = cosmetic, composers flag-independent)→`audit/capadex-3.0-program1-certification/scan.json`; generator `scripts/capadex-1.8-generate-deliverables.ts` reads ONLY scan.json (sha256+mtime, fail-fast) → 15 deliverables (01 Exec Summary…15 Founder Decision). Verdict `STRUCTURAL_CERTIFIED` (7/7 phases present·routes 6/6·getters 6/6·duplicate-clean; traceability 21 INTACT·0 PARTIAL·0 BREAK; gaps 0 Launch-Critical·1 High·3 Medium·1 Low·1 Future = carried-forward ADOPTION/CONFIDENCE/Future items NOT Program-1 defects), `production_ready:false`, `enterprise_launch_readiness:null`. ADOPTION-gated keystone (Outcome→KPI) is STRUCTURALLY INTACT — adoption shortfall reported on its OWN axis NEVER as a chain BREAK. Coverage⟂Confidence⟂Outcome⟂Adoption never composited; null≠0; never fabricate. STOP for approval (flag stays OFF, no merge/enable/deploy). → `docs/PROGRAM1_CERTIFICATION.md` · `.agents/memory/program1-product-certification.md`.
- **Observability, Monitoring & Operational Readiness** (CAPADEX 3.0 Program 2 · Phase 2.5, flag `operationalReadiness`/`FF_OPERATIONAL_READINESS`, default OFF byte-identical incl. schema — read paths ZERO DDL; every write path guards the flag + lazily creates its `ops_*`/`operational_readiness_snapshots` table only on first flag-ON write → **owns 0 tables OFF**) answers "can CAPADEX be operated, monitored, supported, maintained as an enterprise platform?" — additive / flag-gated / reuse-before-build / no V2. COMPOSES the EXISTING observability substrate into ONE certification view (**10 SEPARATE operational axes NEVER combined**: observability·monitoring·logging·metrics·alerting·ai_operations·assessment_operations·report_operations·disaster_recovery·operational_readiness) AND ships the real mechanisms that CLOSE the 7 formerly-classified gaps. pure-data registry `config/operational-readiness-model.ts` (OPERATIONAL_AXES ×10, OPERATIONAL_DOMAINS ×12 each mapped to an axis w/ `reuses:{services,routes,frontend,tables}` + `adoptionTable`, OPERATIONAL_DECISIONS, `OPERATIONAL_GAPS=[]`, RESOLVED_OPERATIONAL_GAPS ×13, META) ⟂ read-only composer `services/operational-readiness-engine.ts` (`composeCoverage`(verify evidence vs live FS+DB, engines NEVER invoked)/`composeCertification`(per-axis structural-coverage score, null when no measurable substrate)/`composeAdoption`(SEPARATE real-volume axis)/`composeGaps`/`composeValidation`/`composeSummary` + `captureOperationalSnapshot`/`getOperationalSnapshots`; GET-only never-throws, null-on-error scalar/rows, `to_regclass` probe, FS existence). **Gap-closure mechanisms (real, additive, all flag-guarded + lazy-ensure-schema): `services/ops/metrics-registry.ts`** (counters/gauges/histograms + `opsMetricsMiddleware` mounted in `index.ts` + Prometheus render → GAP-OPS-1 + GAP-OPS-6 `/version`+`/metrics`), **`services/ops/durable-queue.ts`** (`ops_job_queue`+`ops_job_dead_letter`+`startQueueWorker` retry/backoff → GAP-OPS-2), **`services/ops/alerting.ts`** (`ops_alert_rules`+`ops_alert_events`+rule eval vs live metric signals+email routing → GAP-OPS-3), **`services/ops/ai-token-accounting.ts`** (`ops_ai_token_usage` per-call token+cost, hooked from `aiClient.ts` → GAP-OPS-4), correlation-ID (`routes.ts` upload-proxy `on:{proxyReq}` injects `x-correlation-id` + `backend-main/app/main.py` `correlation_id_mw` echoes → GAP-OPS-5), DR (`config/disaster-recovery-manifest.ts`+`scripts/ops-dr-verify.ts`+`docs/DISASTER_RECOVERY.md` → GAP-OPS-7). ⟂ routes `routes/operational-readiness.ts` (`/enabled` UNGATED probe + super-admin `/model`,`/coverage`,`/certification`,`/adoption`,`/gaps`,`/validation`,`/summary`,`/snapshots`,`/version`,`/metrics`,`/metrics.json`,`/queue/{stats,dead-letter,enqueue,run}`,`/alerts/{rules,rules/:id/toggle,events,evaluate}`,`/ai/token-usage`,`/dr/{manifest,readiness}` + POST `/audit/capture`; flag-gate 503→requireAuth→requireSuperAdmin, never-throws; `startQueueWorker(pool)` at registration is no-op OFF). **public-config getter is a SEPARATE import site:** `routes/capadex.ts` `/public-config` `operational_readiness` must IMPORT `isOperationalReadinessEnabled`. **POST smoke returns 403 not 503 OFF** (CSRF fires before flag gate on mutating methods — accepted ∈{401,403,503}); GET data routes DO 503-before-auth. **Structural coverage = evidence EXISTS (verified vs live FS+DB), NOT quality/adoption.** **DR now SUPPORTED** via in-repo manifest+verifier+runbook, but live restore-drill EXECUTION + managed-DB backups remain infra-owned (declared/verified, never claimed drilled). SSoT scan `scripts/program2-2.5-operational-readiness-scan.ts`→`audit/program-2-operational-readiness/scan.json`; generator `scripts/program2-2.5-generate-deliverables.ts` reads ONLY scan.json (sha256+mtime, null→—) → 16 deliverables. Verdict `STRUCTURAL_COMPLETE_ADOPTION_PENDING` (**12 domains SUPPORTED·0 PARTIAL·0 DEAD_END·0 MISSING; all 10 axes coverage 100; OPERATIONAL_GAPS=[] — 0 open gaps of any severity**; 7 gaps CLOSED w/ real mechanisms → 13 RESOLVED). **Engineering closure ⟂ Adoption ⟂ Confidence:** mechanisms code-complete but real operational volume honest-low/0 in dev + DR restore-drill infra-owned → Adoption + operability_confidence stay **null/WITHHELD**, reported SEPARATELY (Built≠Operated≠Recoverable). Coverage⟂Confidence⟂Adoption never composited; null≠0; never fabricate. STOP for approval (flag stays OFF, no merge/enable/deploy). → `docs/OPERATIONAL_READINESS.md` · `.agents/memory/operational-readiness.md`.
- **Frameworks** `admin/FrameworkPanel.tsx` · `framework-parity.ts`/`sdi.ts` · `lbi_*`/`sdi_*`/`competency_*`.
- **Assessment Modules** `AssessmentModulesManagement.tsx` · `/api/lbi/admin/custom-modules` · `custom_assessment_modules`.
- **Subscription Packages** `/api/admin/subscription-packages` (CRUD + seed/export/import/stats).
- **Concern Areas & Short Assessments** `superadmin/{ConcernAreasPanel,ShortAssessmentsPanel,ActiveAgeBandsReflection}.tsx` · `/api/concerns/*`, `/api/short-assessments/*`.
- **Competency Question Curation** `routes/competency-questions.ts` + `CompetencyQuestionsPanel.tsx` · `competency_question_templates`. Authenticated `GET /api/competency/questions/select` (requireAuth — callers are logged-in Career Builder users; unauthenticated callers get 401 and the UI falls back to the static `ADAPTIVE_QUESTION_BANK_V2`); manual POST always `status='draft'`.
- **Question Factory** (MX-101X, flag `questionFactory`, OFF byte-identical) `routes/question-factory.ts`(`registerQuestionFactoryRoutes`) + `services/question-factory.ts` + `superadmin/QuestionFactoryPanel.tsx` (nav `question-factory`, probe `/api/admin/question-factory/feature-flag` res.ok). Generates DRAFT-only packs grounded in `onto_competencies` genome with provenance/confidence/quality_review_status; human approval is the ONLY coverage-changing op; coverage dashboard separates honest live (`active AND status='approved'`) from draft pipeline; retire archives (never deletes); AI path inert without `OPENAI_API_KEY`. ⚠️ `competency_question_templates.status` is locked to {draft,approved} by two conflicting CHECK constraints → full lifecycle lives in `quality_review_status`+map.active, NOT `status`. → `.agents/memory/question-factory.md`.
  - **MX-101A Coverage Population** (additive on MX-101X, same flag) `services/question-factory-population.ts` + `routes/question-factory.ts` `/population/*` (7 GET + POST `/run`) + Founder/Population/Quality tabs in `QuestionFactoryPanel.tsx`; scripts `mx101a-population-run.ts`/`mx101a-reports.ts` → `backend/audit/mx-101a/*.md` (founder report = deliverable). Drove the FULL 419-comp genome to 100% DRAFT coverage (~2,514 drafts) with ZERO inflation — THREE separate axes Draft/Approved/Assessment-Ready, human approval still the only coverage-changing op. Role-DNA denom=21 distinct comps. → `.agents/memory/question-factory.md` (MX-101A section).
- **Platform Lifecycle Foundation** (MX-700 Phase 1.37, flag `platformLifecycleFoundation`/`FF_PLATFORM_LIFECYCLE_FOUNDATION`, OFF byte-identical incl. schema) `routes/platform-lifecycle.ts` (BASE `/api/admin/platform-lifecycle`) + `services/platform-lifecycle.ts` + migration `20261216_platform_lifecycle_foundation.sql` (5 `platform_*` tables: catalog/ownership/registry/state_history append-only/relationships) + `superadmin/PlatformLifecyclePanel.tsx` (nav `platform-lifecycle`, probe `/api/admin/platform-lifecycle/feature-flag` res.ok). Implements the formal Capability Catalog + Lifecycle Registry MISSING per Phase 1.36 — COMPOSES the existing `FEATURE_FLAGS` registry + repository fs scan (NO parallel registry, NO business-logic change). `POST /discover` populates from the live repo (171 flags/304 routes/417 services/219 migrations/26 docs); **`lifecycle_state` is MANAGED (human `transitionState`, append-only history) ⟂ `activation_state` is DERIVED from live flag runtime — re-discovery refreshes activation but NEVER clobbers a managed lifecycle state**; ownership/doc/dependency gaps reported honest-NULL (never fabricated); flag-gate 503 before DDL so OFF creates no tables. Deliverable `backend/audit/mx-700/phase-1.37-platform-lifecycle-foundation-implementation.md`.
  - **Platform Lifecycle Management Engine** (MX-700 Phase 1.38, flag `platformLifecycleManagement`/`FF_PLATFORM_LIFECYCLE_MANAGEMENT`, OFF byte-identical incl. schema) `routes/platform-lifecycle-management.ts` (BASE `/api/admin/platform-lifecycle-management`) + `services/platform-lifecycle-management.ts` + migration `20261217_platform_lifecycle_management.sql` (4 additive `platform_lifecycle_{deprecation,retirement,version_ledger,evolution}` tables, soft `lifecycle_uid` ref). Management tier that **COMPOSES** the 1.37 Foundation registry + `transitionState()` (NO parallel registry/state machine). Typed lifecycle views (feature/capability/module/api/model) honestly mapped onto the REAL discovered entity_types — `feature`→capability(derived), `api`→module(derived, per-endpoint registry NOT discovered), `model`→registration-only (registry 0 until explicit); version ledger append-only + updates registry authoritative `current_version`; deprecate/retire write metadata THEN compose `transitionState`; **retirement blocked by MEASURED incoming relationship dependencies (409) unless force=true (records snapshot)**; ensure-schema only on flag-ON writes. No frontend panel (STOP clause). Deliverable `backend/audit/mx-700/phase-1.38-platform-lifecycle-management-implementation.md`. → `.agents/memory/platform-lifecycle-management.md`.
  - **Platform Lifecycle Intelligence Engine** (MX-700 Phase 1.39, flag `platformLifecycleIntelligence`/`FF_PLATFORM_LIFECYCLE_INTELLIGENCE`, OFF byte-identical incl. schema) `routes/platform-lifecycle-intelligence.ts` (BASE `/api/admin/platform-lifecycle-intelligence`) + `services/platform-lifecycle-intelligence.ts` + migration `20261218_platform_lifecycle_intelligence.sql` (ONE append-only table `platform_lifecycle_intelligence_snapshots`). READ-ONLY intelligence tier that **COMPOSES** the 1.37 Foundation (`getRepositoryHealth`/`getValidation`/`getSummary`) + 1.38 Management (`getManagementSummary`) — NO parallel registry/engine, NO business-logic change. 9 engines: Evidence (`/evidence` — 7 sources, coverage⟂confidence axes, git best-effort+honest-unavailable), Confidence (`/confidence` — verifiability ratios independent from coverage), Explainability (`/explain/:uid` — why/evidence/impact/deps/compat/migration/alternatives; unknown→found:false), Lifecycle Health (`/health`), Repository Health (`/repository-health` — composes Foundation + large-files/orphan-modules/circular-deps/doc-coverage), Compatibility (`/compatibility`), Validation (`/validation` — composes Foundation + metadata), Audit/drift (`/audit/drift`+`/audit/snapshots`+POST `/audit/capture`=ONLY write path, owns lazy ensure-schema), Metrics (`/metrics` — 6 SEPARATE measured scores, **deliberately NO composited "overall"**). `/summary` composes all; `/enabled`+`/feature-flag` probes. null≠0; Coverage⟂Confidence⟂Evidence never blended; dormant caps flagged built-but-OFF NOT debt; flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). No frontend (STOP clause). Deliverable `backend/audit/mx-700/phase-1.39-platform-lifecycle-intelligence-implementation.md`. → `.agents/memory/platform-lifecycle-intelligence.md`.
  - **Platform Evolution & Technical Debt Intelligence Engine** (MX-700 Phase 1.40, flag `platformEvolutionIntelligence`/`FF_PLATFORM_EVOLUTION_INTELLIGENCE`, OFF byte-identical incl. schema) `routes/platform-evolution-intelligence.ts` (BASE `/api/admin/platform-evolution-intelligence`) + `services/platform-evolution-intelligence.ts` + migration `20261219_platform_evolution_intelligence.sql` (THREE NEW tables `platform_evolution_{technical_debt,knowledge,audit_snapshots}`). ENHANCEMENT tier that **COMPOSES** 1.37 Foundation + 1.38 Management + 1.39 Intelligence — Version/Deprecation/Retirement/Evolution intelligence READ the 1.38 ledgers (`getVersionHistory`/`getDeprecation`/`getRetirement`/`getEvolution`), Validation + architecture-stability READ 1.39 (`getLifecycleValidation`/`getLifecycleMetrics`); NO duplicate registry/engine, NO business-logic change. Engines: Technical Debt (`/technical-debt` curated registry + `/technical-debt/markers` MEASURED TODO/FIXME/HACK/XXX scan — markers≠tracked-debt≠bugs), Version (`/version`), Deprecation (`/deprecation`), Retirement (`/retirement`), Knowledge Preservation (`/knowledge` registry + MEASURED .agents/memory + docs index), Evolution (`/evolution`), Validation (`/validation`), Metrics (`/metrics` — 6 SEPARATE measured scores technical_debt_health/version_health/repository_evolution/knowledge_health/migration_health/architecture_stability, **deliberately NO composite/overall**), Continuous Audit (`/audit/drift`+`/audit/snapshots`+POST `/audit/capture`). Writes (POST debt/`:uid/status`/knowledge/audit-capture) each own lazy ensure-schema → OFF creates 0 tables; reads GET-never-writes (to_regclass probe→ready:false). null≠0; dormant caps NOT debt; Deprecated≠Removed/Retired≠Deleted/Version≠Release/Knowledge-Exists≠Runtime-Active; flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). No frontend (STOP clause). Validate `scripts/mx700-1.40-validate.ts`. Deliverable `backend/audit/mx-700/phase-1.40-platform-evolution-intelligence-implementation.md`. → `.agents/memory/platform-evolution-intelligence.md`.
  - **Platform Lifecycle Automation & Continuous Governance Engine** (MX-700 Phase 1.41, flag `platformLifecycleAutomation`/`FF_PLATFORM_LIFECYCLE_AUTOMATION`, OFF byte-identical incl. schema) `routes/platform-lifecycle-automation.ts` (BASE `/api/admin/platform-lifecycle-automation`) + `services/platform-lifecycle-automation.ts` + migration `20261220_platform_lifecycle_automation.sql` (TWO NEW tables `platform_governance_{policies,audit_snapshots}`). ENHANCEMENT tier that **COMPOSES** 1.37 Foundation + 1.38 Management + 1.39 Intelligence + 1.40 Evolution — every validation number READ from a prior tier's measured getter; NO duplicate registry/engine, NO business-logic change, no dormant activation. 9 engines: Lifecycle Automation (`/automation` read-only continuous checks, Automation≠Activation), Continuous Governance (`/governance` MEASURED areas, Governance≠BusinessLogic), Policy Engine (`/policies` GET + POST register + POST `/policies/:uid/status`; 13 BUILT-IN policies live in CODE + curated custom registry; custom eval injection-safe via exact-match SAFE_FIELDS whitelist before any interpolation + reserved `builtin.*` prefix), Compliance Engine (`/compliance` MEASURED on-demand vs LIVE registry, results NOT persisted — Policy-Exists≠Compliant, Compliance≠RuntimeUsage; per-domain ratios SEPARATE), Orchestration (`/orchestration` read-only coordination of the 4 tiers, no re-run), Continuous Validation (`/continuous-validation` composes 1.39+1.40, Validation≠Modification), Automated Quality Gates (`/quality-gates` 7 gates vs thresholds, Gate-Pass≠Production-Ready, `regression_risk`=STRUCTURAL not runtime-measured, markers≠debt), Continuous Audit (`/audit`+`/audit/drift`+`/audit/snapshots`+POST `/audit/capture`=ONLY write path), Automation Metrics (`/metrics` — 6 SEPARATE measured scores automation_health/compliance_health/governance_health/validation_success/repository_stability/lifecycle_stability, **deliberately NO composite/overall**; stability scores REUSE 1.39). `/summary` composes all; `/enabled`+`/feature-flag` probes. Writes (POST policies/`:uid/status`/audit-capture) each own lazy ensure-schema → OFF creates 0 tables; reads to_regclass-probe→ready:false. null≠0; flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). No frontend (STOP clause). Validate `scripts/mx700-1.41-validate.ts`. Deliverable `backend/audit/mx-700/phase-1.41-platform-lifecycle-automation-implementation.md`. → `.agents/memory/platform-lifecycle-automation.md`.
  - **Platform Lifecycle Operations & SuperAdmin Governance Platform** (MX-700 Phase 1.42, flag `platformLifecycleOperations`/`FF_PLATFORM_LIFECYCLE_OPERATIONS`, OFF byte-identical incl. schema — zero new persistence) FRONTEND EXPOSURE phase: ONE read-only SuperAdmin console `superadmin/PlatformLifecycleOperationsPanel.tsx` (8 sub-tabs Lifecycle/Capability/Repository/Technical Debt/Version/Governance/Observability/Administration) that **COMPOSES** the already-shipped read APIs of 1.37 Foundation + 1.39 Intelligence + 1.40 Evolution + 1.41 Automation **CLIENT-SIDE**. Backend surface is probe-only `routes/platform-lifecycle-operations.ts` (`registerPlatformLifecycleOperationsRoutes(app, requireAuth, requireSuperAdmin)`, BASE `/api/admin/platform-lifecycle-operations`): `/enabled` + gate→auth→superadmin `/feature-flag`. **NO new data endpoints, NO service, NO migration, NO business-logic change, NO dormant activation.** Each section independently probes its OWN engine flag and shows an honest "engine OFF" notice (console flag only says the CONSOLE is exposed — Dashboard≠Runtime, Visible≠Operational, Built≠Activated); null→"—" (null≠0). Nav wiring in `SuperAdminDashboard.tsx` is DOUBLE (extraTabs node + top-level `activeTab` block), tab hidden OFF. Global `/api/admin` gate → OFF smoke (both probes) ∈{401,403,503}. No new persistence. Deliverable `backend/audit/mx-700/phase-1.42-platform-lifecycle-operations-implementation.md`. → `.agents/memory/platform-lifecycle-operations.md`.
  - **Platform Lifecycle Intelligence Production Certification & Enterprise Integration** (MX-700 Phase 1.43, FINAL program phase, flag `platformLifecycleCertification`/`FF_PLATFORM_LIFECYCLE_CERTIFICATION`, OFF byte-identical — zero new persistence) ENHANCEMENT-ONLY read-only certification composer that integrates + validates + certifies the existing 1.37–1.42 tiers and emits MEASURED reports. `routes/platform-lifecycle-certification.ts` (`registerPlatformLifecycleCertificationRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/platform-lifecycle-certification`): `/enabled`, gate→auth→superadmin `/feature-flag`, `/certification`, `/summary`. Service `services/platform-lifecycle-certification.ts` (`composeCertification`/`composeCertificationSummary`) **COMPOSES** the prior tiers' read-only getters — each gathered **EXACTLY ONCE in parallel** then reused across all 10 parts (per-part re-calls → >90s timeout + drift). **NO new engine/V2/parallel/duplicate service, NO business-logic change, NO dormant activation, NO migration.** 10 measured parts (integration/hardening/enterprise-cert/compatibility/repository/performance/security/quality/production-readiness/final-cert); stability scores SEPARATE (no composite); perf HONEST (only composition latency measured — throughput/p95 null, no load tooling); FOUR axes Structural⟂Integration⟂Validation⟂Production-Confidence NEVER composited; **Production-Ready WITHHELD by design** (production_confidence=null, no runtime+outcome evidence); verdict STRUCTURAL_CERTIFIED; null≠0; 1.42 has no service/getter/migration by design (encoded null, not a failure). Script `scripts/mx700-1.43-certification.ts` → `backend/audit/mx-700/phase-1.43-{certification.json,founder-certification.md}`. Global `/api/admin` gate → OFF smoke ∈{401,403,503}. No frontend (STOP clause). Deliverable `backend/audit/mx-700/phase-1.43-platform-lifecycle-certification-implementation.md`. → `.agents/memory/platform-lifecycle-certification.md`.
- **Platform Intelligence Operating System Constitution** (MX-800 Phase 2.1, ENHANCEMENT-ONLY audit+constitution deliverable — NO engine/flag/route/service/migration/persistence, nothing activated; STOP-and-freeze after approval, implementation is a future Phase 2.2). Gives the existing intelligence engines ONE coordination contract; repository stays SSoT. Backed by a pure read-only repo-scan script `backend/scripts/mx800-2.1-platform-intelligence-audit.ts` (no DB/flags/DDL) → deliverables in `backend/audit/mx-800/`: `phase-2.1-platform-intelligence-audit.{json,md}` (MEASURED: 422 services/310 routes/177 flags [32 on/145 off]/223 migrations/268 memory/26 docs) + `phase-2.1-platform-intelligence-constitution.md` (governing doc). 9 constitutional domains (Repository/Platform/Engineering/Runtime/Knowledge/Decision/AI/Analytics/Enterprise Intelligence) mapped via a CURATED file-existence-verified anchor map (NOT a forced partition; all 9 anchored to real engines); 11 `-v2` files flagged as duplication REVIEW CANDIDATES (candidate≠redundant). Honest findings: built≠activated (145 flags OFF), platform is Connected NOT Orchestrated, maturity ceiling = **Managed** (never Self-Optimizing — autonomous unreviewed action is out of scope; human approval authoritative). Honesty ladder: Intelligence-Exists≠Connected≠Orchestrated · Data≠Knowledge≠Intelligence≠Reasoning≠Decision≠Automation · Coverage⟂Confidence⟂Evidence. → `.agents/memory/platform-intelligence-os-constitution.md`.
  - **Platform Intelligence Registry** (MX-800 Phase 2.1 Parts 3–7 implementation, flag `platformIntelligenceRegistry`/`FF_PLATFORM_INTELLIGENCE_REGISTRY`, OFF byte-identical incl. schema) `routes/platform-intelligence-registry.ts` (BASE `/api/admin/platform-intelligence-registry`) + `services/platform-intelligence-registry.ts` + migration `20261221_platform_intelligence_registry.sql` (2 tables `platform_intelligence_registry` + `platform_intelligence_audit_snapshots`). ENHANCEMENT-ONLY foundation that **COMPOSES** the existing intelligence engines into ONE canonical registry (12-entry file-verified catalog / 12 domains, `lifecycle_uid` SOFT-references the MX-700 `platform_lifecycle_catalog` — NO parallel/duplicate registry/engine, NO business-logic change, NO dormant activation, NO dashboards/reasoning/prediction/automation). Part 3 registry+`/discover`+`/register`, Part 4 `/metadata/:uid`, Part 5 orchestration FOUNDATION (`/orchestration`+`/orchestration/route`+`/explain/:uid` — metadata-level routing only, **never executes engines**), Part 6 `/governance` (honest gaps, ownership null), Part 7 `/validation` (**STRUCTURAL** verdict). `lifecycle_state` MANAGED (preserved on re-discover) ⟂ `activation_state` DERIVED from flag; `confidence` STRUCTURAL-only (runtime/outcome confidence = honest null); audit `/audit/capture`(only write)+`/audit/snapshots`+`/audit/drift`. **Flag-gate enforced at BOTH the route (503 before auth/DDL) AND the service write layer** (write fns assert flag before ensure-schema → direct/tooling callers can't create schema OFF). Global `/api/admin` gate → OFF smoke ∈{401,403,503}. Validate `scripts/mx800-2.1-registry-validate.ts` (22/22). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.1-platform-intelligence-os-implementation.md`. → `.agents/memory/platform-intelligence-registry.md`.
- **Engineering Intelligence Engine** (MX-800 Phase 2.3, flag `engineeringIntelligence`/`FF_ENGINEERING_INTELLIGENCE`, OFF byte-identical incl. schema) `routes/engineering-intelligence.ts` (`registerEngineeringIntelligenceRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/engineering-intelligence`) + `services/engineering-intelligence.ts` + migration `20261222_engineering_intelligence.sql` (2 tables `engineering_knowledge_registry` + `engineering_intelligence_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **COMPOSES** the MX-700 1.37–1.40 getters + MEASURED filesystem scans of `backend/services`+`backend/routes`+manifest — NO rebuild/V2/parallel/duplicate engine, NO business-logic change, NO dormant activation, NO dashboards/runtime/AI. 9 parts: (1) Knowledge Registry (file-verified catalog via `POST /discover`, ~736 files/46 libs/782 entries dev; `lifecycle_uid` SOFT-refs MX-700 `platform_lifecycle_catalog`), (2) discover (owner/doc_ref MANAGED preserved on re-discover ⟂ measurement DERIVED), (3) Code Intel (`/code` MEASURED size/large-files/smell-markers; AST complexity/cohesion/duplication/maintainability honest NULL DEFERRED), (4) Dependency Intel (`/dependencies` MEASURED libs + import edges; api deps NULL DEFERRED), (5) Quality Intel (`/quality` MEASURED test-file ratio; line_coverage+maintainability_index honest NULL DEFERRED), (6) Reasoning (`/reasoning`+`/explain/:uid` evidence-grounded, unknown→found:false), (7) Validation (`/validation` STRUCTURAL verdict + no_duplicate_engineering_engine/no_business_logic_change), (8) Metrics (`/metrics` 6 SEPARATE measured scores, **deliberately NO composite/overall**; technical_debt_trend null <2 snapshots), Summary+Audit (`/summary` composes all; `/audit/capture`=ONLY write path, `/audit/drift` null <2). Writes (discover/register/audit-capture) own lazy ensure-schema + `assertEnabled()` before DDL → OFF creates 0 tables; reads to_regclass-probe never DDL. **Perf: capture/summary memoize (8s promise memo) the 3 filesystem scans + 8 composed getters so the deep fan-out gathers each source EXACTLY ONCE (else minutes-long hang + pool exhaustion — MX-700 1.43 lesson); rejections NOT cached; memo only on flag-ON aggregate paths.** null≠0; flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). Validate `scripts/mx800-2.3-engineering-validate.ts` (32/32). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.3-engineering-intelligence-implementation.md`. → `.agents/memory/engineering-intelligence.md`.
- **Runtime Intelligence Engine** (MX-800 Phase 2.4, flag `runtimeIntelligenceEngine`/`FF_RUNTIME_INTELLIGENCE_ENGINE`, OFF byte-identical incl. schema) `routes/runtime-intelligence.ts` (`registerRuntimeIntelligenceRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/runtime-intelligence`) + `services/runtime-intelligence.ts` + migration `20261223_runtime_intelligence.sql` (2 tables `runtime_component_registry` + `runtime_intelligence_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **COMPOSES** the existing `health-aggregator` runtime checks (ONE additive `computeAllHealthDomains(pool)` export — route untouched) + LIVE process/OS/pg measurements — NO rebuild/V2/parallel/duplicate engine, NO business-logic change, NO dormant activation, NO frontend. ⚠️ flag name is `runtimeIntelligenceEngine` because `runtimeIntelligence{Activation,Pipeline,Consumption}` are pre-existing UNRELATED flags. 10 parts: (1) Runtime Registry (`/registry`+`/registry/:uid`+`POST /discover`+`POST /register`; `lifecycle_uid` SOFT-refs MX-700 `platform_lifecycle_catalog` only when present→honest null; `lifecycle_state` MANAGED ⟂ `activation_state` DERIVED), (2) Application Health (`/application-health` composes `computeAllHealthDomains`, no recompute), (3) Performance (`/performance` MEASURED db round-trip latency + event-loop lag; throughput/p95 honest-NULL — no load tooling, `ResponseTime≠Performance`), (4) Service (`/service` postgres+session-store MEASURED status up|down|**unknown**; MongoDB+upload-service configured-only honest-NULL; **availability denom excludes unmeasurable `unknown` services from BOTH numerator AND denominator — null≠down**), (5) Observability (`/observability` snapshot tables+flag registry MEASURED tri-state present/populated `true|false|null`; tracing NULL), (6) Resource (`/resource` process+OS mem/load/cores+pool+db size MEASURED; cgroup/disk honest-NULL), (7) Reasoning (`/reasoning` read-only findings, never a verdict), (8) Validation (`/validation` **STRUCTURAL** verdict only), (9) Metrics (`/metrics` SIX SEPARATE measured scores application_health/performance_health/resource_health/service_availability/observability_coverage/runtime_stability_trend, **deliberately NO composite/overall**; stability_trend null <2 snapshots), Summary+Audit (`/summary` composes all; `/audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift`+`/audit/snapshots`). **null≠0 enforced in the DB helpers themselves: `scalar()`/`rows()` return NULL on query ERROR (0/`[]` only for genuinely-empty result), `pct()` null when numerator null or denom 0/null (no fake 0%)** — call sites propagate (unreadable→`measurement_error`, not absent). 8s promise-memo over composed health + expensive sources on aggregate paths; writes assert flag THEN ensure-schema → OFF creates 0 tables; reads to_regclass-probe never DDL. flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). Validate `scripts/mx800-2.4-runtime-validate.ts` (46/46). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.4-runtime-intelligence-implementation.md`. → `.agents/memory/runtime-intelligence-engine.md`.
- **Knowledge Intelligence Engine** (MX-800 Phase 2.5, flag `knowledgeIntelligenceEngine`/`FF_KNOWLEDGE_INTELLIGENCE_ENGINE`, OFF byte-identical incl. schema) `routes/knowledge-intelligence.ts` (`registerKnowledgeIntelligenceRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/knowledge-intelligence`) + `services/knowledge-intelligence.ts` + migration `20261224_knowledge_intelligence.sql` (2 tables `knowledge_source_registry` + `knowledge_intelligence_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **CONNECTS** the existing ontology/knowledge assets (`ont_*` taxonomy, `onto_*` genome, `map_*` edges, bare `kg_edges` live graph, `pil_kg_*`/`tig_*`, `sci_*` semantic stores, `cg_*`, `onto_aliases`/`onto_relationships`/hierarchies, `ver_*`/`onto_competency_versions`) + prior-tier registries (platform/engineering/runtime intelligence) into ONE **Enterprise Knowledge Graph COMPUTED ON READ** (`computed_on_read:true`, **`materialized:false`** — a projection, NEVER a second copy) — NO rebuild/V2/parallel/duplicate KG/ontology/registry, NO business-logic change, NO dormant activation, NO frontend. 9 parts: (1) Enterprise KG `/graph` (nodes=entity tables+registries, edges=`map_*`+`kg_edges`+`onto_relationships`+hierarchy, exact COUNT(*), 50-source curated catalog), (2) Semantic `/semantic` (sci/aliases/relationships/hierarchies MEASURED; **contextual-meaning/NLP ABSENT→honest-null**), (3) Ontology `/ontology` (cross-ontology bridge `ont_*`↔`onto_*`, STRUCTURAL integrity, evolution from version stores; **NEVER replaces an ontology**; referential orphan scan honest-null), (4) Reasoning `/reasoning`+`/explain/:uid` (evidence-grounded WHY, evidence=table/FK, unknown→found:false; **NOT prediction/decision** STOP clause), (5) Context `/context` (composes platform/engineering/runtime summaries, tier-reachability /3), (6) Validation `/validation` (**STRUCTURAL only**, verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (7) Metrics `/metrics` (**SIX SEPARATE** knowledge_completeness/relationship_coverage/ontology_health/semantic_consistency/knowledge_confidence/context_quality, **NO composite** `composite:null`; knowledge_confidence STRUCTURAL only), (8) Explainability `/explain/:uid` (source/evidence/structural-confidence/context/deps/alternatives/repo-refs), (9) Registry+discover `/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (catalog of knowledge SOURCES not a 2nd KG; `lifecycle_uid` SOFT-refs MX-700 catalog; owner MANAGED preserved on re-discover ⟂ measurement DERIVED). Summary+Audit (`/summary` composes all; `POST /audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift` not-comparable <2 snapshots + `/audit/snapshots`). **null≠0 in the DB helpers themselves** (`scalar()`/`rows()` NULL on query ERROR, `pct()` null on null-num or 0/null-denom); **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables. **Security: `POST /register` user-supplied `physical_table` is identifier-injection-guarded by `isSafeTableIdentifier()` (strict regex ≤63 chars) BOTH as an explicit reject in register AND defense-in-depth inside `countTable` before any `FROM "${table}"` interpolation (a `to_regclass` probe does NOT sanitize identifier injection).** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). Validate `scripts/mx800-2.5-knowledge-validate.ts` (47/47, incl. reads-never-write COUNT(*) sentinel + injection-rejection tests). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.5-knowledge-intelligence-implementation.md`. → `.agents/memory/knowledge-intelligence-engine.md`.
- **Decision Intelligence Engine** (MX-800 Phase 2.6, flag `decisionIntelligenceEngine`/`FF_DECISION_INTELLIGENCE_ENGINE`, OFF byte-identical incl. schema) `routes/decision-intelligence.ts` (`registerDecisionIntelligenceRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/decision-intelligence`) + `services/decision-intelligence.ts` + migration `20261225_decision_intelligence.sql` (2 tables `decision_registry` + `decision_intelligence_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **CATALOGS** the platform's EXISTING decision capabilities (11-entry curated catalog, all engine files + 9 decision tables `wc7b_decision_state`/`ai_decision_audits`/`executive_decision_models`/`interview_decisions`/`role_resolution_decisions`/`wc3_personalization_decisions`/`archetype_governance_decisions`/`m4_ai_decision_logs`/`m5_executive_decision_audits` DB-verified) and **COMPOSES** prior tiers 2.1/2.3/2.4/2.5 (`getPlatformSummary`/`getEngineeringSummary`/`getRuntimeSummary`/`getKnowledgeSummary`) into **EXPLAINABLE DECISION SUPPORT** — **this tier SUGGESTS support, never DECIDES; human approval mandatory.** NO rebuild/V2/parallel/duplicate decision/rule/recommendation engine, NO business-logic change, **NO dormant activation — the dormant decision engines (decisionOrchestrator/decisionPersistence/decisionMentorBridge) are composed by file EXISTENCE / persisted OUTPUT read-only, NEVER INVOKED**, NO frontend. 9 parts: (1) Decision Registry/Catalog `/catalog`+`/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (`present` DERIVED table-OR-engine, `table_count` exact COUNT(*), `flag_state` Built≠Activated; `lifecycle_uid` SOFT-refs MX-700 catalog + `intelligence_uid` SOFT-refs MX-800 2.1 registry incl `intel.decision`; owner MANAGED preserved on re-discover ⟂ measurement DERIVED), (2) Reasoning `/reasoning` (evidence-grounded WHY a type is supported; **NOT prediction/decision/recommendation** STOP clause; every facet asserts Approval≠Automation), (3) Evidence Engine `/evidence` (COUNT-only over decision tables + composes 4 tier summaries, reachability /4; AI evidence audit-trail only=auditability-not-autonomy), (4) Confidence Engine `/confidence` (**SIX SEPARATE** axes evidence_quality/repository/runtime/engineering/knowledge/decision, **STRUCTURAL only**, `composite:null`; **accuracy_confidence honest-null** no labeled outcomes; evidence⟂coverage⟂confidence), (5) Governance `/governance` (human-approval `mandatory:true` + `automated_approval_supported:false`, ownership honest unknown≠0, STRUCTURAL policy validation; **Approval≠Automation**), (6) Explainability `/explain/:uid` (why/evidence/structural-confidence/alternatives/deps/repo+knowledge+runtime refs, governance `human_approval:mandatory`; **confidence basis DERIVED from MEASURED substrate presence**, unknown→found:false), (7) Validation `/validation` (**STRUCTURAL only**: repository/rule/evidence/decision/knowledge integrity; verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (8) Metrics `/metrics` (**SIX SEPARATE** decision_quality/decision_confidence/decision_coverage/recommendation_quality/governance_compliance/explainability_score, **NO composite** `composite:null`; **`recommendation_quality` honest-null** accuracy unmeasurable), Summary+Audit (`/summary` composes all; `POST /audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift` not-comparable <2 + `/audit/snapshots`). **null≠0 in the DB helpers** (scalar/rows NULL on query ERROR, pct null on null-num/0-denom); **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables. **Security: `POST /register` `physical_table` identifier-injection-guarded by `isSafeTableIdentifier()` before any interpolation.** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). Validate `scripts/mx800-2.6-decision-validate.ts` (54/54, incl. sentinel COUNT(*) unchanged on all 9 decision tables proving reads-never-write + engines-never-invoked + injection-rejection). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.6-decision-intelligence-implementation.md`. → `.agents/memory/decision-intelligence-engine.md`.
- **Predictive Intelligence Engine** (MX-800 Phase 2.7, flag `predictiveIntelligenceEngine`/`FF_PREDICTIVE_INTELLIGENCE_ENGINE`, OFF byte-identical incl. schema) `routes/predictive-intelligence-engine.ts` (`registerPredictiveIntelligenceEngineRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/predictive-intelligence`) + `services/predictive-intelligence-engine.ts` + migration `20261226_predictive_intelligence.sql` (2 tables `prediction_registry` + `predictive_intelligence_audit_snapshots`). ⚠️ **name-collision: a PRE-EXISTING tracked `routes/predictive-intelligence.ts` (per-user prediction, BASE `/api/predictions`+`/api/admin/predictions`, export `registerPredictiveIntelligenceRoutes`) was accidentally overwritten then RESTORED from git — this phase uses `*-engine` filenames + renamed export + non-colliding BASE; both register side-by-side, legacy intact.** ENHANCEMENT-ONLY read-only composer that **CATALOGS** the platform's EXISTING prediction capabilities (13-entry curated catalog, all engine files + 13 prediction tables `competency_forecasts`/`frp_user_readiness`/`lbi_behavior_trends`/`lbi_learning_trends`/`lbi_risk_indicators`/`m4_skill_decay_forecasts`/`m4_organizational_capability_risks`/`wos_workforce_risk`/`readiness_predictions`/`roie_forecasts`/`m4_simulation_scenarios`/`m4_simulation_forecasts`/`platform_evolution_technical_debt` DB-verified) and **COMPOSES** prior tiers 2.1/2.3/2.4/2.5/2.6 into **EXPLAINABLE PREDICTIVE SUPPORT** — **this tier surfaces EXISTING predictions, never GENERATES a new forecast/risk/simulation and never DECIDES; human approval mandatory.** NO rebuild/V2/parallel/duplicate prediction/forecast/analytics engine, NO business-logic change, **NO dormant activation — the dormant prediction engines are composed by file EXISTENCE / persisted OUTPUT read-only, NEVER INVOKED**, NO frontend. 9 parts: (1) Prediction Registry/Catalog `/catalog`+`/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (`present` DERIVED table-OR-engine, `table_count` exact COUNT(*), `flag_state` Built≠Activated; `lifecycle_uid` SOFT-refs MX-700 catalog + `intelligence_uid` SOFT-refs MX-800 2.1 registry; owner MANAGED preserved on re-discover ⟂ measurement DERIVED), (2) Trend Intelligence `/trend` (surfaces EXISTING trends, **Trend≠Future**, composes 5 prior tiers), (3) Risk Prediction `/risk` (**Probability≠Certainty**, COUNT-only risks_recorded), (4) Impact Simulation `/simulation` (**SIMULATION ONLY** `modifies_production:false`+`write_paths_to_business_tables:0`, **Simulation≠Reality**), (5) Scenario Intelligence `/scenario` (7 framings over EXISTING substrate, never asserts an outcome), (6) Explainability `/explain/:uid` (why/evidence/structural-confidence/alternatives/repo+knowledge+runtime refs, governance `human_approval:mandatory`, unknown→found:false), (7) Validation `/validation` (**STRUCTURAL only**: repository/model/evidence/prediction/trail/forecast-consistency/registry; `forecast_consistency` STRUCTURAL self-consistency **NOT accuracy**; verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (8) Metrics `/metrics` (**FIVE SEPARATE** forecast_confidence/prediction_quality/trend_accuracy/risk_prediction_coverage/explainability_score, **NO composite** `composite:null`; **`forecast_confidence` STRUCTURAL only**, **`trend_accuracy` honest-null** accuracy unmeasurable no labeled outcomes), Summary+Audit (`/summary` composes all; `POST /audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift` not-comparable <2 + `/audit/snapshots`). **null≠0 in the DB helpers** (scalar/rows NULL on query ERROR, pct null on null-num/0-denom); **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables. **Security: `POST /register` `physical_table` identifier-injection-guarded by `isSafeTableIdentifier()` before any interpolation.** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). Validate `scripts/mx800-2.7-predictive-validate.ts` (56/56, incl. sentinel COUNT(*) unchanged on all 13 prediction tables proving reads-never-write + engines-never-invoked + injection-rejection). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.7-predictive-intelligence-implementation.md`. → `.agents/memory/predictive-intelligence-engine.md`.
  - **Recommendation Intelligence Engine** (MX-800 Phase 2.8, flag `recommendationIntelligenceEngine`/`FF_RECOMMENDATION_INTELLIGENCE_ENGINE`, OFF byte-identical incl. schema) `routes/recommendation-intelligence-engine.ts` (`registerRecommendationIntelligenceEngineRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/recommendation-intelligence`) + `services/recommendation-intelligence-engine.ts` + migration `20261227_recommendation_intelligence.sql` (2 tables `recommendation_registry` + `recommendation_intelligence_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **CATALOGS** the platform's EXISTING recommendation capabilities (**20-entry curated catalog** across **16 domains** / **4 kinds** recommendation/opportunity/intervention/optimization — DB-tables + engine files verified: `capadex_recommendations`/`career_recommendations`/`frp_recommendations`/`lbi_user_recommendations`/`mei_user_recommendations`/`rie_recommendations`/`cg_user_recommendations`/`m5_executive_recommendations`/`development_recommendations`/`learning_recommendations`/`rie_opportunity_flags`/`paie_opportunity_forecasts`/`roie_opportunities`/`iil_opportunities`/`nhda_opportunities`/`capadex_interventions`/`pil_intervention_library` + compute-on-read causal/EI/runtime-optimization/cg [table honest-null]; ⚠️ `paie`/`roie` opportunity logic lives in `routes/` NOT `services/`) and **COMPOSES** prior tiers 2.1/2.3/2.4/2.5/2.6/2.7 into **EXPLAINABLE RECOMMENDATION SUPPORT** — **this tier surfaces EXISTING recommendations, never GENERATES a new recommendation/opportunity and never DECIDES/EXECUTES/AUTOMATES; human approval mandatory.** NO rebuild/V2/parallel/duplicate recommendation/prescriptive/optimization engine, NO business-logic change, **NO dormant activation — the dormant recommendation engines are composed by file EXISTENCE / persisted OUTPUT read-only, NEVER INVOKED**, NO frontend. 9 parts: (1) Recommendation Registry/Catalog `/catalog`+`/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (`present` DERIVED table-OR-engine, `table_count` exact COUNT(*), `flag_state` Built≠Activated; owner MANAGED preserved on re-discover), (2) Action Intelligence `/action` (**Recommendation≠Execution**, `execution_safety` executes/automates false + 0 business writes, composes 6 prior tiers), (3) Opportunity Intelligence `/opportunity` (**Opportunity≠Requirement**, COUNT-only opportunities_recorded), (4) Prioritization Engine `/prioritization` (**Priority≠Approval**, `basis:structural_substrate_volume`, `decision_safety` asserts_priority/approves/decides all false), (5) Prescriptive Intelligence `/prescriptive` (**RECOMMEND ONLY** executes/automates/decides/modifies_production false + 0 business writes, **Recommendation≠Execution**), (6) Explainability `/explain/:uid` (why/evidence/structural-confidence/alternatives/repo+knowledge+runtime refs, governance `human_approval:mandatory`+executes/decides false, unknown→found:false), (7) Validation `/validation` (**STRUCTURAL only**: repository/engine/evidence/recommendation/trail/recommendation-consistency/registry; `recommendation_consistency` STRUCTURAL self-consistency **NOT acceptance/effectiveness**; verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (8) Metrics `/metrics` (**SIX SEPARATE** recommendation_quality/recommendation_confidence/recommendation_coverage/explainability_score/acceptance_rate/effectiveness, **NO composite** `composite:null`; **`recommendation_confidence` STRUCTURAL only**, **`acceptance_rate` + `effectiveness` honest-null** — no adoption telemetry, no labeled outcomes), Summary+Audit (`/summary` composes all `of:6`; `POST /audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift` not-comparable <2 + `/audit/snapshots`). **null≠0 in the DB helpers**; **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables. **Security: `POST /register` `physical_table` identifier-injection-guarded by `isSafeTableIdentifier()` before any interpolation.** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). Validate `scripts/mx800-2.8-recommendation-validate.ts` (62/62, incl. sentinel COUNT(*) unchanged on all 17 recommendation tables proving reads-never-write + engines-never-invoked + injection-rejection). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.8-recommendation-intelligence-implementation.md`. → `.agents/memory/recommendation-intelligence-engine.md`.
  - **Continuous Learning Intelligence Engine** (MX-800 Phase 2.9, flag `continuousLearningIntelligenceEngine`/`FF_CONTINUOUS_LEARNING_INTELLIGENCE_ENGINE`, OFF byte-identical incl. schema) `routes/continuous-learning-intelligence-engine.ts` (`registerContinuousLearningIntelligenceEngineRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/continuous-learning-intelligence`) + `services/continuous-learning-intelligence-engine.ts` + migration `20261228_continuous_learning_intelligence.sql` (2 tables `learning_registry` + `continuous_learning_intelligence_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **CATALOGS** the platform's EXISTING learning capabilities (**23-entry curated catalog** across **12 domains** / **6 kinds** learning(7)/feedback(2)/experience(7)/adaptive(3)/improvement(2)/organizational(2) — DB-tables + engine files verified: `learn_outcomes`/`cg_user_learning_recs`/`lip_learning_paths`/`wos_learning_roi`/`lip_learning_needs`/`meta_learning_profiles`/`learning_recommendations`/`interview_feedback`/`learn_intervention_events`/`cp_experience`/`episodic_memory`/`behavioural_memory`/`competency_memory_history`/`wcl5_memory`/`adaptive_intelligence_events`/`adaptive_runtime_state`/`irt_adaptive_config`/`platform_evolution_audit_snapshots`/`iil_self_evolution_log`/`platform_evolution_knowledge`/`m3_ontology_evolution_events` + compute-on-read prediction/longitudinal-memory [table honest-null]; ⚠️ `iil_self_evolution_log` engine lives in `routes/` NOT `services/`) and **COMPOSES** prior tiers 2.1/2.3/2.4/2.5/2.6/2.7/2.8 into **EXPLAINABLE LEARNING SUPPORT** — **this tier surfaces EXISTING learning, never GENERATES learning and never ADAPTS/DECIDES/EXECUTES/modifies business logic; human approval mandatory.** NO rebuild/V2/parallel/duplicate learning/adaptive/improvement engine, NO business-logic change, **NO dormant activation — the dormant learning/adaptive engines are composed by file EXISTENCE / persisted OUTPUT read-only, NEVER INVOKED**, NO frontend. 9 parts: (1) Learning Registry/Catalog `/catalog`+`/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (`present` DERIVED table-OR-engine, `table_count` exact COUNT(*), `flag_state` Built≠Activated; owner MANAGED preserved on re-discover), (2) Feedback Intelligence `/feedback` (**Feedback≠Truth**, `feedback_safety` treats_feedback_as_truth/generates/decides false + 0 business writes, composes 7 tiers), (3) Experience Intelligence `/experience` (**Experience≠Knowledge**, COUNT-only experience_recorded), (4) Adaptive Intelligence `/adaptive` (**Adaptation≠Autonomous Change**, `adaptation_safety` modifies_business_logic/autonomous_change/executes/decides false + 0 business writes, flag_state Built≠Activated), (5) Continuous Improvement `/continuous-improvement` (**Improvement≠Optimization**, `evidence_basis:verified_existing_substrate`, `improvement_safety` optimizes/modifies_business_logic/autonomous/executes/decides false), (6) Explainability `/explain/:uid` (why/evidence/structural-confidence/**previous_state+current_state+reason_for_change honest "NO CHANGE"**/alternatives/repo+knowledge+runtime refs, governance `human_approval:mandatory`+executes/decides/autonomous_learning false, unknown→found:false), (7) Validation `/validation` (**STRUCTURAL only**: repository/engine/evidence/learning/trail/learning-consistency/registry; `learning_consistency` STRUCTURAL self-consistency **NOT effectiveness**; verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (8) Metrics `/metrics` (**SIX SEPARATE** learning_quality/learning_confidence/learning_coverage/explainability_score/improvement_rate/effectiveness, **NO composite** `composite:null`; **`learning_confidence` STRUCTURAL only**, **`improvement_rate` + `effectiveness` honest-null** — no longitudinal labelled improvement deltas, no labelled outcomes), (9) Organizational Learning `/organizational` (**Experience≠Knowledge**, MEASURED preservation: `.agents/memory` lessons + `docs` count + `platform_evolution_knowledge`/`m3_ontology_evolution_events`/`iil_self_evolution_log` COUNT(*), `preservation_safety` generates_lessons/decides/modifies_business_logic false). Summary+Audit (`/summary` composes all `of:7`, `learn_safety` executes/decides/adapts/modifies_business_logic/learns_autonomously false + learn_only true; `POST /audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift` not-comparable <2 + `/audit/snapshots`). **null≠0 in the DB helpers**; **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables. **Security: `POST /register` `physical_table` identifier-injection-guarded by `isSafeTableIdentifier()` before any interpolation.** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke GET 401/POST 403 ∈{401,403,503}). Validate `scripts/mx800-2.9-learning-validate.ts` (71/71, incl. sentinel COUNT(*) unchanged on all 21 learning tables proving reads-never-write + engines-never-invoked + injection-rejection). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.9-continuous-learning-intelligence-implementation.md`. → `.agents/memory/continuous-learning-intelligence-engine.md`.
  - **Enterprise Intelligence Platform** (MX-800 Phase 2.10, flag `enterpriseIntelligencePlatform`/`FF_ENTERPRISE_INTELLIGENCE_PLATFORM`, OFF byte-identical incl. schema) `routes/enterprise-intelligence-platform.ts` (`registerEnterpriseIntelligencePlatformRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/enterprise-intelligence`) + `services/enterprise-intelligence-platform.ts` + migration `20261229_enterprise_intelligence_platform.sql` (2 tables `enterprise_intelligence_registry` + `enterprise_intelligence_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **REGISTERS** the platform's EXISTING intelligence domains/services into ONE enterprise registry (**19-entry curated catalog** across **13 domains** / **4 kinds** intelligence(8)/executive(5)/organizational(4)/analytics(2) — the 8 prior MX-800 tiers' snapshot trails+engine files + existing enterprise-analytics/executive/governance/report substrate, all DB+file verified) and **COMPOSES** the eight prior tiers 2.1/2.3/2.4/2.5/2.6/2.7/2.8/2.9 into **EXPLAINABLE ENTERPRISE INTELLIGENCE** — **this tier surfaces EXISTING intelligence, never GENERATES and never DECIDES/EXECUTES/AUTOMATES/modifies business logic; human approval mandatory.** NO rebuild/V2/parallel/duplicate enterprise/analytics/executive engine, NO business-logic change, **NO dormant activation — the 8 prior engines are composed by file EXISTENCE / persisted summary read-only, NEVER INVOKED**, NO frontend. 9 parts: (1) Enterprise Registry/Catalog `/catalog`+`/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (`present` DERIVED table-OR-engine, `table_count` exact COUNT(*), `flag_state` Built≠Activated; owner MANAGED preserved on re-discover), (2) Enterprise Orchestration `/orchestration` (**Connected≠Orchestrated**, metadata-level coordination of 8 tiers, `orchestration_safety` executes_engines/re_runs_tiers/decides false + 0 business writes, `tier_reachability.of===8`), (3) Cross-Intelligence Correlation `/correlation` (**Correlation≠Causation**, 8 channels side-by-side, `co_presence` exact COUNT-or-0 NOT a coefficient, `correlation_safety` asserts/infers causation/decides false), (4) Enterprise Insights `/insights` (**Insight≠Decision**, every insight `is_decision:false`, `dormant_capabilities` listed as observation Built≠Activated, `insights_safety` decision/triggers_action/autonomous/modifies_business_logic false), (5) Organizational Intelligence `/organizational` (surfaces EXISTING org/governance+portfolio substrate, records null≠0, `organizational_safety` decides/modifies_business_logic/autonomous false), (6) Explainability `/explain/:uid` (why/evidence/structural-confidence/**previous_state+current_state+reason_for_change honest "NO CHANGE"**/alternatives/repo+knowledge+runtime refs, governance `human_approval:mandatory`, unknown→found:false), (7) Validation `/validation` (**STRUCTURAL only**: repository/intelligence/evidence/enterprise/knowledge/recommendation/organizational/consistency/registry; `enterprise_consistency` STRUCTURAL self-consistency **NOT effectiveness**; verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (8) Metrics `/metrics` (**SIX SEPARATE** enterprise_health/intelligence_coverage/intelligence_maturity/explainability_score/intelligence_effectiveness/enterprise_optimization, **NO composite** `composite:null`; **`intelligence_maturity` STRUCTURAL only**, **`intelligence_effectiveness` + `enterprise_optimization` honest-null** — no labelled enterprise outcomes, no longitudinal improvement deltas), (9) Executive Intelligence `/executive` (**Dashboard≠Intelligence**, MEASURED KPIs/strategic_indicators/trends from composed tiers, KPIs include an honest-null unmeasurable outcome KPI, `strategic_indicators` assert connected_not_orchestrated, trends not-ready <2 snapshots, `executive_safety` decides/fabricates_kpis/autonomous false). Summary+Audit (`/summary` composes all `of:8`, `enterprise_safety` executes/decides/acts/modifies_business_logic/duplicates false + insight-only; `POST /audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift` not-comparable <2 + `/audit/snapshots`). **null≠0 in the DB helpers**; **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables. **Perf: composition memoizes (`EI_MEMO_TTL_MS`, default 8000ms=prod unchanged, clamped 0–3.6M) the 8-tier fan-out so each prior summary is gathered EXACTLY ONCE; reads never write regardless of cache (knob cannot change OFF behaviour or output correctness); offline validator pins a long TTL.** **Security: `POST /register` `physical_table` identifier-injection-guarded by `isSafeTableIdentifier()` before any interpolation.** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke GET 401/POST 403 ∈{401,403,503}). Validate `scripts/mx800-2.10-enterprise-validate.ts` (72/72, incl. sentinel COUNT(*) unchanged on existing intelligence snapshot tables proving reads-never-write + engines-never-invoked + injection-rejection). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.10-enterprise-intelligence-platform-implementation.md`. → `.agents/memory/enterprise-intelligence-platform.md`.
  - **Platform Intelligence Operations Center** (MX-800 Phase 2.11, flag `platformIntelligenceOperations`/`FF_PLATFORM_INTELLIGENCE_OPERATIONS`, OFF byte-identical incl. schema — zero new persistence) FRONTEND-EXPOSURE phase that mirrors MX-700 Phase 1.42 EXACTLY: ONE read-only SuperAdmin console `superadmin/PlatformIntelligenceOperationsPanel.tsx` (9 sub-tabs Operations/Monitoring/Observability/Governance/Operational-Intel/Alerts/Executive/SuperAdmin-Ops/Validation) that **COMPOSES the already-shipped read APIs of the nine prior MX-800 tiers CLIENT-SIDE** — 2.1 registry(`/api/admin/platform-intelligence-registry`) + 2.3 engineering + 2.4 runtime + 2.5 knowledge + 2.6 decision + 2.7 predictive + 2.8 recommendation + 2.9 continuous-learning + 2.10 enterprise. Backend surface is probe-only `routes/platform-intelligence-operations.ts` (`registerPlatformIntelligenceOperationsRoutes(app, requireAuth, requireSuperAdmin)`, BASE `/api/admin/platform-intelligence-operations`): `/enabled` + gate→auth→superadmin `/feature-flag`. **NO new data endpoints, NO service, NO migration, NO business-logic change, NO dormant activation, NO V2, NO duplicate dashboard.** Each section independently probes its OWN engine `/enabled` and shows an honest "engine OFF" notice (console flag only says the CONSOLE is exposed — Built≠Activated, Visible≠Healthy, Dashboard≠Intelligence, Monitoring≠Governance, Insight≠Decision); null→"—" (null≠0); generic RecursiveKV/DataTable renderers handle each tier's differing shapes without fabricating zeros. **Alerts tab is CLIENT-SIDE DERIVED** from each enabled engine's own `/validation` output (concern-status leaves) — Alert≠Incident, never auto-executes. React hooks iterate the **CONSTANT TIERS array** (stable hook count) gated via `enabled:on[base]` (NEVER iterate a filtered list → hook-count mismatch). Nav wiring in `SuperAdminDashboard.tsx` is DOUBLE (lazy import + feature-flag useQuery res.ok gated on isAuthenticated + extraTabs conditional-spread node + top-level `activeTab` block), tab hidden OFF. Global `/api/admin` gate → OFF smoke (BOTH probes) **401** (401 not 404 = registered; ∈{401,403,503}). No new persistence. Deliverable `backend/audit/mx-800/phase-2.11-platform-intelligence-operations-implementation.md`. → `.agents/memory/platform-intelligence-operations.md`.
  - **Intelligence Automation & Governance Orchestration Platform** (MX-800 Phase 2.12, flag `intelligenceAutomationGovernance`/`FF_INTELLIGENCE_AUTOMATION_GOVERNANCE`, OFF byte-identical incl. schema) `routes/intelligence-automation-governance.ts` (`registerIntelligenceAutomationGovernanceRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/intelligence-automation-governance`) + `services/intelligence-automation-governance.ts` + migration `20261230_intelligence_automation_governance.sql` (2 tables `automation_governance_registry` + `automation_governance_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **COMPOSES** the MX-700 1.41 `platform-lifecycle-automation` getters (governance/policies/quality-gates/compliance/orchestration/continuous-validation/automation/metrics) + the MX-800 2.1–2.11 intelligence tiers into ONE canonical automation-&-governance orchestration **view** — composes by file-existence + persisted-output read-only, **NEVER invokes/schedules/executes/activates any engine, never runs business logic, never executes workflows/approvals.** NO rebuild/V2/parallel automation platform/duplicate workflow engine, NO business-logic change, NO dormant activation, NO frontend. 10 parts: (1) Catalog/Registry `/catalog`+`/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (**18-entry** file-verified catalog, `present` DERIVED table-OR-engine, `table_count` exact COUNT(*), `flag_state` Built≠Activated; `lifecycle_uid` SOFT-refs MX-700 catalog + `intelligence_uid` SOFT-refs MX-800 2.1 registry; owner MANAGED preserved on re-discover ⟂ measurement DERIVED), (2) Governance Orchestration `/governance-orchestration`, (3) Workflow Orchestration `/workflow-orchestration` (**Workflow≠Business-Logic**), (4) Policy Orchestration `/policy-orchestration`, (5) Validation Automation `/validation-automation` (**Validation≠Modification**), (6) Event Orchestration `/event-orchestration` (composes `ADAPTIVE_EVENTS` metadata, never emits), (7) Approval Workflows `/approval-workflows` (**Approval≠Execution**, human-approval mandatory, automated_approval false), (8) Observability `/observability`, (9) Validation `/validation` (**STRUCTURAL only**, verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (10) Metrics `/metrics` (**SIX SEPARATE** automation_health/governance_maturity/automation_coverage/explainability/automation_effectiveness/governance_optimization, **NO composite** `composite:null`; **`automation_effectiveness` + `governance_optimization` honest-null** — no runtime/outcome evidence). Plus Explainability `/explain/:uid` (unknown→found:false), Summary+Audit (`/summary` composes all `substrate of:10` + governed `tiers of:9` incl. 2.11 honest non-getter encoded null; `summary.automation_safety` decides/executes/autonomous all false — **Automation≠Autonomy, Orchestration≠Decision**; `POST /audit/capture`=ONLY write path, `/audit/drift` not-comparable <2 + `/audit/snapshots`). **null≠0 in the DB helpers**; **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables (ensure-schema ALSO asserts the flag internally as defense-in-depth so direct/tooling callers can't create schema OFF). **Security: `POST /register` `physical_table` identifier-injection-guarded by `isSafeTableIdentifier()` (exported for unit test) before any `FROM "${table}"` interpolation.** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke GET 401/POST 403 ∈{401,403,503}). **Validator is PHASED** `scripts/mx800-2.12-automation-validate.ts` (argv comma-list `light,gov,metrics,summary,write,drift`; 35/35 across phases) — each heavy getter independently re-scans the repo (~30–55s, NO shared substrate memo) + `capture` recomputes the summary fresh (~55s, no memo reuse) so an all-in-one run exceeds the tool timeout; the 2 captures `drift` needs are SPLIT (`write` leaves tables in place / `drift` reuses + cleans up). No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.12-intelligence-automation-governance-implementation.md`. → `.agents/memory/intelligence-automation-governance.md`.
  - **Enterprise Intelligence Integration Platform** (MX-800 Phase 2.13, flag `enterpriseIntelligenceIntegration`/`FF_ENTERPRISE_INTELLIGENCE_INTEGRATION`, OFF byte-identical incl. schema) `routes/enterprise-intelligence-integration.ts` (`registerEnterpriseIntelligenceIntegrationRoutes(app, concernsPool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/enterprise-intelligence-integration`) + `services/enterprise-intelligence-integration.ts` + migration `20261231_enterprise_intelligence_integration.sql` (2 tables `enterprise_integration_registry` + `enterprise_integration_audit_snapshots`). ENHANCEMENT-ONLY read-only composer that **INTEGRATES** the EXISTING MX-800 2.1/2.3–2.10 (9 intelligence) + 2.12 (enterprise automation/governance, integrated via its LIGHT catalog getter — NO 9-tier recompute) + MX-700 1.41 (automation) + 1.37 (platform foundation) + 2.11 (operations) + report-factory (reporting) into ONE enterprise integration view — **14-service** file/table-verified catalog. Unlike 2.12 (engines-never-invoked), this tier **DOES call read-only SUMMARY getters** (`composeServices` memoized once per process `eii:composition`, env `EII_MEMO_TTL_MS` default 8000ms; **11** getter-backed + workflow auxiliary; the **3 honest non-getters** 1.37/2.11/report-factory registered by existence `reachable:null`, null≠0) — but **NEVER** invokes an engine's compute/write/scheduler/emit/execute path or subscribes to the bus (`integration_safety.invokes_engine=false`). NO new engine/V2/parallel/duplicate registry, NO business-logic change, NO dormant activation. 10 parts: (1) Enterprise Integration Registry `/catalog`+`/registry`+`/registry/:uid`+`POST /discover`+`POST /register` (present DERIVED table-OR-engine, table_count exact COUNT(*), flag_state Built≠Activated; soft `intelligence_uid`/`lifecycle_uid` links; owner+lifecycle_state MANAGED preserved on re-discover ⟂ measurement DERIVED), (2) Cross-Intelligence Integration `/cross-intelligence` (the 9 governed channels, `reachability.of===9`, `integration_safety` invokes_engine/cross_infers/decides/autonomous false — **Insight≠Decision**), (3) Enterprise Service Composition `/service-composition` (`totals.getter_backed===11`, `composition_safety` reimplements/duplicates false — **Composition≠Duplication**), (4) Platform Interoperability `/interoperability` (**4 MEASURED descriptive contracts** feature_flag/read_summary/audit_trail/registry_linkage, `interoperability_safety.enforces=false` — **Standardized≠Enforced**), (5) Enterprise Coordination `/coordination` (METADATA-level routing one per service, `coordination_safety` executes/decides/approves false — **Connected≠Orchestrated**), (6) Explainability `/explain/:uid` (why/evidence/deps, unknown→found:false, confidence STRUCTURAL only), (7) Validation `/validation` (**STRUCTURAL only**, verdict STRUCTURAL_VALIDATED|PARTIAL|ABSENT), (8) Metrics `/metrics` (**SIX SEPARATE** platform_integration_health/enterprise_service_health/api_health/workflow_health/intelligence_integration_coverage/**enterprise_readiness=null**, **NO composite** `composite:null`; **`enterprise_readiness` honest-null/DEFERRED** — operational readiness needs runtime+outcome evidence; **Integrated≠Unified, Unified≠Operational**; axes Structural⟂Confidence⟂Coverage⟂Outcome never blended; workflow_health=STRUCTURAL reachability 100/50/null NOT throughput), (9) **SuperAdmin console** `superadmin/EnterpriseIntegrationPanel.tsx` (read-only, 6 sub-tabs Integration/Services/Registry/Interoperability/Coordination/Metrics&Workflow; Registry tab keys = raw migration columns), (10) Summary+Audit (`/summary` gather-once composition, `POST /audit/capture`=ONLY write path owns lazy ensure-schema, `/audit/drift` per-metric delta null <2 + `/audit/snapshots`). **null≠0 in DB helpers**; **exact COUNT(*) NEVER n_live_tup**; reads `to_regclass`-probe never DDL, writes `assertEnabled()` THEN ensure-schema → OFF creates 0 tables. **Security: `POST /register` `physical_table` identifier-injection-guarded by `isSafeTableIdentifier()` (exported for the validator) before any `FROM "${table}"` interpolation.** flag-gate 503 before auth/DDL (global `/api/admin` gate → OFF smoke ∈{401,403,503}). FE wiring in `SuperAdminDashboard.tsx` is DOUBLE (lazy import + feature-flag useQuery res.ok gated on isAuthenticated + extraTabs conditional-spread + top-level `activeTab` block), tab hidden OFF. **Validator is PHASED** `scripts/mx800-2.13-integration-validate.ts` (argv comma-list `light,compose,metrics,summary,write,drift`; **42/42** across phases; drops the 2 owned tables start+end, write→drift continuation splits the 2 snapshots). Deliverable `backend/audit/mx-800/phase-2.13-enterprise-intelligence-integration-implementation.md`. → `.agents/memory/enterprise-intelligence-integration.md`.
  - **Enterprise Intelligence Platform Production Certification, Maturity Assessment & Release Baseline** (MX-800 Phase 2.14, **FINAL MX-800 phase**, flag `enterpriseIntelligenceCertification`/`FF_ENTERPRISE_INTELLIGENCE_CERTIFICATION`, OFF byte-identical — zero new persistence) ENHANCEMENT-ONLY read-only certification composer that mirrors MX-700 1.43 EXACTLY: integrates + validates + certifies the existing MX-800 tiers (2.1 + 2.3–2.13; 2.2 is the doc-only constitution, named in the freeze list only) and emits MEASURED reports. `routes/enterprise-intelligence-certification.ts` (`registerEnterpriseIntelligenceCertificationRoutes(app, pool, requireAuth, requireSuperAdmin)`, BASE `/api/admin/enterprise-intelligence-certification`): `/enabled`, gate→auth→superadmin `/feature-flag`, `/certification`, `/summary`. Service `services/enterprise-intelligence-certification.ts` (`composeCertification`/`composeCertificationSummary`) **COMPOSES** the prior tiers' read-only getters — each gathered **EXACTLY ONCE in parallel** (`gather()`+`safe()`) then reused across all 10 parts (per-part re-calls → >timeout + drift). **NO new engine/V2/parallel/duplicate service, NO business-logic change, NO dormant activation, NO migration/persistence, NO frontend.** 10 measured parts (integration/production-readiness/enterprise-cert/compatibility/security/performance/maturity/release-baseline/report-sections/final-cert+DoD); stability scores SEPARATE (no composite); perf HONEST (only `composition_latency_ms` ~49s measured — throughput/p95 null, no load tooling); maturity per-domain Level 0–5 reports the FLOOR (ceiling **Managed/L3**, Levels 4–5 WITHHELD — no runtime-adoption/autonomous-optimization evidence); release baseline = MEASURED fs snapshot + freeze list 2.1–2.14 (`repository_commit=null`, `baseline_frozen` structural not a deploy); FOUR axes Structural⟂Integration⟂Validation⟂Production-Confidence NEVER composited; **Production-Ready WITHHELD by design** (`production_confidence=null`); verdict STRUCTURAL_CERTIFIED; null≠0. ⚠️ **tier-2.7 mapping trap**: 3 predictive route files exist — `predictive-intelligence.ts` (older system) / `predictive-intelligence-engine.ts` (**the real 2.7**, `registerPredictiveIntelligenceEngineRoutes`) / `predictive-intelligence-v2.ts` (V2 of the OLDER system); map tiers from `routes.ts`, NOT the plan table, or the duplicate scan false-flags `-v2`. Script `scripts/mx800-2.14-certification.ts` enables the 12 tier FF_* + cert flag for-process-only (no data seeding) → `backend/audit/mx-800/phase-2.14-{certification.json,founder-certification.md}`. Global `/api/admin` gate → OFF smoke ∈{401,403,503}. No frontend (STOP clause). Deliverable `backend/audit/mx-800/phase-2.14-enterprise-intelligence-certification-implementation.md`. → `.agents/memory/enterprise-intelligence-certification.md`.

### CAPADEX ontology data
- **Concerns Master** (~2,489) `routes/capadex-concerns-master.ts` · `CapadexConcernsMasterPanel.tsx`. `display_label` = user copy; `concern_*`/`domain` = join keys.
- **Clarity Questions** (~30,638) `routes/capadex-clarity-questions.ts` · `CapadexClarityQuestionsPanel.tsx`. Join is `clarity.master_bridge_tag = master.relational_bridge_tag` (bucket-level). ⚠️ `concern_id` is DISJOINT from `concerns_master` (0% join); only working bridge is `master_bridge_tag`; inherited age/persona/dev-stage are AMBIGUOUS. → `.agents/memory/{clarity-bridge-tag-classifier,clarity-xlsx-import-quality}.md`.
- **Signal Ontology Hub** (4-tier: 20 domains · 400 families · 20 signals · 15,972 atomic) `routes/capadex-ontology-hub.ts` · `SignalOntologyHubPanel.tsx`. → `.agents/memory/atomic-bridge-general-concern.md`.

### CAPADEX Assessment Flow
- `FreeAssessmentModal.tsx`: intro → analyze → clarify → preview → questions → result → register → OTP → report. Routes `routes/capadex.ts`, `routes/capadex-concern-intelligence.ts`. Tables `capadex_sessions/responses/users/otps/reports/runtime_sessions`.
- **3-tier clarity picker** (`pickQuestionsFromMaster` → `pickQuestionsFromDB` → static) carries `clarity_source` provenance. `resolveCapadexConcern()` keyword fallback → never 404s.
- **Preview ↔ report share ONE visual canon** (`CapadexBridgePhase.tsx` + `CapadexReportPhase.tsx`); tone hopeful/light, header/CTA deep enough for white text.
- Detail → `docs/CAPADEX.md` §18/§20 · `.agents/memory/{capadex-concern-routing-fallback,capadex-clarity-picker-filters,proxy-language-engine,bridge-tag-orphan-remap-leakage,capadex-report-tone-palette}.md`.

### CAPADEX engines (all additive, flag-gated, read-only; detail in docs/CAPADEX.md + memory)
- Runtime spine: signal capture (`routes/signal-capture.ts`) → composites/patterns (Phase 3) → intervention + explainability (Phase 4) → behavior graph aggregator → insight explainer. Tables `capadex_session_*`.
- Investigation/governance: Hypothesis Investigation (0B), Adaptive Questioning (B), Bridge-Tag Coverage (2), Concern→Signal Mapping, Question Registry (5), Phase-6 deepenings, Simulation Harness (0C, **allowed to FAIL**).
- PIL (Problem Intelligence Layer, Phases 1.5–8): capability↔problem↔behaviour↔archetype↔human↔intervention curated layers + recommendation intelligence (7) + knowledge graph (8, `pil_kg_*`). Curated frames with a quality gate that REJECTS weak generic fallbacks.
- Reporting/commercial (gated by `FF_RUNTIME_INTELLIGENCE_ACTIVATION` etc.): Dynamic Report Intelligence (6C, 4 stakeholder reports), WC-3 chain (stage/outcome/journey/longitudinal/personalization), WC-7b decision, WC-7c commercial. ⚠️ Outcome chain depends on `FF_WC3_OUTCOME_CROSSWALK` + a populated behavioural spine — see `audit/launch-readiness/`.
- Supporting: Enterprise Intelligence (`routes/capadex-enterprise.ts`, `postCompletionHooks`), CSI (`routes/csi.ts`), OMEGA-X report intelligence, BIOS Intelligence (`routes/{lbi-engine,behavioural-signals,predictive-intelligence,tenants}.ts`).

### SuperAdmin Reports Console
- `SuperAdminDashboard.tsx` `capadex-reports` → `UnifiedReportsPanel.tsx` (CAPADEX/LBI/SDI/Competency) → `CapadexReportsPanel.tsx`. `STATUS_STEPS=['pending','in_review','approved','published']`; `getLevelFromScore` ≥80/≥60/≥40 bands.
- Email preview (`backend/routes.ts`): `X-Preview-Subject` header MUST be `encodeURIComponent(subject)` (em-dash; ASCII-only headers).
- **Outcome Intelligence** (MX-102X, flag `outcomeIntelligenceActivation`, OFF byte-identical incl. schema) `routes/outcome-intelligence.ts` + `services/outcome-intelligence-engine.ts` + `superadmin/OutcomeIntelligencePanel.tsx` (nav `outcome-intelligence` in Reports, probe `/api/outcome-intelligence/enabled` res.ok). Read-only composer unifying SIX realized-outcome types (hiring/performance/promotion/retention/career/learning) into ONE surface; COMPOSES validation-loop + employer-tig calibration (never recomputes); Coverage ⟂ Confidence kept SEPARATE; abstains < k_min=30; demo excluded; null never 0. Verdict PARTIAL until realized {pred,outcome} pairs ≥ k_min. Deliverables `backend/audit/mx-102x/*.md` (founder report). → `.agents/memory/outcome-intelligence-activation.md`.
- **Career Launchpad Launch Certification** (MX-302J, flag `launchCertification`/`FF_LAUNCH_CERTIFICATION`, OFF byte-identical) capstone for the A→I Launchpad phases. Read-only composer `scripts/mx302j-launch-certification.ts` + 8-report suite in `services/report-pack.ts` (`composeLaunchpadSuite`/`validateLaunchpadSuite`/`buildLaunchpadSnapshot`/`LAUNCHPAD_SUITE_NAMES`; 16-pack `BUILDERS` byte-identical) + route `GET /api/rf/launchpad-suite/:subject` (auth-before-flag: 401 unauth, 503 OFF, `?export=pdf|csv|json`). FOUR axes Structural⟂Activation⟂Adoption⟂Outcome-Confidence NEVER composited; verdict STRUCTURAL-only/PARTIAL (production-ready withheld by design); activation probed via LIVE HTTP `/enabled` not the cert process env; null≠0; demo excluded; UUID subjects masked. Deliverables `backend/audit/mx-302j/*.md` + `certification.json`. → `.agents/memory/mx302j-launch-certification.md`.

---

## Pragati — CAPADEX Behavioural Conversational Runtime
- Backend `routes/pragati.ts`; frontend `PragatiWorkspace.tsx` (3-panel). APIs `POST /session/start`, `/session/:id/respond`, `GET /session/:id/resume`, `/flow-config`, `/ontology` + admin.
- Runtime: 13-state FSM · 8 block types · 12-concern ontology · adaptive density · 4-dim quality score · crisis-escalation + safety middleware · deterministic fallback.

---

## Career Builder
**Page** `CareerBuilderPage.tsx` (monolith). Spec → `docs/CAREER_BUILDER.md`.
- **Foundation**: tokens `design-system/tokens.ts`; shared UI `components/career/`; engines `lib/engines/` (pure); stores `lib/stores/` (Zustand); DB `migrations/20260519_career_builder_schema.sql` (13 tables).
- **Career Operating System** (additive): `useCareerBrain.ts` aggregates profile/resume/competency/BIOS/CAPADEX/market (never throws); pure engines in `lib/intelligence/` surfaced in EXISTING tabs only. Behavioural memory `routes/behavioural-memory.ts` (DB-backed, distinct from in-memory `career-memory.ts`). IDOR guard `resolveEffectiveUserId`. Cross-module sync reuses `services/adaptive-event-bus.ts` (identity-space trap → `.agents/memory/cross-module-event-sync.md`).
- **Employability Passport** (flag `employabilityPassport`) `routes/employability-passport.ts`; snapshot at `career_seeker_profiles.data.passport` JSONB; contact NEVER published. → `docs/EMPLOYABILITY_PASSPORT.md`.
- **CAPADEX → Career bridge** `services/career-behavior-adapter.ts` (pure); adopted ONLY when `session_id` non-null; consumers take optional `behavior?` arg (absent → identical to before). → `.agents/memory/career-behavior-bridge.md`.
- **Competency Assessment** (AssessmentTab) — `selectAssessmentQuestionsFromAPI` → `GET /api/competency/questions/select`, falls back to static `ADAPTIVE_QUESTION_BANK_V2`. Spec `docs/COMPETENCY_ASSESSMENT.md`.
- **Competency framework canonical authority** — the framework IS the `onto_*` genome + `competency_question_templates` (V1 bank) + `competency-runtime(-v2)` scoring (dual ledger: `onto_competency_profiles` runtime / `onto_competency_score_runs` normalized). Legacy `competency_*` tables are EMPTY shells (admin reads fall back to `onto_*`); the many flag-gated `competency_graph_*/propagation/fusion/ucip_*/sci_*` phases are scaffolded-but-unactivated (empty → parkable flag-off). New competency work extends these canonical surfaces — do NOT add parallel namespaces. Full review: `reports/competency_framework_review.md`. Two `question_type` vocabularies coexist by design: rows are STORED in the scorer's canonical keys (`multiple_choice`/`situational_judgment`/`likert`), and `mapQuestionType` (in `routes/competency-questions.ts`) bridges them to the SHORT render tokens (`mcq`/`sjt`/`likert`) — it now accepts BOTH, so canonical rows render correctly. ⚠️ `rowToQuestion` still forces the Likert scale for any option-less row (`!hasAuthoredOptions`), so a `multiple_choice` row with NO authored options renders as Likert regardless of type — that's why the legacy option-less demo stubs still look like Likert.
- **Resume Studio** `components/career/ResumeStudio.tsx` (embedded). **Fitment panel** `FitmentInsightsPanel.tsx` (Jobs tab; Provisional when `sampleSize<30`).

---

## Constraints (do not violate)
- **Tab name canon**: `TabId` union in `CareerBuilderPage.tsx` — use `'jobs'` (not `'tracker'`), `'mentors'` (not `'mentor'`).
- **Preserve existing UI** — Adaptive Intelligence phases are additive new pages, never edits to `CompetencyDashboard.tsx`/`GapAnalysisPage.tsx`/`CareerBuilderPage.tsx` core/`TrajectoryDashboardPage.tsx`.
- **Language policy** — outputs are developmental signals only, NEVER hiring/promotion/suitability predictions; every envelope ships allowed/disallowed term lists.
- **k-anonymity** — peer benchmarks suppressed below `k_min=30`; cohort responses aggregate-only.
- **Append-only history** — `p4_competency_history`, `m3_*` history tables never mutated in place.
- **Feature flags** — every additive V2 phase ships behind a flag; flag-off → 503 + UI hides.
